---
description: M0 配置/知识点弹窗切换 tab 时保持窗口尺寸不变
globs: src/memoria/ui/static/m0/**
alwaysApply: false
---

# M0 Tabbed Modal 尺寸稳定

配置弹窗（`#config-modal`，`data-config-tab`：知识点 / 链接 / 待确认）与知识点弹窗（`#kp-modal`，`data-kp-tab`：范围 / 标识 / 标签）在 **切换 tab 时不得改变模态框外框尺寸**。

## 实现约定

- 两个弹窗的 `.m0-modal-box` 使用固定布局类 `m0-modal-tabbed`（640×520，内容区滚动）。
- 禁止在 tab 切换时按 tab 切换 `m0-modal-wide` / 不同 width·height 等会改变外框的 class。
- 知识点「范围」tab 仅在 `#kp-body` 上切换 `m0-modal-body-kp-range`，用于预览区 flex 布局，不影响弹窗外框。
- 各 tab 内容高度不同时，在 `.m0-modal-body` 内滚动，而不是撑高/缩小弹窗。
