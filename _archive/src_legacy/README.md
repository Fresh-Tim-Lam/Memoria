# src_legacy 备份说明

本目录为 **2026-07-04** 重构前的完整 `src/` 快照，包含：

- 旧版 `viewer/`（图谱 2D/3D、完整 IDE 前端）
- 旧版 `core/`、`api/`、`intelligence/`
- 临时 M0 代码 `m0/`（已迁移至 `src/memoria/`）

重构后源码仅保留 `src/memoria/` 包结构。如需参考旧实现，在此目录查找。

更早的历史版本见 `_archive/viewer/`、`_archive/memoria/` 等。
