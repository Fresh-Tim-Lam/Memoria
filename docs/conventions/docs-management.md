# Docs 组织管理规范

> **用途**：定义 `docs/` 文件夹的组织规则（子目录职责、根目录准入、文档生命周期），建立**自维护机制**（目录登记表 + 索引同步 + 定期整理），使任何人（含 AI Agent）无需额外提醒即可正确归置文档，并允许目录体系随项目演进自行拓展完善。
> **目标读者**：项目负责人（用户）与 AI Agent。
> **关联文档**：[meta-rules.md](./meta-rules.md)（本规范遵循的元规则）；[collaboration.md](../guides/collaboration.md)（开发场景协作）；[context-key.md](./context-key.md)（CTX-KEY 防火墙）；[refs-system.md](./refs-system.md)（refs 系统）。

---

## 1. 核心原则

1. **唯一落位**：任何文档按 [§2 决策表] 必须能唯一确定归属目录，不允许"放哪都行"。
2. **根目录极简**：`docs/` 根只允许 `README.md`（总索引）与活跃工作清单 `to-dolist.md`。
3. **新文档必登记**：新建文档后必须同步父目录索引（README 或本规范相关表格）。
4. **新目录必登记**：新增子目录必须登记到 [§4.1 目录登记表]，否则视为无效。

## 2. 子目录职责与决策表

| 内容类型 | 归属目录 | 示例 |
|---------|---------|------|
| 契约性规范（人机都要遵守的规则） | `conventions/` | 版本、语法、导入格式、目录组织、日志、docs 管理、元规范 |
| 操作指引（how-to：怎么做） | `guides/` | 知识库生产流水线、AI 编程协作方式、开发/发布操作 |
| 参考说明（understand：架构/术语/机制） | `reference/` | 系统架构总览、术语表、硬约束、图片功能机制 |
| 设计文档 / 决策记录（ADR） | `design/` | system-design、refactor 方案、discussion 归档 |
| 历史对话记录（承接上下文） | `sessions/` | 与原 agents/ 相同，仅改名 |
| 示例知识库 / 测试环境 | `example/` | 示例 KB、rich-content-test、导入测试 |
| 开发参考资料 | `refs/` | 官方文档摘录、论文笔记（遵循参考资料系统规范） |
| 提示词模板 | `prompts/` | 知识整理导入提示词等 |
| 少量诊断 / 调试记录 | `tmp/` | 调试会话记录 md |
| 临时产物 / 日志 | `artifacts/`（仓库根，非 docs） | 日志、截图、一次性脚本（已被 gitignore） |

**决策流程**：`契约性规则（人机都要遵守）? → conventions | 操作指引（how-to）? → guides | 参考说明（理解系统）? → reference | 设计决策? → design | 示例数据? → example | 历史对话? → sessions | 提示词? → prompts | 参考资料? → refs`

## 3. 文档规范（符合 AAA 元规范规则 4）

1. **命名**：kebab-case 小写（如 `markdown-form-std.md`）。
2. **头部三要素**：每份文档前 1-3 行必须包含 `用途 / 目标读者 / 关联文档`（格式见各 conventions 文档）。
3. **状态标记**：规范/决策类文档标注状态（`生效中` / `草稿` / `已废弃`）与日期。
4. **索引同步**：文档新增/移动/删除后，创建者必须同步更新父目录 README 或本规范。

## 4. 自维护机制

> 使 docs 体系在不依赖用户逐条指示的情况下自我演进。

### 4.1 目录登记表（新增子目录时在此登记）

| 日期 | 目录 | 用途 | 说明 |
|------|------|------|------|
| 2026-08-20 | `sessions/` | 历史对话记录 | 由 `agents/` 更名迁移，语义更准 |
| 2026-08-20 | `standards/` | 规范体系 | 纳入版本/目录组织/docs 管理/语法/导入格式等 |
| 2026-08-20 | `context/` | AI 注入与交接说明 | 供 skill 注入与新手 agent 上手 |
| 2026-08-30 | `conventions/` | 契约性规范 | 由 `standards/` 拆分重组，中文文件名重命名 kebab-case |
| 2026-08-30 | `guides/` | 操作指引 how-to | 由 `standards/`（AI 协作）+ `context/`（usage-agent-workflow）迁移 |
| 2026-08-30 | `reference/` | 参考说明 | 由 `context/`（image-features）迁移，新增 architecture/glossary/hard-constraints |
| 2026-09-15 | `reference/agent-guide/` | 面向 Agent 的**功能·排版·交互说明书**（10 篇 + 索引） | 供外部集成方（把 Memoria 嵌进 dsh 的插件等）与后续 Agent 建立准确心智模型；每处描述带 `文件:行号` 证据锚，末节登记"未证实/待确认" |

> 历史沿革：`agents/`（旧）→ `sessions/`（2026-08-20）；`skills/`（空目录，供将来存放可执行技能说明或并入 `context/` 用途）；`standards/` + `context/` → 按性质拆分为 `conventions/`（契约）+ `guides/`（how-to）+ `reference/`（understand）（2026-08-30）。

### 4.2 规范修订记录

| 日期 | 规范 | 修订内容 |
|------|------|---------|
| 2026-08-20 | docs-management.md | 初版建立 |
| 2026-08-20 | logging.md | 新增：日志存放位置、命名、格式约定、生命周期 |
| 2026-08-26 | image-features.md | 新增：图片功能说明文档，登记索引；后续随阶段落地更新 |
| 2026-08-26 | image-asset-dev-plan.md | 新增：图片资产管理分阶段开发方案（design/），六阶段可交互验证；A/B/C/D 已落地并记录验证结果 |
| 2026-08-30 | docs-management.md | 目录体系重构：`standards/` → `conventions/` + `guides/`；`context/` → `guides/` + `reference/`；新增三目录 README 与 reference/architecture、glossary、hard-constraints；决策表/登记表/沿革同步更新 |
| 2026-09-01 | preview-formats.md | 新增：预览渲染格式与样式说明（reference/），登记 reference/README.md |
| 2026-09-03 | docs-management.md | 新增 `screenshots/` 目录登记（README/GitHub 演示截图），docs/README.md 导航同步；后经复核 `screenshots/` 属**非文档内容**（图片资源），随仓库截图约定迁出 docs（目录登记行删除） |
| 2026-09-03 | i18n.md / i18n-inventory.md | 新增：语言系统维护规范（conventions/）与 UI 界面文案登记清单（reference/，由 scripts/scan_ui_strings.py 刷新），登记两目录 README 索引 |
| 2026-09-03 | readme-i18n.md | 新增：README 与项目截图中英双语维护规范（conventions/）；同日 `docs/screenshots/` 迁出至 `resources/screenshots/`（docs 不存截图类二进制资源），登记 conventions/README.md |
| 2026-09-04 | i18n.md / i18n-inventory.md | 编辑块工具栏整文件接入 `edit.block.*`（edit-handler.js，32 候选清零，文件退出清单）；i18n.md 修订记录与 `edit.` 前缀登记、清单迁移记录同步 |
| 2026-09-04 | i18n.md / i18n-inventory.md | 图谱/检索设置页整文件接入（graph-settings.js + graph-label.js + search-settings.js，71 候选清零，三文件退出清单）；新增 `graph.settings.*`/`graph.sample.*`/`graph.labelModes.*`/`search.settings.*`，`SAMPLE_GRAPH` 改 `buildSampleGraph()` |
| 2026-09-04 | collaboration.md | 新增 §0「通用协作规则（存疑对齐，适用于所有场景）」：存疑即停先问后做、反问须具体带选项/推荐、先用尽自解手段再问、对齐后按结果执行、低风险事务不阻塞（guides/，登记 guides/README 指引清单现有条目内，无新文件） |
| 2026-09-04 | i18n.md / i18n-inventory.md / scan_ui_strings.py / js/* | i18n 收尾改为「app.js 拆分 + 随拆分同步 i18n」专项：扫描器剔除规则修订；导入流程抽为 `js/import-flow.js`（`window.MemoriaApp` 门面 + boot init）；其余拆分候选顺序依可行性修订（搜索→图片→KB 检查→文件树→取色器/画笔） |
| 2026-09-08 | export-plan.md | 新增：导出功能设计文档（design/，草稿待评审）。范围=知识库包（kb_bundle）导出（导入 §4C 逆过程）；R12/R14/PDF 明确非本期；含产物规格/RPC/UI/阶段与打开问题 |
| 2026-09-09 | maintenance-jobs.md | 新增：维护作业与静默同步设计（design/，草稿待评审）。以 OS 任务视角统一“保存后派生维护”：作业模型/调度内核/前后端执行边界/作业登记表/P0–P3 阶段与打开问题 |
| 2026-09-09 | to-dolist §12 + 代码复核 | 施工总表（A–E）建立并落盘 to-dolist §12；2026-09-09 代码复核修正：A7 原子写注明范围、B2 partial 上屏、恢复 export-plan.md（此前缺失致引用悬空）、E4 补本登记行 |
| 2026-09-09 | maintenance-benchmark.md | 新增：维护机制基准设计（design/，草稿待评审）。受控语料/指标采集/A–B 对照/门禁规则，回答“调度等机制改造前后改善多少”；当前无基准实现 |
| 2026-09-09 | collaboration.md | §0 通用协作规则新增规则 6「待评审问题须答复后才继续」：Agent 提出的待评审/打开问题未获答复前不得默认继续执行或给下一步方案 |
| 2026-09-09 | M1/A7 原子写 | 正文保存（document.py save_document）与 ui-settings（storage/ui_settings.py）改 tmp+os.replace 原子写并验证（M1） |
| 2026-09-09 | maintenance-benchmark.md + to-dolist §12 + pending 存储 | KP 创建链路归因闭环：pending.yaml 全量 YAML 同步为慢 RPC 主因 → pending 存储改 JSON（constants/pending.py 自动迁移）+ confirm/delete 改 `sync_pending_for_file` 单文件范围同步 + 前端弹窗关窗先于图谱刷新；L2 插桩与克隆计时工具 `scripts/benchmark/maintenance/trace_kp_confirm.py`、结果 `results/kp-confirm-2026-09-09.json`；真机 modal 链 2442.6→981.6ms，后端稳态 4s→285.7ms；to-dolist §12 B5/E5 与 benchmark §6.5 同步更新 |
| 2026-09-09 | maintenance-jobs.md + durable-flush.md | L1 保存路径 A/B 归因（2 轮 ABBA +13.8%，diff 定位 M1 fsync；分项剖析 manifest_touch ~50ms 为大头，tool `trace_save_document.py`）→ jobs.md 登记 durable_flush(P0)/manifest_touch_batch(P2)；新增设计 `design/durable-flush.md`（写盘两级 + 屏障触发 + 崩溃语义 + 门禁，草稿待评审）；to-dolist G4 改 M6a |
| 2026-09-09 | G4 M6a 施工 + 设计锁定 | durable-flush 决策锁定（Q1–Q4）；施工：save_document barrier 默认（`MEMORIA_FSYNC_MODE` 开关）+ `durable_flush` RPC + manifest 模块 pending overlay（读侧一致、任一写/屏障批量落盘）+ 前端 3s 防抖/切文件/关库/退出屏障；冒烟 PASS；实测 inline 89.35 → barrier 24.62ms（-72.5%，results/durable-flush-2026-09-09.json）；to-dolist §12 G4/E7 更新 |
| 2026-09-09 | G4 M3 + G3 收口 | G3 门禁通过（VM 全 PASS + 真机陈旧丢弃/queued=0，tag `maint-g3`）；M3 词法索引后台化（锁+合并 daemon，写路径不阻塞；search/切库/关库 wait）；results/m3-lexical-background-2026-09-09.json；to-dolist G3/G4/E8 更新 |
| 2026-09-09 | M6b 施工 | M6b 拍板：KP 创建/更新保持内联不入队；正文编辑行号范围实时同步（app.js：M6b 辅助函数 + Enter 区域并入/顺延、行前插空行吸收、Backspace/Delete 删行收缩；hover 高亮与列表行号实时更新，保存后端 resync 权威校正）；单测 15/15（scripts/benchmark/maintenance/m6b_adjust_test.js）；to-dolist G4/E9、jobs.md kp_create 同步 |
| 2026-09-09 | G4 收口 + C4 拆分 | M6b 迭代：预览 AST 引擎补 `adjustKpRangesAfterSpanReplace` + 即时权威解析 RPC `resolve_kp_ranges`（前端 420ms 回写）+ 每次保存后静默校正（不再仅 resync>0）→ 真机通过。G4 出口汇总 results/g4-gate-summary-2026-09-09.json；C4 图谱局部刷新拆独立后续项（engine.applyDelta 方案要点与风险）；快照 tag `maint-g4` |
| 2026-09-10 | kb-agent.md | 新增：知识库 Agent（Trae 智能体）设计（design/，草稿待评审）。锁定载体=Trae 智能体、契约落点 `.memoria/agent/`、写入面=知识库内容（`.md`+`.memoria/*`）、复习=FSRS、一 KP 多卡、四级评分、内置确定性脚本 `fsrs.py`；含目录布局/三条工作流/数据模型/产品改动清单/用户操作指示。**含「撰写」能力（§4.2A：来源可溯/先查重/必挂链）**，并明确 KP 元数据权威=sidecar（frontmatter 不参与解析）。同步新增随应用分发的提示词 `resources/agent-prompts/kb-agent.zh-CN.md`（**智能体指令**）与 `resources/agent-prompts/kb-spec.zh-CN.md`（**知识库编撰/维护规范**，独立随版本升级），并登记 `resources/README.md`（补 `agent-prompts/` 目录行） |
| 2026-09-10 | KB Agent 落地（T1–T5） | 新增 `resources/agent-prompts/{kb-agent.zh-CN.md,kb-spec.zh-CN.md,kb-agent-readme.md,fsrs.py}`；后端 `services/kb_agent.py`（原子写 + 幂等 + 不覆盖 `review/**`）+ RPC `install_kb_agent` / `get_kb_agent_prompt`（ui.py）；前端「文件 → 创建 Trae 智能体」（新 `js/kb-agent.js` + index.html 菜单项/弹窗 + app.js 一行初始化 + i18n 中英）；打包登记 `build.py _REQUIRED_RELEASE_RESOURCES`。验证：`fsrs.py --selftest` 16/16；真装幂等（二次 `written=[]`）且 `review/` 不被覆盖；有无 `.memoria/agent/` 的 `validate_kb` 输出完全一致（errors=0）；`py_compile`/`node --check`/`scan_ui_strings.py` 通过。to-dolist §9 登记 V02 更新 + V06 |
| 2026-09-10 | KB Agent 细化 | ①菜单项不再禁用：无 KB 时点击先唤起「打开知识库」（`MemoriaApp` 导出 `openKb`）；②补分发**支持格式说明**——`install_kb_agent` 新增 `REFERENCE_DOCS`，把 `docs/reference/preview-formats.md` 复制为 `<kb>/.memoria/agent/preview-formats.md`，使 KB 内智能体可读渲染写法（kb-spec/指令/README 指向同目录该文件）；③**打开知识库自动补写/刷新**——`initKb`/`openKb` 后调 `MemoriaKbAgent.ensure()`（幂等、不阻塞、不弹窗，实际写入时提示），旧库首次打开即补齐、升级后再开即刷新（**有意例外于"禁止 silent 写入"**，缓解见 kb-agent.md §7.5）。验证：真装 5 文件且仍幂等；`node --check`/`i18n_selftest`/`scan_ui_strings`(rows=0) 通过 |
| 2026-09-10 | kb-agent.md 评审收口 | Q1 ✅ 固定 FSRS-5 默认权重（不训练/不调参）；Q2 ✅ 新卡按 KP 批量确认后落盘；Q4 ✅ `log` 保持 append-only；Q5 ✅ 一 Trae 工作区 = 一知识库。§9 更名「评审项与结论（全部已拍板）」，§1/§4.3/§6 与内置指令同步 |
| 2026-09-14 | operations.md | 新增发布规范：§5.1 发布资产命名（完整包/轻量包 `Memoria-v<版本>-win64[-lite].zip`、双形态同时上传）；§5.2 Release 说明双语上下排版（中文段 → `---` → 英文段）、截图必备条款（绝对 URL、中英各引一次、无截图先入库） |
| 2026-09-14 | 截图登记 | 新增演示截图 `resources/screenshots/demo-check-report.png`（知识库检查面板 + 「复制报告」toast，1474×888 自动捕获）；`README.md` §Screenshots / `README.cn.md` §界面预览 各增一条先说明后贴图；`readme-i18n.md` §3.1 场景清单与 §5 修订记录同步 |
| 2026-09-14 | ledger-maintenance.md | 新增：**台账维护规范**（conventions/）。针对首轮对账发现的台账失真（11 条已收口仍开放 / 7 条描述与代码不符 / 3 处内部矛盾），确立七条规则：单一事实源与写者唯一、状态标记 + **四类分区**（真开放 / 代码完成·验收未闭环 / 待评审 / 已收口，禁止混装）、**证据锚必填**（实现/产物/评审/验收四类合法形式）、**收口判据**（实现类/行为类/评审类/边界类分列）、收口动作与归档（就地改状态 + §10 记批次，不物理搬行）、**定期对账**（触发点/五类清单产出/落点 `artifacts/_audit/**`）、禁止事项七条；附三处已知漂移（`to-dolist.md` 命名、`artifacts/agent/**` 契约空转、maintenance-jobs §4/§5 不一致）。登记 conventions/README.md 索引 |
| 2026-09-14 | deepseek-harness-integration.md | 新增：DeepSeek Harness（dsh）与 Memoria **双向融入**的可行性分析与分阶段规划（design/，草稿待评审）。**D1**＝dsh 融入 Memoria；**D2**＝Memoria 作为插件融入 dsh（正由另一项目推进）。含 dsh 事实基线（官方来源 + 未证实清单）、Memoria 四条红线与**对外可调用面**（CLI 已自带 `--json` 与 `--apply` 预览语义）、10 条冲突面（X9 跨进程并发写 / X10 沙箱与知识库位置）、D2 传输面 T1–T3 与分级能力面 L0–L2、阶段 0–3 规划与验收、13 条风险、对已拍板决策的裁决项、阻塞问题 Q1–Q9 与建议门禁登记 H1–H6（待批准）、附带发现（`to-dolist.md` 引用悬空）。**本次仅文档，未改任何代码** |
| 2026-09-15 | reference/agent-guide/ | 新增：面向 Agent 的**功能·排版·交互说明书**（10 篇 + 索引 + 维护约定）。逐处对代码取证（`src/memoria/ui/static/app/**`），每处描述带 `文件:行号` 证据锚，各篇末含「代码锚点表」与「未证实/待确认」。首轮取证并发现**三处既有文档与代码不符**（已在索引 §3 登记待修）：① `static_server` **不存在 `/rpc`**（`/rpc` 是测试 harness 自建端点）—— 与 architecture.md / deepseek-harness-integration.md 的说法冲突；② `hard-constraints.md:31`「保存自动清理未引用图片」与 `document.py:317-323` 相反；③ 事实源口径三处不一致。登记 `reference/README.md` 索引与目录注册表 |
| 2026-09-16 | graph-benchmark.md | 新增：**图谱引擎性能基线与渲染优化**（design/，草稿待评审），服务台账 G05。现状诊断 D1–D11（结论：**拖拽掉帧主因是布局每帧固定 6 次 O(N²) 排斥**，1000 节点档布局:绘制 ≈ 60:1，故不应先改渲染算法）+ 附带发现 X1–X3（视图 opts 缺键经 `_layoutOptsFromView()` 静默毒化布局默认值致仿真发散成 NaN；散落分布取消跨群排斥屏蔽致「全部」页签成本跳到满 O(N²)；排斥核无上限致偶发数值爆炸，实测最大边长 2.13e9）；按场景指标（拖拽/静止/平移缩放/hover/拾取/首帧）+ **每帧分账**；L1 无头（`scripts/benchmark/graph/render_l1.mjs` + README，含防静默 NaN 守卫）与 L2 浏览器两层；A/B 门禁定量阈值（静止 3s `idle_draws` ≤2、1000 档 `frame_ms` p95 ↓≥40%、`first_paint_ms`/`longtask` 不升、布局质量无可见退化）；批次 D1/D2/B5/B/C/A/X 与 P0–P8 顺序。同日锚定改造前基线 `results/render-baseline_346e5f95.{json,md}`。**同日 D1（帧预算化）落地并出 A/B**：`sim-core.runTicksWithBudget`（2D/3D/worker 共用）+ 采集器 `--frame-budget` + 新增 `compare_ab.py`；目标档整帧 p95 star 5000 9252→710ms（−92.3%），未达标项全部为"单 tick 本身 >20ms" ⇒ D2 为必做项 |
| 2026-09-16 | ledger-maintenance.md + docs/todo.md | **规则 8：上下文预算与已收口条目的清理**（台账是每次会话都要读入的上下文）。要点：体积预算（`todo.md` ≤ 36 KB、`maintenance-jobs.md §5` ≤ 8 KB）、**禁写开发流水账**（过程追述/进度罗列/键名枚举属 CHANGELOG 与提交记录）、**压缩前事实须先转移到对应文档**再删、**✅ 条目按批次出表**入 §10（`| 日期 | ID 列表 | 一行汇总 | 证据锚 |`）、**K2/K3 不得被清掉**、可追溯性下限（§10 + `git log -S <ID>`）；同步修订规则 5.3 与规则 7.7 的适用边界。同日按规则 8 清理 `docs/todo.md`：86.8 KB → 32.4 KB（442 → 279 行），49 条 ✅ 归入 §10（19 批次 / 68 个 ID），并归位两段用户粘贴的 bug 记录（E22 补复现记录、新增 E23） |
| 2026-09-15 | frontend-modules.md | 新增：**前端 JS 模块化规范**（conventions/）。针对 `app.js` 12,933 行（是第二名 6.7 倍）的单文件堆砌，确立六条规则：R1 新功能主体不进 `app.js`、R2 `init({...})` 显式依赖注入（禁读闭包私有符号）、R3 单向依赖、R4 **触及即搬**的增量重构（一次一件、纯搬移不改语义、`node --check` + 真机复测）、R5 共享纯函数放 core 文件（禁 `EH.xxx` 跨文件私有导出互借）、R6 内核（`state`/`$()`/`log`/`markDirty`）最后搬且**禁止一次性大爆炸重写**；附候选迁移顺序与提交前自查清单。首例落地 `sel-source.js`（源码区键盘 + 鼠标选区迁出 `app.js`）。登记 `conventions/README.md` / `README.cn.md` 索引 |
| 2026-09-16 | graph-benchmark.md（续） | 同日续做三批并全部 A/B 验证：① **X4 止血**（warmup 工作量上限 `WARMUP_PAIR_TICK_CAP` + worker 就绪超时 3s→15s，装载 3000 节点 47–51s→4.3s、5000 节点 118–136s→4.2s）；② **tick 内部剖面**（`opts.profile` + 采集器 `--profile`，§5.4：pair 循环占单 tick 98.4–99.9%、计算一对 ≈51–70ns；并**证伪**"`dist2<1` 守卫是性能热点"的假设）；③ **D2 Barnes-Hut 落地**（新增 `graph-bh-tree.js`，θ=0.9，分组语义与 `skipPairRepulsion` 严格对齐、拖拽节点单独处理）+ **X3**（`distanceMin` 下限取代 `Math.random()` + 位移上限）：star 5000 单 tick 745→33.9ms（22×）、整帧 9108→41.5ms（212×）、边长峰值 2.13e9→约 5000、装载 4.2s→0.35s。**门禁未全达**（单 tick ≤20ms 仅 partitioned 达标），剖面定位原因为 `nsPerBhInteraction=192ns`（每 tick 约 1.2 万格子对象分配）⇒ 新增批次 **D2b（树扁平化/免分配）**。用户同时明确：**不设节点间距指标、不作质量门禁**（§8 Q2 改写） |
| 2026-09-16 | README 截图整批重拍 + v0.3.4 | 按用户要求「之前的图不要了」重拍全部 8 张演示截图（`resources/screenshots/demo-*.png`，**1536×816** 窗口客户区）：图谱两张取自「软件工程概论」知识库（先拖宽图谱面板再居中），样式类取自仓库内示例库 `docs/example/showcase`；同步修正两处与图不符的配图说明（预览不再声称荧光笔、公式不再声称 cases/align）、两张 README 的 Version 徽标（0.3.2→0.3.4）、`readme-i18n.md` §3.1 分辨率基线与 §5 修订记录。同日发 v0.3.4（`version.md` CHANGELOG 三条：图谱性能主体 / 分布模式「散落」/「构建」连点幂等修复） |
| 2026-09-16 | scripts/screenshots/ | 新增截图采集工具 `hotkey_capture.py`（热键触发抓窗口客户区 + 16×16 亮度指纹去重；此前仅为 `%TEMP%` 下的一次性脚本）：该批 8 张截图即由它采集，`readme-i18n.md` §5 采集方式指向本工具；同步登记 `scripts/README.md` / `README.cn.md` 内容表 |
| 2026-09-16 | example/AAA_Vocab 公开 + site/ + Pages | 公开 IELTS 词汇库（`docs/example/AAA_Vocab/`）：`.gitignore` 政策由「仅展示 showcase 官方库」改为「showcase + AAA_Vocab」（运行时产物 `.memoria/agent|images`、`cache`、`pending.*`、`kp_targets`、`*.bak|log`、`.claude/` 仍忽略）。对外指引页另落**新增一级目录 `site/`**（`index.html` 中英双版 + `GUIDE.md` Markdown 备份 + `images/` 12 张；登记见 [directory-organization.md §1](./directory-organization.md)）；`.github/workflows/pages.yml` 用 GitHub Actions **只上传 `site/`** 发布 Pages（站点根即指引页）。推荐提示词副本 `docs/example/AAA_Vocab/prompts/IELTS-vocab.md` |
| 2026-09-17 | design/dsh-agent-port.md | 新增：**dsh 代码级移植方案（D1′，主线）**。用户拍板口径：逐块吃 `dsh` 代码、**Python 重写**、产品形态=应用内对话 + 远端模型、出网可应用内开关。含：上游事实基线实测（仓库 LICENSE=MIT 与 npm 字段 BSD-3 不一致、12,944 文件/3,634 ts、**pin `0d1f5000`**、`python/sdk-runtime` 实为引导 Node 运行时的壳 ⇒ 官方"Python SDK"不是本路线）、包级映射表（`llm`/`core`/`session`/`context`/`interaction`/`credentials` ✅；`sandbox`/执行/编排/`api`/`sdk` ❌）、M1 只读竖切 + 文件级清单 + Python 落点、四条红线逐条落法、M1–M4 分阶段、6 条风险、P1–P7 待拍板。同步在旧稿 `design/deepseek-harness-integration.md` 顶部加口径变更提示（D1/D2 降为旁支）。**合规核实（更正早前误报）：`LICENSE` 本就存在（MIT，署名 `FreshTim`，`730f9a8a Initial commit`）；实际只缺 `THIRD_PARTY_NOTICES`，已记入方案 §4.3 待补** |
| 2026-09-17 | dsh 移植 M1a 落地 | 新增 `src/memoria/services/agent/llm/**`（10 文件，**纯标准库**：provider 中立接口 / OpenAI 兼容适配 + 手写 SSE / 重试退避 / 用量计量 / 配置与密钥掩码）+ `scripts/agent_llm_smoke.py`（`--mock` 离线冒烟）+ `tests/test_agent_llm.py`（21 例）+ 仓库根 **`THIRD_PARTY_NOTICES.md`**（dsh MIT 原文 + pin `0d1f5000` + npm 字段 BSD-3 差异说明；根目录白名单已同步 `directory-organization.md §1`）。验收：`py_compile` 12/12、`pytest 21 passed`、`--mock` 流式通过、零第三方依赖、12/12 带来源注释、**无既有文件被改**；语义偏差见方案 §6.5。同步登记 `scripts/README.md` / `README.cn.md` |
| 2026-09-17 | dsh 移植 M1b 落地 | 新增 `src/memoria/services/agent/{loop,prompt,approvals,ask}.py` + `tools/{__init__,registry,kb}.py` + `session/{__init__,store}.py`（共 11 文件 / 2,236 行）+ `scripts/agent_ask.py` + `tests/test_agent_loop.py`（12 例）。**只读竖切打通**：提问 → `search_kb` 检索 → 远端模型 → 带 `文件:行号` 锚点回答 → 会话落盘 `<kb>/.memoria/agent/sessions/*.jsonl`。验收：`py_compile` 全过、`pytest 33 passed`、`--mock` 离线跑通、**零写入**（逐文件 mtime+size 对比仅新增会话文件；jieba 词典缓存已重定向到系统 temp）。语义偏差（`max_iterations=8` 本地新增 / 会话格式与上游 v3 不互通 / 指令注入进 system 段 / 只走 lexical 检索 / 零写入依赖 4 个写点重定向）见方案 §6.6。同步登记 `scripts/README(.cn).md` |
| 2026-09-17 | dsh 移植 **M1 前端首版**（侧栏「对话」面板）+ 文档同步 | 前端：新增 `js/agent-panel.js`（595 行，`window.MemoriaAgentPanel = {init,open,clear}`，自绑定事件、只经 `MemoriaApp` 门面取服务）、`index.html` 第 4 个页签 + 面板骨架（116、135-178，共 +45 行）、`app.css` `.-agent-*` 段（324-532，209 行，只用既有主题变量）、`i18n/{zh-CN,en}.js` 新增 `agent.*` 66 键；`app.js` **仅 1 行**（boot 加 `MemoriaAgentPanel?.init?.()`，12853）——侧栏页签切换本就是按 `dataset` 遍历的通用实现，无需扩展。后端：`presentation/api/ui.py` 追加 4 个 RPC（`agent_get_config` / `agent_save_config` / `agent_ask_start` / `agent_ask_poll`，1145-1213，**不改既有方法语义**）；新增 `services/agent/ask_stream.py`（258 行：独立单线程执行器 + 增量缓冲 + cursor 轮询作业面；**不占用** MaintenanceExecutor）；`services/agent/llm/config.py` 追加 `read_raw_config` / `is_enabled` / `save_config`（写侧浅合并 + 原子写，`api_key` 空值不覆盖）并把 `enabled` 纳入已知键。**关键发现：增量回调钩子已存在**——M1b 的 `ask(on_text=...)`（ask.py:128 → loop.py:118）本就是逐片实时投递，故本轮**未改 `ask()`/`loop()` 任何签名或语义**。文案与锚点：`docs/reference/agent-guide/01` 新增 §2.3.1（含四页签、滚动/折叠/忽略本次/换库丢弃等）并重取 tree 行号（发现既有 index.html 锚点整体漂移 13 行，已登记换算规则）、`10` 新增 §2.15（4 RPC + `config/agent.json` + 会话事实源）与 §1.1/§1.2 补充；`conventions/i18n.md` §3 登记 `agent.` 前缀 + 修订记录；`reference/i18n-inventory.md` 由 `scan_ui_strings.py` 刷新（files=3 rows=5，新文件 0 候选）。验收：`node --check` 4 文件通过、`py_compile` 4 文件通过、`i18n_selftest.js` 12/12 PASS、`scan_ui_strings.py` 无新增候选；**浏览器复现 harness（端口 8642，`MEMORIA_CONFIG_DIR` 指向临时目录以免读写用户真实 `config/agent.json`）**通过 8 项断言：① 第 4 页签存在且点击后视图 `display:flex`/可见、其余视图 `.hidden`、`localStorage["-sidebar-tab"]=agent`（刷新后复现）；② 面板关键 DOM 齐全；③ 空输入点发送 → `agent.err.empty_question`，无副作用；④ 未配置端点发送 → `agent.err.no_base_url`（状态行转红 + flash 卡片 + 助手气泡错误条）；⑤ 出网开关点关/点开 → `agent_save_config` 落盘 + 标签追加「出网已关」+ 发送 `disabled`；⑥ 配置经 `/rpc` 写入后重激活页签 → 端点/模型/超时回填、密钥输入框留空且 placeholder 为掩码、`sk-…` 明文不出现在 DOM；⑦ 真实跑一次作业（指向 `http://127.0.0.1:9/v1`，必然拒连）→ 「生成中…」期间发送禁用且「忽略本次」出现 → 结束为 `agent.err.askFailed + 后端原文`（含 `WinError 10061`）；⑧ 点「忽略本次」→ 状态行如实说明「后端仍在跑」，随后立即再提问 → `agent.err.busy`。**不可断言**（真机人工项）：真实模型的流式节奏与答案质量、设置区「保存」按钮的原生点击（harness 窗口仅 376×271，展开设置后该按钮落在面板可视区之外）、两套主题的观感 |
| 2026-09-17 | 对话面板**由左栏第 4 页签改为右侧独立 dock**（回退参照：左栏版提交 **`8f2e88f6`**） | 布局：`index.html` 净 +4 行——删 `data-sidebar-tab="agent"` 按钮与 `#sidebar-view-agent` 容器，在 `#content` 之后新增 `<aside id="-agent-dock">`（**面板 markup 原样迁入，id 与 `.-agent-*` 类名不变**）+ `#agent-dock-resizer`（左缘拖拽柄）+ 浮动 `#agent-dock-collapse-btn`；顶栏 `.toolbar-actions` 末位新增 `#btn-agent`（照 `#btn-check` 写法）。样式：`app.css` +107/-32——新增 `#-agent-dock`（**默认 22rem / 最小 16rem / 上限 34rem**，`rem` 计量随根 `font-size` 缩放）、`.-agent-dock--collapsed`（宽 0 + 隐藏拖拽柄）、拖拽柄与折叠按钮（镜像 `#-sidebar`/`.-sidebar-collapse-btn`，**层级 `z-index:90` 低于 `.-modal`(1000)/搜索(9000)/flash(12000)**）；`.-agent-*` 按 22rem 重算（字号 0.75→0.8125rem、内边距 8→10px、用户气泡 `max-width:85%` 靠右）；**顺手修掉矮窗口「保存」按钮被裁**：`#agent-settings` 加 `max-height: min(24rem,45vh)` + `overflow-y:auto`。逻辑：`js/agent-panel.js` +146/-8（dock 折叠/拖拽/持久化 9 个函数 + init 改绑 `#btn-agent`/折叠按钮/拖拽柄，去掉页签监听；`open()` 改为展开并刷新）；**持久化键 `layout.agentDockWidth`（rem 数值）/ `layout.agentDockCollapsed`** —— 因后端 `save_ui_settings` 是**顶层浅合并**（`storage/ui_settings.py:58-75`），实现改为「先 `get_ui_settings` 读旧 `layout` → 合并 → 写回」，避免抹掉 `layout.sidebarWidth`。i18n：`agent.` **+5 键**（btnTitle/dockAria/dockCollapseTitle/dockExpandTitle/dockResizeTitle），`agent.tab` 复用为顶栏按钮文案（中英成对）。`app.js` **仅 +3 行**（`setSidebarTab` 内 1 行守卫：存量 `localStorage["-sidebar-tab"]="agent"` 命中不到视图时回退 `files`，否则三视图全隐藏 + 2 行注释）。文档：`reference/agent-guide/01` 改为「左栏 3 页签 + 右侧 `#-agent-dock`」（§2.3 重写、新增 §2.4 含实测尺寸表、§2.4~§2.10 顺延重编号、§1 树/§2.2 顶栏表/§5 已知坑/§6 锚点/§7 未证实 全部同步，**并修正 08/10 两篇指向 01 的失效交叉引用**）、`agent-guide/README.md`（275→305 行）、`10` §2.15/§3.1 与 `08` §5 锚点跟随、`conventions/i18n.md` §3 与修订记录；`reference/i18n-inventory.md` 由 `scan_ui_strings.py` 刷新（files=3 rows=5，仅 app.js 行号 +3）。验收：`node --check` 4 文件通过、`i18n_selftest.js` 12/12 PASS、**headless Edge + harness（端口 8642，`MEMORIA_CONFIG_DIR` 指向临时目录；注入断言脚本 + `/assert_result` 落盘）** A 组 1576×668 **12/12 PASS**（三页签与切换、dock 默认可见且 computed width=352px、`#btn-agent` 折叠/恢复、拖拽夹取 34rem/16rem、**预置 `sidebarWidth:301` 落盘后仍在**）、B 组 876×271 **7/7 PASS**（设置区 clientHeight 121 / scrollHeight 267 可滚到「保存」）、B′ 组 492×179 同为 7/7；真实 `config/ui-settings.json` 与 `config/agent.json` 的 SHA256 与 mtime 前后**完全一致**。**未取证**：真实模型端到端效果、`uiScale` 下 dock 宽度的实测换算、652–900px 窄窗口的三栏挤压断点。**同日第三轮补上「文档区最小宽度保护」**（同一行合并登记）：`js/agent-panel.js` 新增 `CONTENT_MIN_PX=360`，把「**期望宽度**」（`layout.agentDockWidth`，只在拖拽/显式操作时写盘）与「**生效宽度**」（每次布局变化按 `available = #main.clientWidth − 左栏生效宽 − 360` 重算：折叠→0；`available ≥ 16rem`→`clamp(期望, 16rem, min(34rem, available))`；否则→0）分开，生效宽度只写内联 `width`（不覆盖 CSS min/max）；空间不足时加 `-agent-dock--auto-hidden` **临时自动隐藏**（**绝不回写盘**，窗口变宽自动还原到期望宽度），自动隐藏时点浮动/顶栏按钮只弹 `agent.dockNoSpace` 提示并保持隐藏（不允许挤压文档区）；重算时机用 `ResizeObserver` 盯 `#main` / `#-sidebar` + `window.resize`，**未改 `app.js` 的左栏代码**（左栏行为零变更）。文件：`agent-panel.js` +约75/-8（新函数 `sidebarEffectivePx` / `availableDockPx` / `applyDockLayout` / `toggleDock`，原 `applyDockWidth` 移除并拆成「期望值赋值 + `applyDockLayout()`」）、`app.css` +14（`.-agent-dock--auto-hidden`）、`i18n/{zh-CN,en}.js` 各 +2 键（`agent.dockNoSpaceTitle` / `agent.dockNoSpace`）；文档同步 `reference/agent-guide/01` §2.4（新增 3 行 + 实测尺寸表整表重测 + §5 坑 11/12 重写 + §6/§7 同步）与本节。验收：`node --check` 4 文件、`i18n_selftest.js` 12/12 PASS、`scan_ui_strings.py`（files=3 rows=5 不变）；**headless Edge（`--headless=new`）+ harness + CDP（端口 8643，`MEMORIA_CONFIG_DIR` 指向临时目录，视口用 `Emulation.setDeviceMetricsOverride` 收窄/恢复）29/29 PASS**：逐档核对生效宽度公式（1576→352/923、1000→339/360、917→256/360、916→自动隐藏/615）、隐藏态点为不展开且弹提示、恢复宽视口免点击自动回 22rem、左栏拖到 480 后 dock 让位、拖拽中上界 = `available`（539 < 34rem）且**松开才落盘**、盘上 `agentDockWidth` 在整轮自动收缩/隐藏后仍为 22 且 `sidebarWidth` 保留、页面无 JS 异常；补测「启动即窄视口首帧隐藏 + 浮动按钮 20×40 完全在视口内 + 三态 title 随语言切换」5/5。真实 `config/ui-settings.json` / `config/agent.json` SHA256 前后完全一致。**未取证**：`uiScale≠1` 的 rem 换算、左栏折叠时按 0 计的端到端链路、原生鼠标事件。**行为变更**：宽 ≤916px（左栏 301px 时）dock 改为自动隐藏，上一轮的「876×271 设置区可滚到保存」需在 ≥917px 宽窗口复现。**同日续（2026-09-18，M1c：多轮续聊 + 会话历史，同一行合并登记）**：后端新增 `services/agent/session/history.py`（292 行，纯标准库）——`build_history()` 把会话事件**回放**成可再发的消息序列（**全保真**：assistant.tool_calls + 紧随其后且 `id` 配对的 tool 消息；`id` 空/数量不等/顺序不符的**异常轮整轮丢弃**，绝不留下孤立 `tool_calls`），容量上限 `MAX_HISTORY_MESSAGES=40` / `MAX_HISTORY_CHARS=32000`（**从最新往旧保留**整条消息、至少保留最新 1 条，再丢弃因截断变成孤儿的前导 tool 消息）；`summarize_session()` / `conversation_messages()` 供只读视图（每轮只出**一条** assistant 气泡 = 该轮最后一条非空 `assistant/message`；该轮 `tool/result.anchors` 去重后挂在该气泡上）。`ask()` 追加 `replay: bool = True`，且 `session_id` 指向**已存在**会话文件时先回放为 `messages=`（**在追加本轮 `user/message` 之前**，故不重复），`session_id` 省略/文件不存在 ⇒ 与 M1b 完全一致；`ask_stream.AskJob.resume_session_id` 透传。`presentation/api/ui.py` 追加 2 个**只读** RPC（`agent_sessions_list`：按 mtime 倒序 ≤30 条，含 `turn_count`/`preview`/`title`；`agent_session_load`：渲染视图，未知会话 ⇒ `code:"unknown_session"`），并给 `agent_ask_start` 追加**可选第三参** `session_id`（省略即原语义不变，`SESSION_LIST_LIMIT=30`）。前端：`index.html` +4 行（历史行 `.-agent-history` + `<select id="agent-history">`）、`app.css` +34 行（`.-agent-history` 5 条规则 494-526，其后各条行号下移 33/34）、`js/agent-panel.js` +217/-12（`sessionId`/`sessionKb` 续聊状态、`renderHistory`/`syncHistorySelection`/`refreshHistory`/`loadSession`、等待计时 `tickWait`/`startWait`/`stopWait`（「生成中… Ns」）、`ask()` 带 session 并回写、`clear()` = 开新会话、换库作废会话 id）；`i18n/{zh-CN,en}.js` 各 +10 行（`agent.history.*` 5 键 + `agent.status.elapsed` + `agent.err.unknown_session`/`sessionFailed`，中英成对）；**`app.js` 零改动**（历史刷新挂在 `init`/展开 dock/提问结束/语言切换，沿用既有门面）。文档：`reference/agent-guide/10` §2.15 重写为「6 RPC + 多轮续聊 + 回放保真度/容量上限与截断 + 会话历史只读视图」，§1.1/§1.2/§3.1/§4/§6/§7 同步；`01` §2.4 增「历史会话下拉」「伪流式与等待计时」两行、§1 tree/§2.4 全表/§5 坑 15/§6/§7 按新行号重取（`index.html` dock 之后 +4、`app.css` 其后 +33/+34、`agent-panel.js` 全表重锚）。验收：`py_compile` 5 文件、`node --check` 3 文件、`i18n_selftest.js` 12/12 PASS、`pytest tests/ -q` **44 passed**（新增 `tests/test_agent_history.py` 11 例：序列合法性 / 异常轮成对丢弃 / 从最新截断 / 不留前导孤儿 tool / 续聊请求体含第一轮 / 全新会话无历史 / `replay=False` / `user/message` 2 条且 `seq` 连续 / 渲染视图与锚点归属 / 摘要）；`scan_ui_strings.py`（files=3 rows=5，无新增硬编码候选）；**端到端 harness `/rpc` + 本地假 SSE 端点（照 `--mock` 帧格式）27/27 PASS**（两轮共用 `session_id`、第二轮请求体含第一轮 user+assistant 且末尾是当前问题、`agent_sessions_list` 的 `turn_count=2`/`preview=第一问`、`agent_session_load` 返回 4 条、未知会话 `unknown_session`、两次提问只新增会话 JSONL 且两个只读 RPC 前后 KB 文件快照一致）；真实 `config/agent.json`（SHA256 `c9cfc8b7…`）与 `config/ui-settings.json`（`88f65cd1…`）前后**完全一致**。**未取证**：真实模型端点下的续聊效果、浏览器里点选历史下拉的原生交互与计时观感、长会话（>40 条 / >32000 字符）截断后的模型表现。**同日再续（2026-09-18，M1 收尾：确定性连接失败不重试 + 面板 epoch 作废）**：后端 `services/agent/llm/errors.py` 新增稳定 code `UNREACHABLE`（连接被拒 / DNS 解析失败 / TLS 证书校验失败 / 无效 URL）——**刻意不进** `RETRYABLE_CODES`（本轮**未增未删**，仍为 `EMPTY_RESPONSE`/`RATE_LIMIT`/`SERVER`/`TIMEOUT`/`TRANSPORT`），列入 `_TERMINAL_CODES`；新增 `is_unreachable()`（先剥 `urllib.error.URLError.reason` 再分类，对已分类的 `UNREACHABLE` 幂等）、`is_retryable()` 的裸 `OSError` 分支改为「非确定性才可重试」、`classify_detail()` 收确定性措辞（`connection reset/aborted/broken pipe` 仍归 `TRANSPORT`）；`llm/providers/openai_compatible.py` 新增 `_transport_error()`（建立连接 + 读取响应两条路径统一分类）与 `http.client.InvalidURL` 分支，错误消息含**目标 URL + 底层原因 + 「确定性失败，不会重试」**；`services/agent/ask_stream.py` 新增 `PANEL_RETRY_POLICY`（`max_retries=2` / `total_timeout_s=20`，**库层 `retry.py` 的上游默认值未改**）并传给 `ask()`；`services/agent/loop.py` 把 `iter_with_retry` 的 `on_retry` 接进计数器，最终错误串在 N>0 时追加「（已重试 N 次）」、`step/error` 事件附 `retries`（`AgentLlmError` 公开字段未变）。前端 `js/agent-panel.js` 引入**世代号（epoch）**：每次提问 `epoch += 1` 并记进 `job.epoch`，提交/轮询回调只在 `epoch` 仍是最新时写回（`sessionId`/气泡/状态行/锚点）；**「清空对话」与「忽略本次」都 `epoch += 1` 且 `job = null`** ⇒ 在飞结果一律作废（停止轮询、不写回会话 id），`clear()` 一并复位 `busy`/等待计时器（生成中点「清空」不禁用——按用户口径允许清空但作废结果）；i18n **未加键**、`app.js` **零改动**。文档：`reference/agent-guide/10` 新增 §2.16「重试与失败分类」（含 A/B 实测）、§2.15/§6/§7 同步；`reference/agent-guide/01` §2.4 新增「清空对话（生成中亦可用）」「世代号（epoch）作废机制」两行、改写「忽略本次」与「伪流式与等待计时」、全表 `agent-panel.js` 行号重取（1-1025 → 1-1051）、§6/§7 同步；`conventions/i18n.md` 行号跟随（1 处）。验收：`py_compile` 5 文件、`node --check` 1 文件、`i18n_selftest.js` 12/12 PASS、`scan_ui_strings.py`（files=3 rows=5 不变）、`pytest tests/ -q` **56 passed**（新增 12 例：确定性连接失败不可重试 / 瞬时故障仍可重试 / 假时钟下的 `total_timeout_s` 放弃 / 重试计数回调 / 错误串含「已重试 N 次」/ 面板策略取值与作业实际传参）；**A/B 实测**（`base_url=http://127.0.0.1:9/v1` 走 `agent_ask_start` + 轮询）**27578ms → 2281ms**（1 次尝试、0 次重试；单次拒绝耗时实测 ≈2.06s，故修复前 ≈ 6×2.06s + 15.5s 退避）；**B 端到端**（本地慢速假 SSE 端点 + harness `/rpc` + headless Edge/CDP，`MEMORIA_CONFIG_DIR` 指向临时目录）**12/12 PASS**：生成中点「清空对话」⇒ 气泡与面板 sessionId 清空、**作业结束后 sessionId 仍为空**、随后新提问得到**新会话文件**且旧会话保留、新提问请求体不含旧历史（确证未续到已作废会话）；「忽略本次」同样不写回（状态行/气泡均未被改写）；真实 `config/agent.json`（SHA256 `c9cfc8b7…`）与 `config/ui-settings.json`（`88f65cd1…`）运行前后**完全一致**。**未取证**：真实模型端点下的重试节奏与错误文案观感（用户 `config/agent.json` 是真密钥，仍刻意不调用）；面板对 `UNREACHABLE` 的**本地化文案**未做（前端未加 code 映射，经 `ask_failed` 兜底 + 后端原文呈现，原文含 URL 与「确定性失败，不会重试」）。**同日再续（2026-09-18，M1 收尾打磨四件事：真取消 / 会话列表去读放大 / 恢复上次会话 / 删除会话，同一行合并登记）**：① **真取消**——`services/agent/loop.py` 新增 `CancelToken`（`threading.Event`；选它而非裸可调用对象：线程安全、名字明确、幂等、与作业锁无耦合）并在**三处**检查（每轮迭代前 287-291、流式逐事件 240-241、每次工具调用后 371-374）；取消时 `_stream_once` **停止消费生成器并 `stream.close()`**（provider 的 `with closing(response)` 随之关闭底层 HTTP 响应、不再收完），`StopReason.ABORTED` + **保留已生成部分文本为 `answer`** + 正常发 `loop/end` 落盘、不抛异常；`ask()` 追加可选 `cancel`、`ask_stream.AskJob` 持令牌且 `cancel()` **立即**把作业收敛为 `done/aborted`（`succeed()/fail()` 加取消守卫，只补会话 id），`AskJobManager.cancel(job_id)` 幂等（未知 job → `unknown_job` 结构化错误、已结束 job → `cancelled:false`）且 `start()` 跳过已取消作业 ⇒ **busy 立即释放**；`ui.py` 新增 RPC `agent_ask_cancel`（1220-1240）、`agent_ask_poll` 追加 `cancelled` 字段。② **`agent_sessions_list` 去读放大**——新增 `session/history.py::summarize_session_file()`（260-308，**字节串原始行扫描**、不整体 `json.loads`、只对首个命中行解码取预览）+ `SESSION_SCAN_MAX_BYTES=2MiB` 上限，RPC 追加 `capped` 字段并复用 `list_sessions()` 已 stat 的字节数；`turn_count` 语义改为「扫描上限内的 user 行数」（docstring 与 10 篇均写明）。③ **恢复上次会话**——`ui-settings.json` 顶层新增 `agent` 段（`{lastSessionId}`，前端 `saveAgentPrefs()` **先读旧段再合并写回**，不覆盖 `layout`），面板打开/切换知识库（`app.js` 在 `initKb:452`/`openKbAt:560`/`closeKb:598` 三行调 `MemoriaAgentPanel.onKbChanged()`）时若会话文件仍在则自动载入、不存在则静默空态。④ **删除会话**——新增 RPC `agent_session_delete(session_id, kb_path=None)`（ui.py:1320-1355），只删 `<kb>/.memoria/agent/sessions/<id>.jsonl`（复 `session_file()` 的 id 正则防穿越 + `realpath` 目录归属校验双保险；非法/未知 → `unknown_session`），**这是产品首次允许写知识库**（会话目录是产品自管的运行时产物，与"零写入正文/sidecar/manifest"边界不冲突，已在 10 篇 §2.15 立界）；前端历史行加 `#agent-history-delete`，**两次点击确认**（首点变「再点一次删除」、3s 未再点复位、不弹窗），删当前会话即回到"新会话"态并清空偏好。⑤ **顺带必修的行为变更**：`llm/providers/openai_compatible.py` 响应体读取由 `read(_READ_SIZE)` 改 `read1(_READ_SIZE)`——实测 `HTTPResponse.read(n)` 在「无 Content-Length / `Connection: close`」的 SSE 上**阻塞到 EOF**（3.6s 的流只在结束时返回一整块 1094B），使增量投递与"取消立即停止消费"同时失效；`read1` 逐帧返回（0.02/0.32/0.62…s 各 90B）⇒ 面板「生成中…」由"整段一次性上屏"变为**真增量**。⑥ i18n：`agent.` 键改/增——`abandon`/`abandonTitle`/`status.abandoned` **废除**，新增 `stop`/`stopTitle`/`stopped`/`status.stopped`/`history.{delete,deleteTitle,deleteConfirm,deleted,capped}`/`err.cancelFailed`（中英成对）；`index.html` +2 行（`#agent-stop` 改名 + 删除按钮）、`app.css` +11 行（删除按钮 528-533、「（已停止）」587-589）。验收：`py_compile` 6 文件、`node --check` 4 文件、`i18n_selftest.js` 12/12 PASS、`scan_ui_strings.py`（files=3 rows=5 无新增）、`pytest tests/ -q` **67 passed**（新增 `tests/test_agent_cancel.py` 6 例：迭代边界取消 / 流式取消且生成器被关闭且后续事件不再产出 / 工具后取消 / cancel 幂等与 busy 立即释放 / 令牌幂等与跨线程 / provider 必须用 `read1`；`tests/test_agent_history.py` +5 例：行扫描等价性 / `capped` / RPC 面 `capped` / 正常删除 / 非法与未知 id 不误删）；**去读放大实测**（30 份会话 / 3.4 MiB）**12.25–13.89 ms → 4.75–5.63 ms（2.2–2.7×）**、单份 3.0 MiB **10.4 ms → 1.4–1.7 ms（`capped=true`）**；**端到端 31/31 PASS**（慢速假 SSE 端点 + harness `/rpc` + headless Edge/CDP，`MEMORIA_CONFIG_DIR` 指向临时目录）：停止保留部分文本 + 「（已停止）」+ 状态行 + **停止往返 0.060s** + 立刻可再提问 + 会话文件 `loop/end.stop_reason=aborted` + **假端点侧 `frames_sent=2/15 aborted=true`**（确证未收完）、清空顺带取消且无后续请求、刷新自动恢复上次会话且删文件后静默空态、删除两次点击才生效且 3s 复位、仅删目标会话；真实 `config/agent.json`（SHA256 `C9CFC8B7…`）与 `config/ui-settings.json`（`3BC0ACC4…`）前后**完全一致**。**未取证**：真实模型端点下的取消/恢复观感；被取消那一轮的 `assistant/message` 不落盘（01 篇 §7 已列为待拍板项）；多库切换时的恢复路径。 |

### 4.3 定期整理约定

- **触发点**：每次发布里程碑（`packaging\build_release.cmd` 成功）或每月底。
- **动作**：① 检查 `docs/` 根是否出现非允许文件 → 归位；② 检查新文档是否已登记索引；③ 检查临时产物是否都在 `artifacts/`；④ 检查台账体积是否在预算内（`docs/todo.md` ≤ 36 KB、`docs/design/maintenance-jobs.md §5` ≤ 8 KB），超限即按 [ledger-maintenance.md 规则 8](./ledger-maintenance.md) 清理（已收口条目出表入归档、开发流水账移除、压缩前事实须先转移到对应文档）。

## 5. 已执行的整理

### 5.1 2026-08-20

- `agents/` → `sessions/`（更名迁移，内容不变）
- 根目录散文档归位：`designV0.md`/`dicussion.md`/`system-design.md`/`preview-sync-refactor-plan.md` → `design/`；`markdown_form_std.md` → `standards/markdown-form-std.md`；`usage.md` → `standards/import-format.md`；`description_of_kb.md` → `example/`；`outs.md`/`127.0.0.1-*.log`（日志）→ `artifacts/`
- 删除 `_archive/`（早期草稿，git 历史可恢复）
- 根目录现存文件：`README.md` + `to-dolist.md`（活跃待办），符合 [§1 原则 2]

### 5.2 2026-08-30（目录体系重构）

- `standards/` 按性质拆分：契约性规范 → `conventions/`；AI 编程协作 → `guides/collaboration.md`
- `context/` 按性质拆分：usage-agent-workflow → `guides/`；image-features → `reference/`
- 中文文件名重命名为 kebab-case：`上下文规范.md` → `conventions/context-key.md`、`参考资料系统.md` → `conventions/refs-system.md`、`AAA_讨论元规范.md` → `conventions/meta-rules.md`、`AI 编程协作规范.md` → `guides/collaboration.md`
- 删除 `standards/README.md` 与 `context/README.md`，由 `conventions/`/`guides/`/`reference/` 三目录 README 取代
- 全部交叉引用同步（docs 内互链 + prompts/ + design/ 关联文档）

### 5.3 2026-09-03（演示截图迁出 docs）

- `docs/screenshots/`（`demo-*.png` × 7）→ `resources/screenshots/`（`git mv` 保留历史）——截图属**应用仓库静态资源**而非 docs 文档内容，且 README 仓库级展示配图应与 GitHub 展示约定一致。
- 引用同步：`README.md`、`README.cn.md` 全部 `<img src="docs/screenshots/…">` → `resources/screenshots/…`（14 处）。
- 目录登记同步：docs-management.md §4.1 删除 `screenshots/` 行（docs 不存二进制截图）；docs/README.md 子目录导航删除该行；resources/README.md 内容表补充 `screenshots/`。
- 新增规范 [conventions/readme-i18n.md](./readme-i18n.md)：README 双语（`README.md`↔`README.cn.md`）与截图收录/存放的维护规则（含自查清单）。
