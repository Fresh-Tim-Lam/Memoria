# 语言系统（i18n）维护规范

> **用途**：定义 Memoria 桌面应用的语言系统（语言包 + 界面文案接入）的契约性规则——文案如何抽取成键、语言包结构与存放位置、缺失回退策略、新 UI 如何接入、清单如何维护，使人机在新增/迁移界面文案时无需猜测即可遵守。
> **目标读者**：项目负责人 / AI Agent（所有新增或修改 UI 界面文案、维护语言包的开发者）。
> **关联文档**：[i18n-inventory.md](../reference/i18n-inventory.md)（当前界面文案登记清单，本规范的事实依据）；[readme-i18n.md](./readme-i18n.md)（README 双语与演示截图规范，属**仓库展示文案**，与应用内语言包正交）；[docs-management.md](./docs-management.md)（docs 组织规则）；[meta-rules.md](./meta-rules.md)（规范书写元规则）。
> **状态**：生效中。语言包机制已于 2026-09-03 落地（`app/i18n/zh-CN.js` + `en.js`、`app/js/i18n.js`、设置→显示「界面语言」即时切换、index.html 首批静态文案接入）；同日「检查」功能链路文案接入（检查弹窗/状态栏/角标/设置→检查页，见 `check.*` 键族），并落地后端检查消息的 code+params 本地化机制（见 §5 与 §6.5）。全量界面文案迁移（清单 900 行候选）为进行中事项。
> 制定日期：2026-09-03。

---

## 1. 总则

1. **界面文案禁止硬编码中文**：新增 UI 的用户可见文本一律通过 `t('key')` 取当前语言，语言包缺失时自动回退中文。
2. **中文为源语言**：`zh-CN` 包为完整事实源；新增键必须**同时**补入 `zh-CN` 与 `en`，英文翻译可暂缺（缺键回退中文，见 §5），但中文不可缺。
3. **字符串不拼接**：禁止 `前缀 + 变量 + 后缀` 拼界面文案（中英文语序不同）；用占位符 `{name}` 整体入包。
4. **语言范围**：当前支持 `zh-CN` / `en`；新增语言时只加语言包文件，不改业务代码。

## 2. 文案接入硬规则

- 所有用户可见文本（按钮/菜单/标题/占位符/提示/tooltip/空态/错误提示/对话框/检查报告文案）必须走 `t()`。
- 开发日志、`console.*`、`syncLog`、debug 面板文本**不属于**界面文案，可保留中文，不抽键（清单扫描器已自动剔除）。
- 图谱渲染、Markdown 渲染中的**用户文档内容**不是界面文案，不抽键。
- 文件路径、`id`、标签名、语言代码等标识符不抽键。
- 含参数的文案用 `{参数名}` 占位：`t('toast.fileSaved', { name: 'x.md' })`；包内值如 `文件已保存：{name}` / `File saved: {name}`。

## 3. 键命名与语言包结构（目标布局）

**键命名**：小写点分路径，第一段为区域前缀，逐级语义化；同一区域的键在文档清单里彼此相邻便于审阅。

```
app.status.kbLoaded        → 状态栏「已加载知识库」
toolbar.save               → 工具栏「保存」
menu.tree.rename           → 文件树右键「重命名」
settings.display.lang      → 设置-显示「界面语言」
check.badge.pass           → 检查徽标「检查通过」
toast.fileSaved            → 提示「文件已保存：{name}」
```

**语言包结构（目标路径，落地时建立）**：

```
src/memoria/ui/static/app/
├─ i18n/
│  ├─ zh-CN.js          # 中文包（源语言，必全）
│  └─ en.js             # 英文包
└─ js/
   └─ i18n.js           # 引擎：t(key, params)、getLang/setLang、加载 zh/en、
                        # 缺失回退、切换后派发重新渲染
```

**加载顺序**：`zh-CN.js` → `en.js`（按需）→ `i18n.js`（选择当前语言）。语言偏好持久化于本地设置（`ui-settings`，配置键约定 `ui.lang`），首次启动默认 `zh-CN`。

**区域前缀登记表（新增前缀在此登记）**：

| 前缀 | 归属 | 示例键 |
|------|------|--------|
| `app.` | 主框架/状态栏/欢迎页 | `app.status.kbLoaded` |
| `toolbar.` | 顶栏工具栏 | `toolbar.save` |
| `side.` | 侧栏/文件树 | `side.tree.empty` |
| `editor.` | 源码/编辑相关 | `editor.placeholder` |
| `edit.` | 编辑模式/格式工具/块编辑工具栏 | `edit.mode.title` / `edit.block.lang` |
| `preview.` | 预览相关 | `preview.math.renderFail` |
| `search.` | 工具栏搜索框 / 设置-检索页 | `search.scopeKb` / `search.settings.enableEmbedding` |
| `graph.` | 图谱面板 | `graph.3d.groupBy` |
| `settings.` | 设置各面板 | `settings.display.lang` |
| `menu.` | 右键/上下文菜单 | `menu.tree.rename` |
| `dlg.` | 对话框标题/按钮 | `dlg.ok` / `dlg.cancel` |
| `check.` | 检查页/待确认/统计 | `check.badge.pass` |
| `toast.` | flash/toast 提示 | `toast.fileSaved` |
| `import.` | 导入相关消息 | `import.conflict.title` |
| `img.` | 图片管理/诊断/清理/行内替换 | `img.diag.title` |
| `tree.` | 侧栏知识库文件树（渲染/右键菜单/新建·重命名·删除操作） | `tree.rename` / `tree.deleteBody` |
| `color.` | 取色面板/自定义颜色管理/色板 | `color.customTitle` / `color.swatchTitle` |
| `brush.` | 样式笔刷（画笔模式状态与提示） | `brush.ready` / `brush.applied` |
| `cfg.` | 配置文件弹窗（Tab 栏/KP 列表/链接审计·搜索选项·匹配面板/M4 建议，后续含边与链接编辑器） | `cfg.kpTab.config` / `cfg.match.summary` |
| `common.` | 通用词 | `common.confirm` |
| `backend.` | Python 返回给界面的消息 | `backend.file.renameConflict` |

> 新增区域前缀时：在语言包键树、上表、清单文档三处同步登记。
> 注：检查功能的后端问题行不使用 `backend.` 前缀，而是直接入 `check.issue.<code>`（见 §5.1）；`backend.` 保留给其它待接入的后端消息。

## 4. 语言包文件格式

每个包导出一个扁平或树状对象（推荐树状便于审阅）：

```js
// i18n/zh-CN.js
window.MEMORIA_LOCALES = window.MEMORIA_LOCALES || {};
window.MEMORIA_LOCALES["zh-CN"] = {
  app: { status: { kbLoaded: "已加载知识库" } },
  toolbar: { save: "保存" },
  toast: { fileSaved: "文件已保存：{name}" },
};
```

引擎取值支持点路径；`en` 包缺键时回退 `zh-CN`，`zh-CN` 缺键回退键名本身（便于发现）。

## 5. 缺失键回退（已确认策略）

- 查找顺序：当前语言 → `zh-CN` → 键名本身。
- 回退行为保证：**英文翻译未完备时界面永远显示中文**，功能文案不会因缺英文而空白/报错。
- 英文包完成度以清单文档「状态」列跟踪（`☐ 未迁移` → `✔ 已迁移`）。

### 5.1 后端检查消息的本地化（方案 1）

后端生成的问题行（如检查报告里的逐条 issue）遵循"中文保留 + code/params 双发"约定，避免中文在前后端两处漂移：

- **后端**：每条 issue 始终携带中文 `message`（zh 界面/CLI/未知 code 的事实源），并附稳定 `code` 与结构化 `params`；`code` 即 en 模板 `check.issue.<code>` 的键后缀，`params` 仅供英文模板占位。
- **语言包**：中文**不**复制进 `zh-CN.js`；英文模板写入 `en.js` 的 `check.issue.<code>`。
- **前端渲染**：当前语言 ≠ zh-CN 且当前语言包含该 code 的模板时用 `t('check.issue.<code>', params)`，否则原样显示后端中文 `message`（未知 code 永不空白）。
- CLI 等不经过前端语言包的直接打印仍使用后端 `message`，零改动。

## 6. 新增 UI / 文案的标准流程

1. 写界面时用 `t('area.key', {…})`，不写中文字面量；
2. 到 `zh-CN.js` / `en.js` 补两个键值；
3. 区域前缀若为新域，按 §3 登记；
4. 运行 `python scripts/scan_ui_strings.py` 刷新 [i18n-inventory.md](../reference/i18n-inventory.md)，并把对应文件状态置「✔ 已迁移」（或在该文件「迁移记录」表登记一行）；
5. 涉及检查报告/导入消息等后端文案时，按 §5.1 约定：后端中文 `message` + 稳定 `code`/`params` 双发；英文模板写入 `en.js` `check.issue.<code>`；前端渲染点走"当前语言有 code 则翻译、否则回退中文 message"的 localize 助手（前端 `localizeCheckIssue`）。

## 7. 语言切换（已确认交互规格，落地项）

- 入口：设置 → 显示 → 「界面语言」（`zh-CN` / `en`）。
- 生效：选择后**即时生效**并重渲染当前界面（不重启），同时持久化到本地设置。
- 实现要点：切换后需对当前可见 DOM 中带 `data-i18n` 的静态节点与动态区域做一次文案刷新；动态 toast/对话框类由渲染时取 `t()` 天然跟随。

## 8. 维护检查清单（自查/评审用）

- [ ] 新增文案没有中文字面量直出到界面（允许出现在 zh-CN 包与注释/日志）；
- [ ] 键已按区域前缀命名且点路径唯一；
- [ ] 中文包已补、英文包尽力补（缺则回退中文可接受）；
- [ ] 含参数文案使用了 `{param}` 占位而非字符串拼接；
- [ ] 清单已刷新、迁移状态已更新；
- [ ] 语言包文件通过 `node --check`，无语法错误。

## 9. 修订记录

| 日期 | 修订内容 |
|------|---------|
| 2026-09-03 | 初版：总则/键命名/包结构/回退/流程/切换规格（机制待落地） |
| 2026-09-03 | 机制落地：新增 `app/i18n/zh-CN.js`、`app/i18n/en.js`、`app/js/i18n.js`（`t()/setLang/hydrate/applyStatic`）；设置→显示「界面语言」即时切换并持久化（ui-settings `i18n.lang`）；index.html 工具栏/侧栏/视图切换/设置弹窗首批静态文案接入 `data-i18n`；自测 `node scripts/i18n_selftest.js` |
| 2026-09-03 | 「检查」功能文案接入 `check.*`：index.html 检查弹窗、app.js 状态栏角标/统计/检查弹窗/图谱建边提示与路径修复流程、check-settings.js 设置→检查页（选项文案本地化）；引擎新增 `rawLookup`；新增 §5.1 后端检查消息方案 1（中文 message + code/params 双发；en `check.issue.<code>`；未知 code 回退中文）并落地到 kb_integrity/manifest/link_audit/sidecar_validate 四处 issue 生产者 |
| 2026-09-03 | 相邻文案补齐：图片管理/异常引用诊断/清理/行内替换与保存自动清理提示接入 `img.*`；共享「请先打开知识库」统一为 `app.openKbFirst`（替换 check.status.openKbFirst）；图谱画布与设置预览空态提示接入 `graph.hint.*`（graph-settings.js 新增模块级 `TT`）；检查相关 RPC 错误（path_cascade / sync_manifest）补 `code` 复用 §5.1 机制，前端 repair/sync 失败提示按 code 翻译 |
| 2026-09-03 | 右键上下文菜单整文件接入 `menu.*`（link-context-menu.js 光标/粘贴/插入图片/复制/样式/建链/设为知识点/链接菜单与状态）；同文件层面完成源码区右键「插入图片」与预览对齐（showForCursor 已支持 onInsertImage，源码区 contextmenu 补传行号） |
| 2026-09-03 | 关联文档更新：新增 readme-i18n.md（README 双语/演示截图维护规范）；明确「应用内语言包（i18n.md）≠ 仓库展示文案（README）」边界 |
| 2026-09-04 | 编辑块工具栏整文件接入 `edit.block.*`：edit-handler.js 块类型标签（代码块/思维导图/公式/表格/图片）、语言/类型/符号/对齐/大小/名称工具与 tooltip、行内公式工具栏、图片名称开关（`edit.block.nameOn/nameOff`）、编辑/浏览模式切换提示（新增 `edit.mode.browseTitle`）；index.html 块编辑工具栏 `aria-label` 接 `data-i18n-attr`；`_BLOCK_TOOLS.label` 改为 `labelKey` 键、展示处 `_t()` |
| 2026-09-04 | 图谱/检索设置整文件接入：graph-settings.js 节点标签/2D·3D 布局/图谱样式/节点群页签/示例预览（新增 `graph.settings.*`、`graph.labelModes.*`）；设置示例图数据本地化（`graph.sample.*`，`SAMPLE_GRAPH` 常量改为 `buildSampleGraph()` 函数、语言切换重绘时重建）；graph-label.js 标签模式选项改 `labelKey`（`graph.labelModes.*`）；search-settings.js 检索内核页（新增 `search.settings.*`）；`graph.`/`search.` 前缀登记、graph-settings 路径提示 fallback 去除中文硬编码 |
| 2026-09-04 | 扫描器规则修正（scan_ui_strings.py）：剔除 `console.*` 开发日志（i18n.md §2 一致）；HTML 已挂 `data-i18n`/`data-i18n-attr` 的行视为已迁移。index.html 60 候选清零，全库仅剩 app.js |
| 2026-09-04 | app.js 拆分试点 + 随拆分同步 i18n：导入流程抽为 `js/import-flow.js`；新增 `window.MemoriaApp` 服务门面；导入文案扩入 `import.*`（18 键）。app.js 候选 496→472。剩余 app.js 拆分候选顺序（技术可行性修订）：工具条搜索 → 图片管理 → KB 检查 → 文件树 → 取色器/画笔（画笔与编辑格式管线耦合，需先抽独立 editor-styles 层） |
| 2026-09-04 | 文件树子系统抽为 `js/file-tree.js`（`window.MemoriaFileTree`：render/expandToPath/init）；树文案接入 `tree.*`（20 键，zh/en 成对：右键菜单/新建·重命名·删除弹窗与状态提示），`tree.` 前缀登记；门面新增 renderTabs/closeTabAt/updateNavButtons 供 file-tree 调用。app.js 候选 448→426，file-tree.js 候选 0 |
| 2026-09-04 | 画笔/取色/自定义颜色区域原位接入语言包（app.js ~9464–10012）：`color.*` 8 键（面板标题/色板 title/右键菜单/添加·删除状态）与 `brush.*` 11 键（画笔样式名与状态提示）zh/en 成对；拼接型状态改整句模板 `{hex}`/`{x}` 内插（呈现与原先逐字符一致）；`color.`/`brush.` 前缀登记。app.js 候选 426→406 |
| 2026-09-04 | 图谱侧栏分组页签 + 构建/加载状态原位接入 `graph.*`（app.js）：`graph.group.*` 3 键（allLabel 全部/allTitle 全部节点群（横向瀑布流）/tabTitle `{name}·{size} 节点·{edges} 边` 模板）、`graph.build.*` 6 键（building/buildingDetail/failed/done/doneDetail `{msg}·{n} 处待人工配置`/doneKinds `{stats}（{kinds}）`）、`graph.loadFailed`；状态拼接型整句模板化（含全角括号并入 doneKinds）。app.js 候选 406→394 |
| 2026-09-04 | 应用启动/KB 开关/状态栏/打开文件与加载/标签关闭/文件状态统计原位接入 `app.*`（app.js）：`app.status.*` 6 键（kbLoaded 已加载知识库/kbOpened 已打开知识库/kbEmpty 未打开知识库/kbEmptyDetail 点「打开知识库」选择目录/waitingBackend 等待后端…），`app.loading/loadFailed/listFailed/apiUnavailable`，`app.stat.*` 4 键（kpLines `{kp} KP·{lines} 行`、errors `{n} 错误`、warnings `{n} 警告`、sidecar `· 配置 {parts}`）；标签关闭 title 复用 `common.close`；文件统计拼接改模板+参数字符串（内部 parts.join(" ") 保留）。app.js 候选 394→380 |
| 2026-09-04 | 预览加载/自检/链接一致性提示/KP 列表空态原位接入（app.js）：`preview.*` 新增 9 键（loadNotReady/rendering/renderFail `{e}`/okDetail/failDetail/brief/incomplete/selfcheck `{parts}`/linkAuditHint `{n}`），TeX 摘要 `<summary>` 复用 `preview.math.texError`；KP 列表空态入 `app.kpList*` 4 键（SelectFile/NoKp/OnlyProposals `{n}`/OnlyProposalsHint）；渲染异常 message 直接过 `esc()` 或经模板占位。app.js 候选 380→366 |
| 2026-09-04 | 配置文件弹窗「链接审计/匹配」区段接入（app.js 8 函数：kpRangeMeta/m4SuggestBlockHtml/renderConfigTabBar/renderConfigKpTab/linkAuditBadgeHtml/renderLinkAuditBanner/renderLinkSearchOptionsHtml/renderLinkMatchPanelHtml）：新增顶层 `cfg.*` 51 键（tab.kp/links/pending、kpRange.unset、kpTab.newKp/newKpHint/empty/config/deleteTitle、audit.missing/stale/partial/unresolved 各 Label+Title、audit.openFirst/bannerTitle/bannerBody、m4.mergeTitle/linkTitle/mergeHint/linkHint/sameFileNote/crossFileNote `{file}`/bodyUnboundNote `{line}`/mergeBtn/citeBtn/routeBtn、search.ignoreSpace/suggest/case 各文本+Title、match.needInput/scanning/summary/blockedReason/locateBlocked/locateSubstring/locateExcluded/locateRow/collapse/confirmWrap/selectAll/clearAll/panelTitle `{anchor}`/onlyUnwrapped/noMatch）；common 新增 delete；`cfg.` 前缀登记（§3）。app.js 候选 366→320 |
| 2026-09-04 | 配置文件弹窗「链接 Tab/待确认 Tab/概要·校验提示」+ KP 弹窗 Tab 标签 + 待确认忽略/刷新状态接入（app.js：renderConfigLinksTab/renderConfigPendingTab/renderConfigModal/dismissPendingItem/syncPendingFromConfig/renderKpModalTabs/loadLinkEditorMatch）：`cfg.*` 追加 43 键（linktab 12：newLink/newLinkHint/empty/poolNote `{n}`/attachedNote/bodyWrappedNote/unbound/matchBtn/matchBtnTitle/locateTitle/editBtn/deleteTitle；pending 19：confirmAll/refreshBtn/kbTotal `{n}`/empty/grpHeading·grpMention·grpDefinition `{n}`/tagHeading·tagMention·tagDefinition/confirmDirect/confirmConfig/dismiss/ignored/ignoreFail/refreshing/refreshFail/refreshed/items `{n}`；config 7：title `{name}`/summary `{sidecar}·{kp}·{links}`/metaDone/metaNone/errors·warnings `{msg}`/linkWarn `{n}`；panel.tabs 4：range/identity/tags/edges；match.scanFail）；删除按钮复用 common.delete。app.js 候选 320→274 |
| 2026-09-04 | KP 弹窗「边」页接入（app.js 4 函数：edgeTypeBadgeHtml/edgeOriginLabel/renderKpEdgesTabHtml/bindKpEdgesEvents）：`cfg.edge.*` 34 键（typeContain/typeReference/typeExtend、originLink/originRange/originManual、outTitle/inTitle/suppressed/restoreBtn/suppressBtn/configLinkBtn/noOut/inScopeNote/createTitle/targetKp/targetPlaceholder/typeLabel/optReference/optExtend/relevanceLabel/createBtn、suppressInProg/opFailed/suppressedContain/restoreInProg/restoredContain/confirmDelete `{src}→{tgt}({type})?`/deleteInProg/deleted/needTargetId/createInProg/createFail/created）；删除按钮复用 common.delete；select option 保留 value code 仅文本入包。app.js 候选 274→241 |
| 2026-09-04 | KP 弹窗「范围编辑器 + 标题 + 标识/描述」接入（app.js 3 函数：buildRangeEditorHtml/syncKpModalFooter/renderKpModalBody）：`cfg.*` 新增 19 键——cfg.range 7（start/end/wheelNote/totalNote `{total}`/startCand/endCand/updateNote）、cfg.kpModal 3（createTitlePlain/createTitleNamed `{name}`/editTitle `{name}`）、cfg.kpIdent 9（nameLabel/createHint/editHint/descLabel/descPlaceholder/syncAux/suggestDesc/descCreateHint/descEditHint）；保存按钮复用 common.ok。app.js 候选 241→224 |
| 2026-09-04 | KP 弹窗「Tag/别名/描述候选编辑器」接入（app.js 9 函数：renderKpAliasChip/renderKpDescCandChip/renderKpAliasEditorHtml/renderKpDescCandEditorHtml/refreshKpAliasEditorDom/refreshKpDescCandEditorDom/renderKpTagChip/renderKpTagsEditorHtml/refreshKpTagsEditorDom）：`cfg.*` 新增 22 键——cfg.kpTag 18（aliasSelEmpty/aliasCandEmpty/descCandEmpty/zoneAliasSel/partSearchNote/zoneAliasCand/zoneDescCand/fillDescNote/aliasNewPlaceholder/tagNewPlaceholder/addCandBtn/zoneTagsSel/tagsSelNote/zoneTagsCand/tagsCandNote/sysSuggestBtn/tagSelEmpty/tagCandEmpty）、cfg.chip 4（moveToCand/applyToSel/fillDesc/remove）；render/refresh 空态复用同键。app.js 候选 224→198 |
| 2026-09-04 | 「aux 隐式提议/建议合并 + 待确认直接确认/全部确认 + 跳转/链接确认按钮」接入（app.js 7 函数：loadKpImplicitProposals/loadKpTagSuggestions/loadKpMergeSuggestions/confirmKpFromProposal/confirmAllPending/jumpToTarget/syncLinkModalFooter）：`cfg.*` 新增 29 键——cfg.aux 6（syncFail/merged `{n}`/none/noTagSuggest/tagAdded `{n}`/mergeHint）、cfg.confirm 21（cannot/endOutOfRange `{end},{total}`/missingId/missingName/idTaken/validateFail/inProg/writeFail/done/doneDetail `{kpId},{start},{end}`/nonePending/confirmAll `{n}`（含 \n）/working `{cur},{total},{name}`/skNoId/skRange/skIdTaken/skValidate `{msg}`/skWrite `{msg}`/result `{ok},{skip}`/alert `{n},{list}`/allDone `{n}`）、cfg.jump 2（done `{label}`/confirmBtn）；confirm/alert 整句模板入包、skipped 分支逐条模板化。app.js 候选 198→172 |
| 2026-09-04 | 链接编辑器（编辑/创建）主体区接入（app.js 7 函数：suggestLinkRelevanceForTarget/renderTargetEdgeControls/renderLinkTargetEdgesPanel/linkEditorSaveHint/linkEditorStatusText/renderLinkEditMeta/renderLinkTargetList）：`cfg.*` 新增 36 键——cfg.linkEdge 11（recommendFail/applied `{tid}`/notIntegrated/defaultDetail `{tid},{rel}`/typeLabel/strengthLabel/relTitle/suggestBtn/sectionTitle/empty/panelHint）、cfg.linkSave 5（hintNoTarget/hintNeedAnchor/statusCreate `{n}`/statusMulti `{n}`/statusSingle）、cfg.linkMeta 20（anchorLabel/anchorPlaceholder/metaHint（包内唯一含 <code>）/openTitle/targetsHint/legend 5/ noTarget/pickAria/addPlaceholder/addBtn/m4Summary/m4Hint/pickBtn/queuePrimary `{name}`/queueMore `{n}`/queueHint）；select 选项文本复用 cfg.edge.typeReference/typeExtend，移除复用 cfg.chip.remove。app.js 候选 172→130 |
| 2026-09-04 | 链接保存/扫描/挂接/移除/删除/点击解析/预览链接 title/图谱焦点提示流程接入（app.js 14 函数：）：`cfg.*` 新增 51 键——cfg.linkSave 追加 15（targetExists/addFail/titleCreate/titleCreateNamed `{text}`/titleEdit `{text}`/titleMulti `{title}`/foundRoute/fail/needTarget/needAnchor/needPositions/saving/attachFail/saved/savedDetail `{saveAnchor},{targets}`）、cfg.match 追加 16（badge 6：Conflict/Excluded/Substring/Fuzzy/Wrapped/Attached，scanMatching/matched `{anchor}`/applyFail/noTask/substringNote/selectNote/needText/applying/attached/attachDetail `{anchor},{n}`）、cfg.linkOps 12（detachConfirm `{line},{label}`/noLine/noLineDetail/removing/removeFail/detached/deleteConfirm `{anchor}`/deleting/deleteFail/deleted/unwrapConfirm `{text}`/unwrapped，confirm 均含 \n\n）、cfg.linkClick 8（unresolvable/notFound `{target}`/failed `{target}`/unbound/unboundTitle `{target}`/graphLinkWord/notInGraph）；扫描失败复用 cfg.match.scanFail、“跳转到”复用 preview.link.jump `{key}`。app.js 候选 130→72 |
| 2026-09-04 | 样式应用错误消息 + 格式化状态 + 剪贴板/粘贴接入（app.js，24 处替换）：`cfg.*` 新增 19 键——cfg.style 14（notEditable/applyFailed（多处共用含三元）/rangeBoundary/invalidBlock/uneditableContent/crossList/crossQuote/boundaryUnsplit/unsupportedType/singleLineOnly/applied/needPreviewSel/needSourceSel/formatFail）、cfg.clip 5（empty/needCursor/editorNotReady/pasteFail/pasted）；`return {ok:false,message}` 与 setStatus/setStatusError 主文案走键，res.message/e.message 透传。另扫描器规则扩展：剔除自定义 `log("TAG", …)` 开发日志行（与 i18n.md §2 一致），`log("STYLE", "_extract FAIL: …")` 等不再计入清单。app.js 候选 72→45 |
| 2026-09-04 | KP 定位辅助（assist）预览区接入（app.js 5 函数：openAssistFromSelection/assistPreviewToolbarHtml/updateAssistEllipsis/renderAssistSourcePreview/renderAssistMarkdownPreview/renderAssistPreview）：`cfg.assist.*` 8 键（ellipsisTop `{to}`/ellipsisBottom `{from},{total}`/rangeLabel/previewModeAria/viewSource/viewMarkdown/rangeError/defaultName）。app.js 候选 45→34 |
| 2026-09-04 | KP id 迁移/保存配置/标签保存/删除知识点/range 校验保存流程接入（app.js saveKpIdentity/saveKpTags/deleteKp/saveKpRange/reloadDocAfterKpChange 等）：`cfg.kpSave.*` 23 键（fail/nameEmpty/idEmpty/renameConfirm `{oldId},{newId}`（含 \n）/migrating/migrateFail/migrateStats `{n},{m}`/migrated `{old},{new}`/savingCfg/updated `{kpId}`/updatedTags `{kpId}`/deleteConfirm `{label}`（含 \n）/deleting/deleteFail/deleted/invalidRange/endBeforeStart/endBeyond `{end},{total}`/cannotSave/startEmpty `{start}`/endEmpty `{end}`/created/updatedRange）；复用 cfg.confirm.idTaken/validateFail/writeFail/doneDetail。app.js 候选 34→7 |
| 2026-09-04 | 收尾清零：errorLabel 错误 map 入 `cfg.kpErr.*` 4 键（startSnippet/endSnippet/endBeforeStart/missingSnippet）；匹配行定位状态 `cfg.match.located/substringBlocked/checkboxHint` 3 键；「请先打开文件」复用 app.openFileFirst。**app.js 界面文案候选清零达成（rows=0，files=0）**；i18n-inventory 为空清单 |
