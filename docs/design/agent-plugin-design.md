# 能力插件与写模块总设计（协议 · 写场景 · 模块设计）

> **用途**：承载产品内 Agent **能力插件与写模块的全部相关设计** —— ① **插件契约**（字段集 §1、暂缓字段 §2、上游对照 §3、待讨论 §4）；② **写场景清单**（今天 Memoria 到底支持哪些「写入形态」、哪些不支持、各落到哪个 op/原语，见 §7；场景间关系与排序见 §8）；③ **模块设计**（注册与装载 / 写管线 / 唯一写者 / 备份与撤销 / 计划 API / 编译器与校验器 / 权限与越界 / 四线落法 / M3 分期与安全门：**逐节自 [agent-capabilities.md](./agent-capabilities.md) §2 迁入**，迁移状态见 §6）。本文件是相关设计的**唯一事实源**；其他文档只留**指针**，**不得复制字段表**。
> **关联文档**：[agent-capabilities.md](./agent-capabilities.md)（**历史 / 路线图 —— 自 2026-09-20 起不再是事实源**；其 M3 模块设计将逐步迁入本文，见 §6）、[dsh-agent-port.md](./dsh-agent-port.md)（**移植总纲**：M1–M2 已落地，M3 写能力待拍板/待实施）、[preview-formats.md](../reference/preview-formats.md)（**正文书写格式与渲染语法权威**，§7 的「今天支持什么」逐处据它核对）、[agent-guide/README.md](../reference/agent-guide/README.md)（人 UI 现有写入路径的取证文档）。
> **状态**：**活文档 —— 由人逐步对齐**（2026-09-20 建立；同日由「最小协议」**重定位为「协议 + 场景 + 模块设计」总设计文档**）。
> **治理约定**：**人主导、逐步对齐；Agent 只追加、不改人已确认的条目**。Agent **只在被明确要求时追加**，**不得改写人已确认的条目**（字段、选项、结论一并适用）；每次改动经人确认后**由人自行定稿**。讨论未完的地方留在 §4 与 §7 / §8，只写"问题 + 可选项"，不代替人下结论。
> **事实源口径（2026-09-20）**：本文是能力插件与写模块相关设计的**唯一事实源**；[agent-capabilities.md](./agent-capabilities.md) **不再是事实源**（降级为**历史 / 路线图**，其 M3 部分将**逐步迁入**本文，见 §6）。**迁移完成前**，凡两份文档冲突处，**一律以本文为准**。
> 本文表内出现的 `§x` / `P9` 等编号，指 [agent-capabilities.md](./agent-capabilities.md) 的对应小节与待拍板编号（§6 迁移完成后改指本文）。

---

## 1. 最小可用契约 v1（6 字段）

> 用户口径（2026-09-20）：「能力契约我们慢慢完善，我们先以第一个写模块进行设计，只需要满足『最小可用契约字段集』，不要上来就框住，除非 dsh 上游有成熟的设计」。故本节只定**第一个消费者（写模块）今天真的会用到**的字段；上一轮 13 个字段的旧表**收缩为 6 个**（2026-09-20）。
> **准入规则（v1 唯一的字段裁判）**：一个字段要进 v1，必须满足**其一** —— ① **有真实消费者点名**（本节每行写清"谁读它、在哪一步用"）；② **照搬 dsh 上游的成熟设计**（必须点名只读检出 `dsh-src/` 的 `file:line`，且**语义与命名照它的**）。**两条都不满足 ⇒ 一律移出 v1**（进下方「暂缓字段」清单，并逐条给复评触发条件）。**不接受**"将来可能有插件需要"这类理由。

| 字段 | 必填 | 为什么需要（消费者是谁） | 上游对照 | 校验规则 |
|---|---|---|---|---|
| `id` | ✅ | 装载器：按 `id` 与库级 `enabled[]` 求交集与启用位（§2.2）；审计：每条 `capability/*` 带插件 id；冲突判定：两个声明同 `id` | **有据**：插件模块的稳定标识 `export const name`（`dsh-src/packages/fs/tool-fs/src/index.ts:19`）＋组合树条目 `id`（patch 以 `id` 定位，`dsh-src/apps/cli/src/profile-boot.ts:173`） | 英文 kebab-case、唯一；重复 ⇒ **装载失败**（不静默取其一） |
| `name` | ⬜ | 库级启停 / 插件列表面板的**人读标签**（`id` 是机器标识，不可替代）；§2.2 第 ③ 步"一条可见告警"指名插件 | **有据**：上游确有"发现 UI 用的展示标签"形态 —— `PresetSpec.name`（注释原文 "display label a client shows"，`dsh-src/packages/interaction/permission-presets/src/index.ts:62-71`）、`CommandDefinition.description`（"used in discovery UI"，`dsh-src/packages/interaction/commands/src/index.ts:66-67`） | 非空字符串；缺省回落 `id` |
| `provides.tools[]`（`tool_id` 必填；`constrain` / `gate` 可选） | ✅ | 装载器：`tool_id`（**必须**取自核心原语目录）+ 按 `gate` 决定该**领域动词**是否出现在模型可见工具面；**编译器 / `validate_plan`**：`constrain` 是参数收紧的唯一来源（§2.1.1 / §2.3.4，如边类型枚举取自 `graph.edge_types.EDGE_TYPES`）。**该工具只产出 plan、不落盘**（§2.3.3） | **本地自定**：上游**没有**"包声明工具清单"这种声明面 —— 工具是 `apply()` 里**运行时**注册的 `ctx.tools.register(definition)`（`dsh-src/packages/core/tools/src/index.ts:1043`）。仅**取值语义**对齐上游 `ToolSchema`（`name`/`description`/`parameters`，`dsh-src/packages/core/tools/src/schema.ts:483-498`；构造走 `defineTool` `:545`） | `tool_id` 不在原语目录 → **拒绝装载**；同一 `tool_id` 的同一动作类被两个插件声明 ⇒ 装载失败（§2.2） |
| `permissions.read` | ✅ | 装载器：声明的 `tool_id` 动作类必须落在声明面内（`read` 之外的 `tool_id` 一律拒）；§2.4 矩阵与 UI 展示"该插件可读范围" | **本地自定**：上游没有 per-plugin 读写 glob，只有**执行器级**档位 `SandboxMode = 'read-only'｜'workspace-write'｜'danger-full-access'`（`dsh-src/packages/sandbox/sandbox/src/index.ts:29`） | 相对库根的 glob；归一后落在库根外一律 Deny |
| `permissions.write` | ✅（有写动作类时非空） | **§2.4 apply 入口的路径校验读它**（glob + `os.path.realpath` 前缀校验 ⇒ 越界即拒，§2.3.1 第 3 条）；装载器：空 ⇒ 该插件不可写 | **本地自定**；语义对应上游 `workspace-write` 档且 workspace = 库根（§2.7 的"本地发明"口径不变） | 非空才可写；逐条 glob 做前缀校验；`.memoria/agent/sessions/**` 与 `.memoria/agent/backups/**` 不在白名单 |
| `approval` | ✅ | 审批策略在工具**分发前**读它（`services/agent/approvals.py:109-115`；即 §2.3 第 3 步"逐条确认"）；§10 P9 决定它能否被库级覆写 | 字段**本地自定**：上游审批是**运行时**询问 waterfall `approval/request`（`dsh-src/packages/interaction/user-approval/src/types.ts:85-89`），档位是**会话级** `ApprovalPolicy = 'ask'｜'never'`（`dsh-src/packages/interaction/user-approval/src/index.ts:60`）—— **没有**"插件按动作类声明档"的形态。**仅词汇对齐**：`ApprovalOutcome`（`allowed-once`/`rejected`/`cancelled`/`unavailable`，不可用即 fail-closed，`types.ts:32`） | **必须显式声明**，按动作类分档：`auto` / `confirm` / `never`。**安全下限：`permissions` 含 `write` ⇒ `approval` 不得为 `auto`**（硬校验，见 §10 P9）；v1 **不含 `exec` 字段** ⇒ **引入 `exec` 时该下限随之扩到 `exec`** |

> `provides.tools[]` 的两个子字段 `constrain` / `gate` 属**判断项、可再收紧**（是否留在 v1 见 §4）。
> 字段之外还有**一条铁律**（模块侧反复引用，见 [agent-capabilities.md §2.1/§2.2](./agent-capabilities.md)）：**插件目录内不含可执行代码**（无 `.py` / `.js` / 脚本），只含**声明 JSON + 只读资源**。

**字段演进（一）：删除 `kind`（2026-09-20）**：本契约**已删除 `kind` 字段**（原取值 `read` / `write` / `network` / `skill` / `host`）。用户口径原话：「那就不要写这个 kind 的字段，我们慢慢攒插件，后面才能知道有没有必要保留这个字段，以及怎么划分」——即**暂不引入分类维度**，等**复评触发条件：攒够 3–5 个真实插件**时再决定"有没有必要、按什么划分"。`kind` 原本承担的两件事就地改为：① **默认 `approval` 档**：**不由分类推导**，改为 `approval` **必须显式声明**（按动作类）+ 上表的安全下限（`permissions` 含 `write`/`exec` ⇒ 不得 `auto`）；② **UI 分组**：**暂不由契约决定** —— **UI 先按插件来源目录/名单分组**（内置 `resources/agent-capabilities/**` vs 用户 `<kb>/.memoria/agent/skills/**`），是否需要分类维度等上述复评后再定。

**内置声明实样**（`resources/agent-capabilities/kb-write.json`，随版本分发、只读）—— **只写 v1 字段集**：

```jsonc
{
  "v": 1,
  "id": "kb-write",
  "name": "写能力（建点 / 改点 / 连边 / 文件增删改）",
  "provides": {
    "tools": [
      { "tool_id": "kb.kp.create",   "gate": "always" },
      { "tool_id": "kb.kp.update",   "gate": "kb_has_sidecar" },
      { "tool_id": "kb.link.create", "gate": "always", "constrain": { "type": { "enum_from": "graph.edge_types.EDGE_TYPES" } } }
    ]
  },
  "permissions": {
    "read":  [ "**/*.md", ".memoria/**" ],
    "write": [ "**/*.md", ".memoria/sidecars/**", ".memoria/manifest.yaml", ".memoria/pending.json" ]
  },
  "approval": { "read": "auto", "write": "confirm" }
}
```

> `v` 是**声明文件的信封版本**（"只增不改"，语义对齐 [AGENTS.md §3](../../AGENTS.md) 的事件信封与 §10 P12 的 plan 版本口径），**不占**上表字段位。上方「暂缓字段」里的键**不写进声明文件**。

**库级启用实样**（`<kb>/.memoria/agent/capabilities.json`，随库走、用户可改）：

```jsonc
{
  "v": 1,
  "enabled": [
    { "id": "kb-write",       "on": true,  "config": {} },
    { "id": "web-fetch",      "on": false, "config": {} },
    { "id": "kb-skill-local", "on": true,  "config": {} }
  ]
}
```

> `enabled[]` 就是**库级注册表**（§2.2 位置 2）：**条目存在即启用**，故契约层**不再需要 `enabled` 字段**（见下方「暂缓字段」）；`config` 的**值位保留**，但 v1 没有参数 schema ⇒ 值暂不校验（一律空对象）。
> 注意 `kb-write` 的 `permissions.write` **不含** `.memoria/agent/sessions/**`：审计事件是**核心**写的，不是插件写的 —— 这正是 §2.3.1「插件物理上不能越界」的一个可核实例；也**不含** `.memoria/agent/backups/**`（§2.3.2 第 4 条：插件既写不了、也删不了备份）。

---

## 2. 暂缓字段（不在 v1）

**暂缓字段（不在 v1）**：按下表逐条移出，**每条都给复评触发条件**（不写"将来再说"）；复评时按 §1 准入规则重新过一遍，过了才回到字段表。

| 暂缓字段 | 移出理由（v1 下它没有消费者） | 再引入的触发条件 |
|---|---|---|
| `provides.prompt` | 写模块的模型可见文本由**核心** system 段落 + 工具 schema（`description`/`parameters`）承担；v1 没有需要 per-plugin 注入的提示词段落 | 出现第二个**必须自带提示词段落**的插件（N 线域名/私网策略、S 线 `SKILL.md` 正文）且核心段落装不下时；**且**须先定两条校验：与它的工具**同门控**、不得写进 `resources/agent-prompts/**`（既有事实源） |
| `permissions.exec` | §2.6 明确 M3 不做命令执行 ⇒ 没有 exec 动作类，字段既无校验对象也无消费者 | 出现第一个真要跑外部命令的插件，**且**沙箱/白名单方案单独评审通过时；同时把 `approval` 的安全下限扩到 `exec` |
| `permissions.network` / `permissions.host`（动作类列） | v1 只开 `read`/`write` 两类动作；N/S/H 三条线未开工（§2.5） | N1 / H1 各自引入与 `net.*` / 宿主原语配套的动作类时；此前 §2.4 矩阵对应列不发生效 |
| `forbidden` | 纯文本、无程序消费者；它列的禁项今天已由**结构**保证（插件目录无代码 §2.2 + `permissions.write` 白名单 + realpath 前缀校验 §2.3.1 + apply 入口唯一写者 §2.3.2） | 出现"必须由声明驱动、装载器能**机检**"的禁项（如某插件须禁 `git` 而权限面表达不了），**且**同时有装载期或调用期检查器 |
| `config`（**参数 schema**；库级 `config` **值位**保留，见 §2.2） | v1 的 3 个 op 没有需要用户调的参数（批量上限、删文件开关都属 M3b） | 出现第一个必须由用户调参的插件行为（`max_files_per_apply`、`allow_file_delete` 之类）；须同时定"声明侧给参数名/类型/边界、库级只存值"的分工与超界校验 |
| `emits` | 审计事件名今天由**核心**写（`capability/proposal`｜`apply`｜`reject`｜`backup`｜`undo`，§2.3 / §2.3.2），插件不产生自有事件 ⇒ 没有"按声明过滤"的消费者 | 出现**插件自有**事件名（第三方插件的领域事件）且核心的追加通道需要按声明白名单过滤时 |
| `verify` | v1 的自证是**模块级**：§2.6 四道安全门 + 证据锚落 `artifacts/agent/verify/**`；只有一个内置插件时 per-plugin 单测/harness 路径是空转 | 出现第 2–3 个插件（各自需自证）或要做**第三方插件准入**评审时；须同时定"缺失是否拒载" |
| `provenance` | 唯一内置插件 `kb-write` 的移植面已写在 §2.7 与根 `THIRD_PARTY_NOTICES.md`，per-plugin 字段冗余 | 出现真正 `ported-from-dsh@<pin>` 的第二方插件时；须与根 `THIRD_PARTY_NOTICES.md` 联动校验 |
| `unload` | v1 只有 `restart` 一种可能（没有热卸载消费者） | 出现热卸载需求（H 线常驻插件 / 前端挂载点）**且**给出卸载用例时 |
| `enabled`（库级开关） | **契约层冗余**：库级启停已由 `<kb>/.memoria/agent/capabilities.json` 的 `enabled[]` **条目存在性**承担（§2.2）；上游同样落在**条目级**（`dsh-src/apps/cli/src/profile-boot.ts:173` 的 `{ id, disabled: true }` patch） | 同一 `id` 需要按作用域（目录/会话）多份启用记录，或需要"随版本分发但默认关"的第三态时 |

---

## 3. 上游事实（供对齐用）

> 只读 `dsh-src/`（pin `0d1f5000`）核对，**结论不改**：上游**没有**本契约的三个"声明面"，故这三处只能是本地发明（或本地转译）。

| # | 结论 | 证据锚（`file:line`） |
|---|---|---|
| 1 | 上游**没有**"包声明工具清单"这种声明面 —— 工具是 `apply()` 里**运行时**注册的 | `ctx.tools.register(definition)`：`dsh-src/packages/core/tools/src/index.ts:1043`；取值语义对齐 `ToolSchema`（`name`/`description`/`parameters`）：`dsh-src/packages/core/tools/src/schema.ts:483-498`，构造走 `defineTool` `:545` |
| 2 | 上游**没有** per-plugin 的权限/审批声明 —— 权限只有**执行器级**档位、审批只有**会话级**档位 | `SandboxMode = 'read-only'｜'workspace-write'｜'danger-full-access'`：`dsh-src/packages/sandbox/sandbox/src/index.ts:29`；`ApprovalPolicy = 'ask'｜'never'`：`dsh-src/packages/interaction/user-approval/src/index.ts:60`；运行时询问 waterfall `approval/request`：`dsh-src/packages/interaction/user-approval/src/types.ts:85-89` |
| 3 | **启用与配置在组合层**（不是包自声明）—— 组合树按条目 `id` 打 patch | `dsh-src/apps/cli/src/profile-boot.ts:173`（`{ id, disabled: true }`） |
| 4 | **`ApprovalOutcome` 含 `unavailable` ⇒ 不可用即 fail-closed**（本契约 `approval` 的词汇对齐它） | `dsh-src/packages/interaction/user-approval/src/types.ts:32`（`'allowed-once'｜'rejected'｜'cancelled'｜'unavailable'`；`types.ts:30` 注释 "Callers fail closed on `unavailable`"） |

---

## 4. 待讨论（写组件细节）

> 只列**问题 + 可选项**，**不预设结论**；由人逐步对齐。口径细节见 [agent-capabilities.md](./agent-capabilities.md) 对应节与 §10 待拍板。

| # | 问题 | 可选项 |
|---|---|---|
| Q1 | `name` 是否保留（去掉即**5 字段**） | ① 保留（人读标签；缺省回落 `id`）② 去掉（`id` 足够，UI 标签用 `id` / 来源目录） |
| Q2 | `constrain` / `gate` 是否留在 v1 | ① 都留（写模块今天就用：`enum_from` 收紧参数、`gate` 门控可见性）② 都移出（进 §2 暂缓清单，等第二个消费者）③ 只留其一 |
| Q3 | 首批 op 个数（[§10 P8](./agent-capabilities.md)） | ① 三个（`upsert_kp` + `attach_links` + `detach_links`）② 五个（再加 `set_kp_range` + `rename_kp`）③ 一个（只有 `upsert_kp`） |
| Q4 | `approval` 覆写口径（[§10 P9](./agent-capabilities.md)） | ① 不可覆写（库级只接受启停位与 `config` 值；审批档是安全不变量）② 可覆写（需醒目告警 + 额外审计；与装载期硬校验的冲突待解） |
| Q5 | 备份保留口径（[§10 P10](./agent-capabilities.md)） | ① 写前快照 + 会话内撤销 + 每会话 5 批 / 每库 64 MiB（FIFO，最新批次永不淘汰）② 完整历史（可撤任意批次，存储线性增长）③ 仅审计重放（不保证逐字节） |
| Q6 | plan 版本号 / 兼容承诺（[§10 P12](./agent-capabilities.md)） | ① plan 自带 `v`、"只增不改"，未知 `op`/字段 ⇒ 拒整批 ② 无版本号（靠工具 schema 隐式约束）③ 完整版本协商（`v` + `min_compiler` 区间） |
| Q7 | **静默写入**与「按 agent 配权限」（**人 2026-09-21 提出，明确说"以后设计讨论"，本轮只登记不拍板**） | ① **现状（保守）**：写一律经确认卡——`propose_write` 只产 plan、零落盘，"落盘"这一步永远要人点「应用」；审批面未放宽（`DefaultApprovalPolicy` 对任何 `read_only=False` 照旧拒）。② **人提的方向**：在**良好的临时备份机制**下允许一定**静默写入**（含"向用户申请权限"这类自动路径），并让**用户能在设置里按 agent / 技能配置权限档**（只读 / 必须逐条确认 / 可静默写）。③ 更保守：维持一律确认，只把"申请权限"做成一个显式弹窗。**前置依赖**（无论选哪条都要先有）：per-agent 权限**配置面**（属 §2.1 暂缓的 `config` 字段面）、Q4 的 `approval` 覆写口径、以及"静默写"必须仍满足 §0/§9 的硬红线（写前 pre-image、备份失败即不写、盘上版本权威、撤销可用、审计可见）——**"静默"只能省掉"点确认"这一步，不能省掉备份与审计**。**2026-09-21 人追加口径**：审批/审计**不废**，但降级为**运行模式之一** —— 目标档位 `manual approval` / `auto approval` / `custom` / `all access`（人给的命名；默认档与切换面待人定），**兜底交给"像 git commit 一样"的备份 + 撤回**（每批 pre-image + post 哈希 + 可撤销批次）。**当前阶段（已落地）**：既然每批都有备份与撤回，"为审批而拆批"就没必要 ⇒ **写机制全线放开**（见 §5 同日本轮变更：`file_op_alone` / `body_edit_order` 两条顺序限制删除、批内改为按 op 顺序视图推进），但**"写前必须有备份、备份失败即不写"这条硬红线不动**。**2026-09-21 本轮落地（人再次明确"你没有放开写"后纠正）**：默认档 = **全放行**（`DefaultApprovalPolicy` 改为读 / 写都 `allowed-once`；`propose_write` 声明 `read_only=False` 且**在工具调用内直接落盘**，不再等人点「应用」）—— 即人给的四档里的 **`all access`**；前端那张卡从"确认卡"变成**回执卡**（无勾选、无「应用」，只留「撤销这一批」）。`manual approval` / `custom` 档**2026-09-22 已落地**（见 §5 同日本轮变更）：人给的四档命名收敛成**三个可切换档 + 一个派生只读态** —— `manual-approval`（逐条确认）/ `auto-approval`（**默认**：常规写自动放行、风险写问一次）/ `all-access`（不问全放行），外加**派生态** `custom`（可展示、不可作切换目标）；档位以**会话事件**落盘（`permission/preset` + `approval/policy`，可回放），默认档**按 agent 配**（`config/agent.json: permission.<agent>`，设置面板可改），切换面 = 输入区选择器 + `agent_permission_*` / `agent_approval_answer` 四个 RPC。**仍未移植**：上游的 `/permission <preset>` **命令面**、`approval/asked` / `approval/decided` **审计事件**、以及"部署可配置的档位表"（本地档位表是代码常量） |
| Q8 | **应用结果要不要"回灌"给 agent（自动续批）**（人 2026-09-21 真机撞出，**待讨论**） | 现状：卡片是**用户手动点**的，点了「应用」**不会**开启 agent 的下一轮 —— 所以"先改正文、下一批建 KP"这类**天然分两批**的写入，会停在半路（agent 自己在 intent 里写了"KP 下一批建"，用户却只看到它停了、以为没做完）。**本轮只做缓解（已落地）**：① 工具回文与提示词都要求模型**明说**"请应用后回复「继续」"；② 卡片把**会话 id** 一并带给 `agent_plan_apply` ⇒ 写入审计落在**那次对话**里（原来落 `ui-plan` 伪会话，模型与人都看不出"这批是谁提的"）。**待选**：① 直接**把应用结果作为一条会话可见事件**（如 `user/message` 的"系统"前缀，或新事件类型 + 回放时序列化成文本）⇒ 模型下一轮自然看到"已应用 N 项"，可自动续批；② 只做**前端提示**（卡片应用后追加一条对话项，模型看不到）；③ 维持现状（靠提示词请用户催）。**前置**：会话契约属事实源（[AGENTS.md §1](../../AGENTS.md)），新增可见事件类型要先定"回放是否呈现、compaction 是否计长、会不会污染 KV 前缀"——不擅自改 |

> **已拍板（2026-09-20，人）：Q3 = ①三个**（`upsert_kp` + `attach_links` + `detach_links`）、**Q5 = ①写前快照 + 会话内撤销**（每会话 5 批 / 每库 64 MiB，FIFO；**最新批次永不淘汰**）、**Q6 = ①自带 `v` + "只增不改"**（未知 op / 字段 ⇒ 拒整批）。三者逐条对应本表选项①；**Q1**（`name` 去留）/ **Q2**（`constrain`/`gate` 去留）/ **Q4**（`approval` 可否覆写）**仍待定** —— 它们只影响插件契约字段面，**不阻塞第二个写闭环起步**（该闭环按 op 与备份口径动工，不依赖这三项）。

---

## 5. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-20 | 本轮建立。契约正文自 [agent-capabilities.md](./agent-capabilities.md) §2.1 **迁出**：6 字段最小可用契约 v1 + 准入规则（§1）、10 条暂缓字段（§2）、上游事实三条 + `ApprovalOutcome`（§3）、待讨论 Q1–Q6（§4）；两份 JSON 实样与 `kind` 删除演进随迁。`agent-capabilities.md` §2.1 只留指针，§11 追加说明；登记 `conventions/docs-management.md §4.2` |
| 2026-09-20 | **重定位为「协议 + 写场景 + 模块设计」总设计文档**（用户口径：「这个文件不是只用于协议，所有相关设计都放在这，旧的我们不作为事实源；先讨论插件几种写场景……」）。① 标题由「能力插件最小协议 v1」改为「能力插件与写模块总设计」；头部「用途 / 关联文档 / 状态 / 治理约定」相应改写，并新增**事实源口径**一句：本文为**唯一事实源**，[agent-capabilities.md](./agent-capabilities.md) **不再是事实源**（降级为**历史 / 路线图**，M3 逐步迁入本文；**迁移完成前冲突处一律以本文为准**）。② 新增 **§6 迁移清单**（15 行：§2.1.1 / §2.2 / §2.3 / §2.3.1 / §2.3.2 / §2.3.3 / §2.3.4 / §2.4 / §2.5 / §2.6 / §2.7 与 §8 / §9 / §10 中与写模块相关的行，逐行标 `未迁`；**本轮不搬**）。③ 新增 **§7 写场景清单（讨论中，2026-09-20）**：一张表 **44 行**（1.x 九行 / 2.x 八行 / 3.x 五行 / 4.x 八行 / 5.x 五行 / 6.x 九行），覆盖纯知识库维护 / 文件内容增删改 / 字体样式 / 图片引用 / 代码块书写 / 读码新发现的写入形态（侧车重建、pending、锚点重锚、路径级联、会话与配置面），每行给「现状（`file:line` 或"无"）+ 落到哪个 op / 原语 + 是否需新原语（暂定名）+ 审批档建议（标"待定"）+ 归属」。④ 新增 **§8 场景间的关系与排序建议（讨论用）**：共用原语分组、依赖关系（连边依赖 KP 存在等）、候选第一批（标"待你拍板"）、以及**判断今天不该给 agent 做**的场景及理由。⑤ **§1–§5 编号与内容不变**（新增节追加在 §5 之后，编号连续）。**docs only，未改任何源码**；`agent-capabilities.md` 仅 §2.1 指针补一句 + §11 追加一行；登记 `conventions/docs-management.md §4.2`；`docs/todo.md` **未改动**（AG04 行引用的 §2/§10 内容仍在原文，字节中性） |
| 2026-09-20 | **M3a 只读骨架第一片落地（首次有源码）**：新增 `src/memoria/services/agent/plan.py`（plan schema + `validate_plan` / `preview_plan` / `resolve_target`，**零 apply 函数**）+ `tests/test_agent_plan.py`（10 例）。**两条实证结论请人复核并据此校正 §2.3.3 / §2.3.4（本轮只登记，未改那两节正文）**：① **`scan_link_text_matches()` 不产出纯文本出现** —— 对纯文本正文它只回 `'。注意力机'` / `'在别处。'` 这类**模糊建议命中**（实测三种 `search_options` 组合结果一致），它匹配的是**既有 wikilink 路由 + 建议**；纯文本出现（即 `attach_links` 要落的那种）由 `wrap_plain_on_lines()` 内部调用的 `find_plain_text_in_line()` 定位 ⇒ §2.3.3「编译器要解析」列里写的 `scan_link_text_matches` **对纯文本不成立**，本片改用后者（候选行与包裹位置因此由**构造**保证一致，仍满足"同一套校验器"硬不变量）。② **`normalize_link_edge_type()` 对未知值一律回落 `reference`** ⇒ 单看归一化结果会把 `contain` / `prerequisite` 放行；卡白名单必须卡**原值**（本片先卡原值再归一化）。另记一条范围口径：`preview_plan()` 的"零落盘"= **不写事实源**（md / sidecar / manifest / pending 逐字节不变），但复用的 `resolve_link_target()` → `build_kp_index()` 会补写**可再生缓存** `.memoria/cache/search_aux/kp/*.json`（[AGENTS.md §1](../../AGENTS.md)：`cache/**` 不作为事实源）—— 测试已逐文件钉住"只碰 cache"。**未做**：四个只读面的 RPC 暴露、前端确认卡、`set_kp_range` / `rename_kp` 的编译、`detach_links` 的正文 diff 试算、`upsert_kp` 的 sidecar 终校。登记 `conventions/docs-management.md §4.2`；`docs/todo.md` 未编辑（另一写者并发重写中） |
| 2026-09-20 | **拍板回填 Q3 / Q5 / Q6（人已确认，全部取选项①）**：Q3 = 首批**三个 op**（`upsert_kp` + `attach_links` + `detach_links`）、Q5 = **写前快照 + 会话内撤销**（每会话 5 批 / 每库 64 MiB，FIFO，最新批次永不淘汰）、Q6 = plan **自带 `v` + "只增不改"**（未知 op / 字段 ⇒ 拒整批）。记录位置：本节末 §4 表下新增「已拍板」块（**只追加，未改 Q1–Q6 原行**）。Q1 / Q2 / Q4 仍待定且**不阻塞**第二个写闭环。**顺带定下第二片（写入闭环）的施工顺序**（不新增契约、不改人已确认条目）：① **备份子系统先行**（Q5 已定，它是所有写场景的前置，且**零事实源写入**可独立验收）→ ② 三个 op 的 apply（编译器产物 = 原语调用序列，写前取 pre-image）→ ③ 审批 + 审计（`approval` 档位待 Q4）→ ④ 四个只读面 + apply 的 RPC 暴露与前端确认卡。**本轮 docs only，未改任何源码** |
| 2026-09-20 | **备份子系统落地（第二片 ①：写前 pre-image + 保留 + 撤销的数据面，Q5 = ① 已拍板）**：新增 `src/memoria/services/agent/backup.py` + `tests/test_agent_backup.py`（**9 例**）。**① 逐条照 §2.3.2 落地**：目录 `<kb>/.memoria/agent/backups/<session_id>/<txid>/{journal.json, files/<原相对路径>}`；粒度 = 一个 `txid` 一个批次；新建文件记 `{"existed": false}` 且**不留字节**（撤销 = 删除）；**原字节复制**（不存 diff，不重新编码/不规范化换行）；上限 **8 MiB / 32 MiB** 超限即**预检失败且不留半个批次**（不降级为"无备份的写入"）；保留 **5 批/会话**、**10 个会话目录/库**、**64 MiB/库**，FIFO 且**永不淘汰当前会话最新批次**；撤销前比对 sha256，不符即**拒绝**（不静默覆盖用户手改）；撤销失败**保留备份 + 如实报错**。**② 两处本地补充（请人复核后并入 §2.3.2 正文）**：㈠ **批次目录整体原子落位**（先在 `<txid>.tmp-<pid>` 建齐 `files/**` 与 journal，再 `os.replace()` 整目录改名 ⇒ 任何失败都只需删临时目录，**不会留半个批次**）；㈡ **新增 `post.json`** —— §2.3.2 第 2 条列的 journal 字段只有 **pre-image** 哈希，而第 5 条要求"撤销前校验当前 sha256 == **apply 时**记录的 sha256"，apply 又发生在 journal 落盘**之后** ⇒ pre-image 哈希当不了这个凭据；故增 `record_post_images()`（apply 四原语后由 apply 入口调用一次，落**写后**逐文件哈希），撤销自动读它校验，**缺 `post.json` 默认拒绝撤销**（fail-closed；`allow_unverified=True` 仅供人工处置且如实标 `verified:false`）。**③ 测试自己抓到两个真问题（已修）**：㈠ **整批全是新建文件时临时目录没建** ⇒ journal 写不进去（补 `os.makedirs(tmp_dir, exist_ok=True)`）；㈡ 绝对路径被 `lstrip("/")` 后**悄悄当相对路径用**（`/etc/passwd` → `<kb>/etc/passwd`，静默改语义）⇒ 改为**绝对路径一律拒**，且调用方不再做"宽容归一化"。**④ 验收**：`pytest tests/test_agent_backup.py -q` **9 passed**、`pytest -q` **375 passed**（原 366）；测试逐条钉住"除 `.memoria/agent/backups/**` 外逐文件不变""字节级撤销往返""apply 后被手改 ⇒ 拒绝且不覆盖""缺 post.json 默认拒""FIFO 淘汰且最新永不淘汰"。**⑤ 未做（如实）**：`capability/backup` / `capability/undo` 事件与审计、撤销的 `approval=confirm`、打开/关库时的 trimming 挂点（属第二片 ③④）；撤销后的一致性恢复（清 `DocumentService._cache` + `validate_kb()`）留给 apply 网关。登记 `conventions/docs-management.md §4.2`；`docs/todo.md` 未编辑（另一写者并发重写中） |
| 2026-09-20 | **② apply 入口落地 + 修掉一个阻塞它的产品缺陷**：新增 `src/memoria/services/agent/apply.py`（编译器 + apply 入口 + 整批回滚）+ `tests/test_agent_apply.py`（8 例）。**调用序**照 §2.3.2 第 4 条：`validate_plan` → `snapshot_pre_images` → **原语白名单**（`confirm_kp_range` / `update_kp` / `apply_link_instances` / `detach_link_instance` / `delete_link_route` 五个落点，别的一律不可达）→ `record_post_images` → 机会式 `trim_backups`；任一步失败 ⇒ 用该批次 pre-image **整批回滚**并如实回报。**三条实证发现（请人复核后并入设计正文）**：① **产品缺陷（非 M3 引入，人机 UI 同链路）** —— `link_text_search` 纯文本分支把 **body 绝对偏移**当**行内**偏移去查 `view_to_orig`，返回的 span 整体右移且 `matched_text` 不等于锚文本（最小复现：`前缀文字注意力机制后缀文字。` ⇒ `col=9, '后缀文字。'`，应为 `col=4, '注意力机制'`）⇒ `apply_link_instances()` 会把正文改坏（实测曾产出 `注意力机制是核心[[。注意力机]]制也出现在别处。`）。**已修**（等量替换、行号零漂移）：`row_pos = pos - (body.rfind("\n", 0, pos) + 1)` 后再查映射；新增 `tests/test_link_text_search.py`（13 例：三例最小复现 + 通用不变量 `row[replace_start:replace_end] == matched_text`）。② **§2.3.3 缺一条前置**：同一 plan 内「先建点、后连边」要求**前序 op 的结果对后序 op 的 `attach_links` 目标解析可见**（信封注释已写"前 op 的结果对后 op 可见"，但校验列未落实）—— 否则最常见的 plan 恒被 `target_not_found` 拒；已在 `validate_plan` 里补 `pending_ids` 集合 + 回归用例。③ **`attach_links` 在 M3a 内只能到行粒度**：`apply_link_instances()` 只收 `selected_lines`（**没有列**），同一行多处出现时只能包裹其中一处（实测包最后一处）⇒ 设计稿 op 字段 `occurrences[].matched_text` **落不到列级**（要列级得改原语签名，属后续批次）。**测试自己抓到的**：`upsert_kp`+`attach_links` 同 plan 的 5 个用例先失败（即发现 ②）；`instances` 按出现处记（同行两处 ⇒ 两条 `{line:3}`）。**验收**：`pytest -q` **393 passed**（原 375；无 xfail 残留）。**未做**：③ 审批 + 审计（`capability/apply`/`capability/backup`/`capability/undo`）、④ RPC 与前端确认卡；撤销的网关侧一致性恢复（清 `_cache` + `validate_kb`）。**另记一条工具链教训**：测试若构造 `DocumentService` 而不隔离 `MEMORIA_CONFIG_DIR`，会经「记录最近打开」写**真实** `config/ui-settings.json`（本轮实测把 8 条临时库写进 `recent_kbs`，已清理并给两个 fixture 补隔离）—— 建议加 `tests/conftest.py` autouse 隔离，待人拍板。登记 `conventions/docs-management.md §4.2`；`docs/todo.md` 未编辑 |
| 2026-09-20 | **更正上一条的「行粒度」结论 + 给原语补列级 span（人已拍板）**：用户追问「原本 memoria 就是可以选中一行内任意文字建跳转，怎么现在颗粒度就变成行？」—— **追问成立，我上一条的说法不准确**。**① 查清人机 UI 真实路径**（读码）：选中文字 ⇒ `wrap_text_as_link()`（`document.py:2821`，**只建 sidecar 路由、不碰正文**，`update_markdown=False`；锚文本 = 选中的那段文字）⇒ 随后**匹配面板**确认包裹位置 ⇒ `apply_link_instances(…, [...m.selected], …)`（`app.js:6025`，`m.selected` 是**行号集合**，`app.js:5963`）。⇒ 所以"字符级"的真正来源是**锚文本本身**（选中哪段文字，锚就是哪段）；只有"**同一行里该文字重复出现**"时，锚文本与行号两条信息都相同、才是定位极限 —— 而**人机 UI 在那种情形下同样只包一处**（同函数同参数）。**② 拍板：补列级参数**。`DocumentService.apply_link_instances()` 新增关键字参数 `selected_spans: dict | None = None`（**默认 None ⇒ 旧行为逐字不变**，人机 UI 与既有调用者零影响）；给了 span 就**必须先与锚文本逐字相符**，不符即 `{status:"error"}`（`document.py:2611-2626`）—— 与 `plan.py` 校验形成**双保险**。RPC `apply_link_instances`（`ui.py:564`）追加同名可选参数并透传。**③ plan 侧扩字段**：`occurrences[].col`（**1 起列号**，与 `#L3C2` 同口径）⇒ 校验核对该列起确为锚文本（新错误码 `col_mismatch`），并产出 `pinned_spans: {line: (start0, end0)}`；编译期把它作为 `selected_spans` 传给原语；`preview_plan()` 用**同一份 pinned span** 试算 ⇒ 预览与落地不可能漂移。同行多处**且没给 col** ⇒ 新警告 `ambiguous_occurrence`（不拒，如实说明"哪一处由后端按行取值决定"）。**④ 设计稿 §2.3.3「需新增的最小能力」第 2 条**（`occurrences[].matched_text` 前置核对）**据此升级为"行 + 列"双核对**，请人复核后并入该节正文。**⑤ 顺带（未做，可选后续）**：人机 UI 的匹配面板其实已列出**每一处**，只是确认时把列丢了 —— 前端接上 `selected_spans` 后即可区分同行两处；本轮**未改前端**。**⑥ 验收**：`pytest -q` **397 passed**（原 393；+4 例：C1/C10 两个方向各包对一处、列号不符即拒、不给 col 的旧口径回归、`ambiguous_occurrence` 警告）；三个原语锚点行号未动（`confirm_kp_range` 1360 / `detach_link_instance` 2428 / `apply_link_instances` 2493）。登记 `conventions/docs-management.md §4.2` |
| 2026-09-20 | **新增 §9「写冲突优先级与并发保护」（人已拍板）+ 落后端一半实现**：用户问「用户修改和 agent 修改哪个定位更高优先？」⇒ 结论**不是"某一方优先"**，而是三条规则：① **盘上版本 = 唯一权威**（任何写者写前必须确认"我读到的版本 == 盘上版本"，不一致即拒写并回报 `stale_write`）② **人的当下操作最高**（agent 不得趁人正在编辑同一文件时抢写）③ **冲突由人明示决定**（前端弹「重载 / 以我为准 / 看差异」，**agent 不自动合并、不静默赢，人的过期 buffer 也不自动赢**）。**明确否掉**多线程（写路径本是无锁同步短事务，开线程只会引入进程内竞态）与增量修改（对 lost update 不免疫，属优化后置）。**实现（本轮只做后端一半）**：新模块 `storage/file_version.py`（`file_version`=内容 sha256 / `rel_version` / `with_version` / `guard_save`）；`ui.py` 的 `load_document` **追加** `version`（只增不改）、`save_document` 追加可选 `base_version`（**空串 = 不校验 ⇒ 旧行为逐字不变**）—— 两处都是**等量替换**委托进新模块，`services/document.py` **一行未动**（零行漂移）；`preview_plan()` 返回 `base_versions`（= 用户看过的那一版），`apply_plan()` **先比版本再谈 plan**（文件变了还报"plan 非法"是误导），不一致 ⇒ `stale_write` 且**尚未建备份**。**验收**：新增 `tests/test_write_conflicts.py`（8 例：版本=字节 sha256、`load_document` 带版本、版本匹配才写且回写新版本、**过期保存被拒且不覆盖**、不传版本=旧行为、`preview_plan` 交基准、**预览后被改 ⇒ 整批拒且不建备份不写盘**、版本一致正常落地）；`pytest -q` **405 passed**（原 397）。**未做（下一步）**：前端保存带 `version` + `stale_write` 三选一弹窗（含 i18n 中英）、apply 期间前端忙位与"当前文件正被编辑 ⇒ 拒 apply"。**另记**：`_write_body` 的 `os.replace` 偶发 `WinError 5`（外部瞬时持锁，非句柄泄漏）—— 是否加短退避重试仍待人拍板。登记 `conventions/docs-management.md §4.2` |
| 2026-09-20 | **写冲突保护：前端一半落地（版本令牌"通电"）+「以我为准」先备份再覆盖**：`app.js` **IIFE 内仅四处同行内替换**（守卫行 `deferIfBusy(markDirty)` / 保存调用带 `state.doc?.version` / **失败行**改走 `onSaveFailed`（返回 true 即不再 `console.warn`）/ 成功后滚回新版本；`git diff` 只有 `@@ -124 / -133 / -135 / -137 @@` 四个单行 hunk）⇒ **零行漂移**；文件末尾追加 `memoriaWriteGuard` 块（80 行）（`setBusy` / `deferIfBusy` / `onSaveFailed` + **三选一弹窗**「重载 / 以我为准 / 稍后」，复用既有 `.-modal` 样式与 `confirmTreeAction` 同构；跨闭包只经 `window.MemoriaApp` 门面，`state` 是同一对象引用 ⇒ 版本可直接读写）。**「以我为准」= `force=True`**：后端 `guard_save` 先走 `services/agent/backup` 落 `manual-force/<txid>` 批次（含 `post.json`）**再覆盖**，备份失败即拒（不降级为无备份覆盖）⇒ 被覆盖掉的那一版**可直接 `restore_batch` 撤销**（测试已跑通往返）。i18n 以**文件末尾 `Object.assign`** 追加 `writeConflict.*`（中英各 8 键，`i18n_selftest.js` 全 PASS）。**验收**：`pytest -q` **409 passed**（原 405；`test_write_conflicts.py` 12 例，含两个前端接线不变量——保存带版本/成功后滚基线/`stale_write` 只走弹窗/忙位让路/三选一按钮与 force 调用，以及中英键成对）；`node --check` 3/3。**仍未做**：apply 期间的前端忙位与 agent 写入口接线（等 ④ 一起：`setBusy` 包住 apply + apply 前查"当前文件脏 ⇒ 拒"，即规则 ② 的另一半）。登记 `conventions/docs-management.md §4.2` |
| 2026-09-20 | **第二片 ③「审计事件 + 撤销后一致性恢复 + `detach_links` 预览 diff」落地（log-only，不动请求体）**：① **新模块** `src/memoria/services/agent/audit.py`：`append(kb_path, session_id, type, payload)` —— 把写动作**追加进既有会话 `session/*.jsonl`**（`SessionStore.append` + `flush`），`capability/apply` / `capability/undo` / `capability/force_save` 三种事件；**fail-open 但如实**：返回 `{status:"ok"\|"skipped"\|"error"}`，**不抛不回滚**（写已完成，审计失败只是没留痕，绝不因此撤销用户的写入）。之所以选会话流而不是新事实源：回放对未知 `type` 直接跳过、`compaction.event_chars()` 计 0 ⇒ **不改请求体、不动 KV 前缀、不影响回放**（与既有 `session/title` 同构），避免新增并行事实源（AGENTS.md §1）。② **接入三条写路径**：`apply.py` **四处返回**（`stale_write` / 备份失败 / 原语失败 / 成功）都带 `audit` 字段；`backup.restore_batch()` 新增 `audit: bool = True` 形参并在成功后记 `capability/undo`；apply 的**事务内回滚显式传 `audit=False`**（它不是"用户撤销"，apply 失败事件已记全貌，不冒充）；`file_version.py` 的「以我为准」强制覆盖记 `capability/force_save`。③ **撤销后一致性恢复**（补上第二片 ① 留的尾巴）：新增 `apply.recover_after_write(kb_path, rel_paths, service=None)` —— 清 `DocumentService._cache` 中受影响条目 + 跑 `validate_kb()` 并回报 `{cache_cleared, errors, warnings}`。**为什么必须显式清**：`backup.restore_batch()` 是**直接写盘**、不走 `save_document()`，应用侧那份进程内解析缓存会残留撤销前的正文。④ **`detach_links` 预览不再是空 diff**：`plan.py` 的 `preview_plan()` 原先如实标 `diff_available: false`，现改用 `detach_link_instance()` 内部**同一个原子函数** `unwrap_lines()` 给出逐行 before/after ⇒ **预览与落地不可能漂移**（docstring 同步更正）。⑤ **验收**：新增 `tests/test_agent_audit.py`（**7 例**：成功/被拒都留痕、撤销留痕、**内部回滚不冒充 undo**、force_save 留痕、审计 fail-open 且如实、`recover_after_write` 清缓存并校验、detach 预览出行级 diff）；`pytest -q` **427 passed + 3 flaky**（见下）；`py_compile` 7 文件全过。⑥ **如实交代两件未决**：㈠ `test_agent_apply.py` 的 **`WinError 5` 非确定性 flake**（`os.replace` 被瞬时持锁；本次全量跑 3 挂，单跑必过）—— 是否加**有界退避重试**（仅 `WinError 5/32`）**仍待人拍板**；㈡ `recover_after_write` 触碰 `_cache` 私有属性，更干净的做法是给 `DocumentService` 加**公开失效 API**（已登记，未做）。⑦ **仍未做**：审批应答者与确认 UI、RPC 暴露 + 前端确认卡 + apply 忙位接线（M3a 剩余 ③④）。登记 `conventions/docs-management.md §4.2`；`docs/todo.md` 未编辑（另一写者并发重写中） |
| 2026-09-20 | **写路径 `os.replace` 加有界退避重试（人已拍板）**：`test_agent_apply.py` 的非确定性 `PermissionError: WinError 5`（连续写同一 md 时偶发，重跑从不复现、单跑必过；句柄泄漏已排除）判定为**外部瞬时持锁**（AV / 索引器 / 后台词法索引线程）。**① 新模块** `src/memoria/storage/atomic_write.py`：`replace_with_retry(src, dst)` —— **只**对 `WinError 5`（拒绝访问）/ `WinError 32`（共享冲突）重试，`5 × 50 ms`（上限 250 ms），每次重试 `logger.warning`（带 winerror / 第几次 / 源→目标，**可见**），**耗尽仍抛**（绝不把真权限错误吞成静默成功）；非 Windows 上 `OSError` 无 `winerror` ⇒ 等价于裸 `os.replace`。**② 两处写点等量换用**（`document.py` **零行漂移**，import 追加在文件末尾）：`save_document` 的 `os.replace(tmp_full, full)`（`:347`）与 `_write_body` 的 `os.replace(tmp, full)`（`:2074`）—— 即"人机保存"与"agent 写"两条正文写路径。**③ 验收**：`tests/test_write_conflicts.py` 追加 3 例（瞬时锁救回且睡满次数 / 非瞬时**立刻抛不重试** / 有界耗尽仍抛）⇒ 15 passed；`test_agent_apply.py` 连跑 **9 次全绿**。**④ 如实交代**：这 9 次**没有一次真触发到那个瞬时锁**（无重试日志）⇒ "重试把人救回来"目前**只由单测覆盖**，尚未在线复现（能证明的只是没引入回归）。**⑤ 有意不扩面**：sidecar / manifest / `kp_rename` / `backup.restore_batch` 等其余 `os.replace` 点**保持裸 replace**（本轮只治"用户与 agent 的正文写点"）。`docs/todo.md` 未编辑 |
| 2026-09-20 | **M3a ④ 落地：计划 API 的 RPC 暴露 + 前端确认卡 + apply 忙位（第二片收尾）**。**① RPC 面（`ui.py`，追加在类末尾 ⇒ 既有 `ui.py:<行>` 锚点零漂移）**：四个只读面 `agent_plan_validate` / `agent_plan_preview` / `agent_plan_resolve` / `agent_plan_audit` + `agent_plan_apply(plan, kb_path, session_id, base_versions)` + `agent_plan_undo(kb_path, session_id, txid)`；只读面**零落盘**且一律复用 `plan.py` 的同一套校验器（不另写宽松判断）；当前库复用应用侧 `DocumentService`（同一份缓存与 KP 唯一性判定），**别的库传 `None`** 由 plan 层自建（`agent_plan_audit` 只支持当前库，否则 `kb_mismatch`）；未开库一律结构化 `no_kb`。`agent_plan_apply` 省略 `session_id` ⇒ 归入 `ui-plan` 伪会话（**返回值带回** `session_id`/`txid`，供撤销原样传回）；成功后跑 `recover_after_write()`（清解析缓存 + `validate_kb()`）。`agent_plan_undo` 走 `restore_batch()` + 同一套恢复，**外部改动保护 fail-closed**（apply 之后被改过 ⇒ `external_change` 整批拒）。**② 两条口径本轮钉死**（此前含糊）：㈠ **校验期拒绝 = 零字节变化**（连审计都不写，对齐 §2.6 安全门第 1 条"无 silent 写"）；**只有过了校验的落地尝试**（stale / 备份失败 / 原语失败 / 成功）才一律留痕 `capability/apply`。㈡ **勾选 = 编译前的选择**：未勾的 op 由**前端**从 `plan.ops` 去掉再提交，后端**不引入**任何"部分执行"（仍 all-or-nothing）。**③ 前端（不改 `app.js`，零行漂移）**：新模块 `js/plan-confirm.js`（`window.MemoriaPlan = {open, openFromText, close, undo}`）—— dry-run 预览 → 卡片摊开"哪个文件、哪一行、before → after" + 逐条勾选（默认全勾，按钮联动计数）→ apply；§9 规则 ① 靠 `preview.base_versions` **原样回传**；§9 规则 ② 的另一半 = `MemoriaWriteGuard.setBusy(true)` 包住 apply（人机保存经 `deferIfBusy` 重新入队）+ 当前编辑未落盘时**先有界等待（≤3 s）仍脏即拒**；成功后给 txid + 「撤销这一批」，并 `openFile(cur, {skipNav:true})` 重开当前文件；失败（含 `stale_write`）给「重新预览」。`index.html` 末尾挂载该文件、`app.css` 末尾追加计划卡样式块、i18n **中英各 23 键**（`plan.*`，末尾 `Object.assign`）。**④ 验收**：新增 `tests/test_agent_plan_rpc.py`（**14 例**：四个只读面逐文件 sha256 不变、未开库一律 `no_kb`、preview→apply→undo **逐字节还原**（正文+sidecar+manifest+pending）、看完预览后被改 ⇒ `stale_write` 且**未建备份**、非法 plan ⇒ `invalid_plan` 且**含审计在内零字节变化**、撤销缺 `session_id` 明确报错、RPC 路径的 `capability/apply` 落会话文件、确认卡接线不变量（勾选在编译前 / 忙位包住 apply / 脏编辑拒写 / 写完重开当前文件 / 中英键成对））⇒ `pytest -q` **447 passed**；`node --check` 3/3；`scripts/i18n_selftest.js` 全 PASS。**⑤ L4 交互实证**（静态服务器 + 浏览器实渲染 + 截图）：契约齐全的 preview 下卡片渲染为标题（含 intent）/摘要（共 3 项、2 文件）/3 个默认全勾的勾选项/两个文件分组/行级 `- before` `+ after`/取消+应用按钮，盒子 760×333 **无重叠、无裁切、无溢出**，`plan-confirm.js` **零报错**；缺 `status` 的非法 preview 按设计落入"预览异常"分支（证明 fail-safe 分支可用）。**⑥ 未做（如实）**：**工具面未接** ⇒ 模型还不能自己产出 plan，故卡片当前入口是 JS API（`MemoriaPlan.open(plan)` / `openFromText(json)`，控制台可测），聊天里出现 plan 即自动给确认按钮属下一步；`approval` 的**应答者**（`ApprovalRequest` / `AskPolicy` / `agent_ask_start`+`agent_ask_poll` 这条现成通道）仍未挂；`recover_after_write` 仍触碰 `_cache` 私有字段（待加公开失效 API）。登记 `conventions/docs-management.md §4.2`；`docs/todo.md` 未编辑（另一写者并发重写中） |
| 2026-09-20 | **更正：计划确认卡改挂「对话栏」（用户口径）+ 入口按钮 + 长文本悬浮看全** | 用户反馈：「这个卡片不是应该做在对话栏里面吗，或者在对话栏里面给入口，文字不够显示可以鼠标悬浮显示」—— **成立**。计划是"智能体提出、人确认"的东西，它属于那条对话流，用弹窗盖住界面是错的位置。**① 卡片改为对话栏内的一条聊天项**：渲染进 `#agent-messages`，class 与既有消息同族（`.-agent-msg -agent-msg--assistant`）+ 专用 `.-agent-plan-msg`（左侧主题色细边区分）；追加后 `scrollTop = scrollHeight`（与问答流同款：新内容滚进视野），并移除 `.-agent-empty` 占位。**② 对话栏不可用时回落弹窗**（面板收起 / 没有对话栏 / `clientHeight=0`）⇒ 复用 `.-modal` 族，**绝不出现"点了没反应"**。两种承载共用同一份 `.plan-*` 样式与同一套 `data-act` 按钮（`apply` / `discard` / `undo` / `repreview`）。**③ 入口按钮长在对话栏输入区**：`MemoriaPlan` 自己在 `. -agent-composer-actions` 里装一个「计划」按钮（id `agent-plan-open`，幂等），点开=粘贴 plan JSON → 「预览」→ 卡片落进对话；**装按钮的时机是 `MemoriaBridge.onReady`**（早装拿不到 i18n ⇒ 会显示裸 key），切语言时经 `MemoriaI18n.addRefresh` 同步按钮文案并**原地重绘**已开的卡片（不再打 RPC）。这是**工具面接入前的临时入口**，措辞已写进 `plan.pasteHint`（模型能自己出计划后，卡片直接出现在回答之后）。**④ 长文本一律"单行省略 + 悬浮看全"**：文件名 / 意图 / 操作名 / 差异行都 `white-space:nowrap + overflow:hidden + text-overflow:ellipsis + cursor:help`，并把**全文写进 `title`**（新增 `clipped()` 统一出口）；`.plan-op-name` 另给 `flex:1 1 auto; min-width:0`（flex 子项不收缩则省略号不生效）。**⑤ 零行漂移**：仍然**不改** `app.js` / `agent-panel.js` / `index.html`（入口按钮与卡片全是本模块 DOM 注入），`app.css` 只在**文件末尾**追加/整理自己那块样式。**⑥ 验收**：`tests/test_agent_plan_rpc.py` 更新接线不变量（断言卡片落 `#agent-messages`、有弹窗回落、`clipped()` 带 `title`、入口按钮幂等、`onReady` 后才装、中英 27 键成对）+ 新增 1 例 CSS 断言 ⇒ 该文件 **15 passed**；`node --check` 3/3、`i18n_selftest` 全 PASS。**⑦ L4 复验（浏览器实渲染，数字为实测）**：卡片刻在对话栏内（`#agent-messages .-agent-plan-msg = 1`、`.plan-box = 0`、未新增弹窗；卡片 331×313.7，纵向子块 top/bottom 单调不交叉、横向溢出元素 0、真实重叠 0 对、`plan-confirm.js` 报错 **0** 条）；截断与悬浮成立（`.plan-file-path` 308/448、`.plan-intent` 308/325、`.plan-diff-line` 286/1249 均溢出且 `title` = 全文 114/128 字、`cursor:help`）；入口按钮在 `-agent-composer-actions` 内且可见；把 `#agent-messages` 隐藏后再开 ⇒ `.-modal .plan-box = 1`（**回落分支可用**）。**⑧ 一处如实说明**：`.plan-op-name` 未截断时不自带 `title`（`title` 挂在外层 `label.plan-op` 上，含"操作名 · op_id"），不影响"看全"。登记 `conventions/docs-management.md §4.2`；`docs/todo.md` 未编辑 |
| 2026-09-20 | **W 线第一步落地：`propose_write` 提议工具（工具面第一次能"写"）+ 提示词能力自述改为按工具在场二选一** | 用户报障：「agent 说它对知识库是只读的，只给改动清单 + 草稿，改不了 trial.md」—— 读码确认**它没坏、是在遵守提示词**：① 工具面全是只读（`tools/kb.py:484`「绑定到某个知识库的只读工具集」，`tools/__init__.py:12` M1 只读）；② 系统提示词直接写「当前能力是**只读**的：…不能修改、创建或删除任何文件」（`prompt.py:239`）。⇒ 补上设计 §2.3 第 1 步缺的那一环。**① 新工具** `propose_write`（`tools/kb.py` 末尾追加，`KB_TOOL_NAMES` 8→16 名等量改写、`build_kb_tools()` 返回元组末行同行追加 `*_write_proposal_tools(root)` ⇒ 上方 `file:line` 锚点零漂移）：参数只收 `intent` + `ops`（**信封的 `v`/`txid`/`op_id` 由程序填** —— `txid` 有严格形态、`op_id` 要 plan 内唯一，交模型最易错）；三件事：`validate_plan`（复用人类 UI 同一套校验器）→ `preview_plan` dry-run → 把 plan 排进**进程内信箱**；不过校验就回 `INVALID_PLAN` + 逐 `op_id` 明细（让它**重写整批**），成功则在工具结果里写明「**尚未写入**…不要声称已写入」。整段由 `kb_read_only()` 包裹 ⇒ **连可再生缓存都不落库**；测试逐字节钉住"提议前后全库（含审计）不变"。**② 一处与设计稿的偏差（如实登记）**：工具声明 `read_only=True`。`registry.Tool.read_only` 的定义就是「该工具**不改动知识库**」（`registry.py:106`），本工具确实一个字节都不落盘 ⇒ 字面成立；设计稿那句「写工具声明 `read_only=False` ⇒ 必过审批」假定的是"工具=落盘那一步"，而本架构把落盘推到**人手确认之后**（卡片 → `agent_plan_apply`）⇒ **审批面没有被放宽**（`DefaultApprovalPolicy` 对任何 `read_only=False` 照旧一律拒绝，本轮未改它一行）；M3b 的真写原语工具仍须 `read_only=False`。另：`_ValidationService` 是"只借 `DocumentService.check_kp_id` 的实现、不建实例"的最小只读服务（建实例会走 `set_kb_path()` ⇒ 写用户 `config/ui-settings.json`），`__getattr__` 让 plan 校验将来新增依赖时**立刻可见**而不是静默降级。**③ 计划怎么到人眼前**：新 RPC `agent_plan_pending`（`ui.py` 类末尾追加，取走即清空）⇒ 前端 `plan-confirm.js` **包装门面 `call`**，在每轮问答收尾（`agent_ask_poll` 报 `done`/`error`）顺手取一次并逐张开卡（同款做法见 `agent-panel.js` 包装 `call` 追加思考游标；这样**不必**改 `agent-panel.js` / `index.html`，也不新增轮询循环）。卡片同时改为**追加不替换**（对话流语义：一张卡一条），切语言时按状态栈原地重建。**④ 提示词**：`prompt.py:239-240` 两行**等量替换**为 `_CAPABILITY_RO/_WRITE_HINT_RO` 与 `_CAPABILITY_WRITE/_WRITE_HINT_WRITE` 的二选一（判据 = 提议工具在不在 `tools` 里；两组常量与 `_has_write_tool()` 追加在文件末尾 ⇒ `prompt.py:385-394` 等锚点零漂移）；写侧自述同时说清"可以提议"与"**你自己没有落盘权**"，两头堵住"拒绝配合"和"谎称已写入"。**⑤ 验收**：`tests/test_agent_plan_rpc.py` **21 例**（+6：工具在场且 `read_only=True` 被默认策略放行、提议成功 ⇒ 信箱有 plan 且**全库逐字节不变**、越界 op ⇒ `INVALID_PLAN` 且**不进信箱**、工具文本含「尚未写入」、RPC 取走即清空、提示词按工具在场二选一）⇒ `pytest -q` **454 passed**（顺带更新 3 处"工具面清单"守卫断言：`KB_TOOL_NAMES` 尾部改为「会话四 + 引用两 + 提议一」）；`node --check` / `py_compile` 全过。**⑥ 未做/未覆盖（如实）**：**端到端（模型真的调工具）没跑** —— 需要你配好端点后实测（模型行为不可离线断言）；`agent_plan_pending` 用**进程内**信箱（重启即丢，非事实源）；卡片仍是"一张卡一批"，模型若在一轮里提两批会并排两张。登记 `conventions/docs-management.md §4.2`；`docs/todo.md` 未编辑（另一写者并发重写中） |
| 2026-09-21 | **W 线第二步落地：改正文（`replace_lines` / `insert_lines` / `delete_lines`）+ 新原语 `kb.file.edit`** | 用户口径：「现在还没有写正文能力，我还没办法验证」—— 成立：§7 的 2.1/2.2/2.3 三行都标着「**需要新原语** `kb.file.edit`」，而今天只有**整篇**入口 `DocumentService.save_document()`（`document.py:317`），agent 侧三个 op 只能建点/挂拆跳转，动不了正文。**① 新模块** `src/memoria/services/agent/body_edit.py`：`splice(body, edits)` —— 内部按 `splitlines(keepends=True)` 取行、splice 后 `"".join()` ⇒ **只有被点名的行会变**（未触及行逐字节保持，含 CRLF、末尾有无换行、"最后一行没有换行"）；三条编辑模式（`replace` / `insert` / `delete`）与两个自检函数分开：`normalize_edits()` = **结构级**（mode/行号/区间/文本非空/不重叠），`check_edit()` = **内容级**（行号界内 + **`expect` 与盘上原文逐字相符**）。**② 落盘仍走唯一入口** `save_document()`（frontmatter 保留、tmp+`os.replace`、缓存失效、manifest 记账全在那一处）⇒ `apply_body_edits()` 只负责"把正文算对"。**③ 三条 op（只增不改）**：`replace_lines`（`range` + `expect` + `text`）、`insert_lines`（`after` + `expect`（`after=0` 时省略）+ `text`）、`delete_lines`（`range` + `expect`）；`expect` 是**必填**的内容级门槛（与 `attach_links` 的 `anchor_text` 同款口径：**不猜**）；`v`/`txid`/`op_id` 仍由程序填。**④ 两条跨 op 硬规矩（本轮新钉）**：㈠ **同一文件里"改正文"的 op 必须排在"按行号锚定"的 op 之后**（锚定 op 不改行数 ⇒ 只要先跑，两边行号都成立；反过来必错位）⇒ 违反报 `body_edit_order` 并让模型调顺序；㈡ 同一文件的多条改正文**区间不得重叠**（`overlapping_edits`）。**⑤ 编译/落地**：同一文件的改正文在编译器里**合成一次** `edit_body` 调用（`splice` 内部按行号**从大到小**应用 ⇒ 每条行号都以"编辑前"的正文为准，调用方不必倒序）；原语白名单新增 `edit_body`（仍是白名单第 6 项，别的一律不可达）；备份/整批回滚/审计/撤销**沿既有链路**（受影响文件集含该 `.md` + 其 sidecar + manifest + pending）。**⑥ 预览与落地同一函数**：`preview_plan` 对每个改正文 op 跑**同一个** `splice()` ⇒ 卡片上看到的就是要写下去的那几行；diff 行沿用既有形状并允许**一边为空**（删除给 `after: null`、新增给 `before: null`，`line` 是旧行空间里的位置），卡片渲染器（`plan-confirm.js`）同步支持"只来一边"。**⑦ 工具面**：`propose_write` 的说明补上三个 op 的字段与顺序规矩（模型这才知道怎么写正文）。**⑧ 验收**：新增 `tests/test_agent_body_edit.py`（**20 例**：`splice` 的字节保真 / CRLF / 无末尾换行 / 多条倒序 / 六类拒绝码 / 计划级校验与 `expect` 回显 / 顺序拒与顺序放行 / 落地只改那一行且撤销逐字节 / 编译后文件被改 ⇒ `stale_write` / 工具说明含三 op / RPC 往返：`preview → apply → undo` 回到原样）⇒ `pytest -q` **474 passed**（另更新 1 处旧断言：`op_not_in_m3a` 警告码改名为 `op_not_compiled`，因为它现在说的是"未实现编译器分支"而不是"不在 M3a 首批"）；`node --check` 通过。**⑨ 未做（如实）**：`set_kp_range` / `rename_kp` / `merge_kp`、文件级（新建 / 删除 / 重命名 / 移动）、**块级重建**（表格 / 代码块 / 公式，§7 的 `kb.block.upsert`）、frontmatter 写入（§7 的 2.7）全部仍在后续批次；**端到端（模型真调工具改正文）未跑**（需配端点实测）；经 `save_document` 写 ⇒ frontmatter 按既有行为**重新序列化**（与人类编辑器同一路径），正文其余行逐字节不变。登记 `conventions/docs-management.md §4.2`；`docs/todo.md` 未编辑（另一写者并发重写中） |
| 2026-09-21 | **计划卡改「一份计划一张卡、状态原地推进」**（修我上一轮留的坑）+ 登记 **§4 Q7（静默写入 / 按 agent 配权限）** | 人提三点。**① 「agent 思考里那段时间上下文是上游设计吗」——是**：`TIME_CONTEXT_SECTION`（`prompt.py:295-320`）是 `dsh-time-context` 的**语义移植**（静态策略段进 system、动态读数 `render_time_context()` 追加在**本轮请求末尾**、不落盘；门控对应上游"插件被挂载"故**无条件注入**，与 `FILE_REFERENCE_SECTION` 按工具门控不同；三态时区收敛为一条固定说明，见模块 docstring 第 4 条）；模型那段"要不要提 pending.json 的时间"就是它在按该段第 1 条规则把 UTC 换算成本地时间后自我判断——**思考通道**本身也是移植（AG08 的 reasoning delta）。**② 「计划确认完之后还显示，没有自动消失」——是我留的坑，已改**：上一轮我让卡片"追加不替换"，于是点完「应用」后**旧预览卡仍留在对话里、且它的「应用」按钮还能点**（重复点会被 `base_versions` 拦成 `stale_write`，但界面不该留这种坑）。现改为**一份计划一张卡、状态在原地推进**：`mount()` 追加新卡仅用于**新计划**，`apply` / `undo` / `repreview` 一律 `renderInto()` 原地换内容（预览 → 「已写入 … + 撤销这一批」→「已撤销」）⇒ 确认后旧的勾选框与「应用」按钮随之消失，对话里不堆卡；新计划（模型再提一批）才追加新卡。**③ 「在良好临时备份下允许一定静默写入 / 用户在设置里按 agent 配权限」——按人明说"以后设计讨论"，本轮只登记为 §4 的 Q7**（不拍板）：写明现状（写一律经确认卡）、人提的方向（临时备份到位 + 可申请权限 + 设置里按 agent/技能配权限档）、更保守备选，以及**前置依赖**（per-agent 配置面属 §2.1 暂缓的 `config`；Q4 覆写口径）与硬红线（**"静默"只能省掉"点确认"，不能省掉写前 pre-image、备份失败即不写、盘上版本权威、撤销可用、审计可见**）。**验收**：浏览器实渲染实测——同一张卡走完三态，`#agent-messages .-agent-plan-msg` **始终为 1**（预览 → 应用后 → 撤销后），应用按钮 1→0、撤销按钮 0→1→0、勾选框 1→0，摘要原地变为「已完成 1 项操作（txid …）」/「该批次已撤销…」，`plan-confirm.js` **零报错**；`tests/test_agent_plan_rpc.py` 补 3 条接线不变量（`renderInto` / 原地 `showResult(res, plan, root)` / 原地撤销）⇒ **21 passed**；`node --check` 通过。登记 `conventions/docs-management.md §4.2`；`docs/todo.md` 未编辑（另一写者并发重写中） |
| 2026-09-21 | **W 线第三步：文件级 op（`create_file` / `rename_file`）+ 新模块 `file_ops.py`；`delete_file` 按设计暂不提供** | 人报「agent 只会创建跳转、不会建知识点**等**，也不会非 markdown 文件相关操作」—— 查证：`upsert_kp` 是**已实现**的（M3a 首批；真机日志显示模型提过、被"过期行号"整批拒，见 §5 上一行），**确实缺的是文件级与更广的写入面**。**① 新模块** `src/memoria/services/agent/file_ops.py`：`rename_plan()` —— 重命名的**只读预演**，用**与 `DocumentService.rename_file()` 同一批原子函数**（`build_kp_index()` / `replace_link_id_in_markdown()` / `migrate_sidecar_kp_refs()`）算出"这次改名会牵动哪些文件"，这份清单**同时就是 apply 的 pre-image 备份集**（否则级联改写过的正文/侧车在撤销时还原不回来 —— 这是本轮最要紧的一条）；`apply_create_file()` / `apply_rename_file()` 只做薄包装，落盘仍走服务层。**② 两条 op（只增不改）**：`create_file`（`file` + 可选 `body`；文件**必须不存在**，父目录自动建）、`rename_file`（`file` + `new_name`；只给文件名、保持目录，会**全库级联改写** `[[stem]]`）。原有 8 个 op → 10 个。**③ 两条新硬规矩**：㈠ **文件级 op 必须单独成一批**（`file_op_alone`）—— 它改路径或全库引用，同批别的 op 的行号与"引用能否解析"都可能被牵动，与其替模型猜顺序不如让它分两批（要写初始正文就用 `create_file.body`）；㈡ 重命名的备份集**含级联文件 + 图片注册表**（后者 `_update_registry_for_doc()` 会碰，存在才加）。**④ 预览**：`create_file` 把初始正文按"全新增行"渲染（卡片上直接看到 `+ 行`）；`rename_file` 给 `from → to` 与**"引用将改写：<文件>"**逐条列出（`entry.rename` / `entry.cascade` 也给结构化字段，供后续 UI）。**⑤ `delete_file` 本轮不给**（设计 §2.6 把它标成**默认关**、开关属 §2.1 暂缓的 `config` 面，§7 2.5 也写明"不建议设 `auto`"）⇒ 不在 `KNOWN_OPS` 里，模型提了即 `unknown_op` 拒；等权限档（§4 Q7）定案再开。**⑥ 验收**：新增 `tests/test_agent_file_ops.py`（**12 例**：新建往返 + 撤销后文件消失、预览列初始正文、已存在即拒、重命名**级联清单**与"备份集含级联文件"、落地后 `[[a]]` 真的被改写成 `[[注意力|a]]` 且撤销逐字节还原、三类拒（同名/目标占用/带路径）、`file_op_alone`、`delete_file` 不在表内且零写入、工具说明含两条 op、RPC 往返）⇒ `pytest -q` **486 passed**；`py_compile` 全过。**⑦ 未做**：块级重建（`kb.block.upsert`：代码块/表格/公式/mermaid）与图片（`kb.image.*`，§7 4.x）按人选定顺序排后。登记 `conventions/docs-management.md §4.2`；`docs/todo.md` 未编辑（另一写者并发重写中） |
| 2026-09-21 | **W 线第四步：块级重建 `upsert_block`（代码块 / mermaid / 公式）—— 不新增落盘原语** | 人选定顺序的第二刀：§7 的 5.1（代码块）/ 5.3（公式块）/ 5.5（mermaid）都标着「需要**整块原语** `kb.block.upsert`」。**本轮实现方式（有意偏差，登记）**：**不新增落盘原语** —— `body_edit` 里加一组**语义层**函数（`block_bounds()` 核"这段区间是否**恰好**是一个 `kind` 类围栏块"、`rebuild_block()` 把 `content` 重建成**含围栏的整块文本**），然后交给**同一个** `splice()`（`replace`）落地 ⇒ op `upsert_block` 编出来仍是 `edit_body` ⇒ 备份 / 整批回滚 / 审计 / 撤销 / 预览一致性**一处都不用另写**（这正是设计里"块级原语"想要的效果，只是没有多开一条落盘路径）。**① op 契约**：`file` / `kind`=`code`|`mermaid`|`math` / `range`（**恰好**覆盖开围栏→内容→闭围栏，1 起含端点）/ `expect`（整块逐字原文，内容级门槛同 `anchor_text`）/ `content`（**不含围栏**）/ 可选 `lang`（缺省**沿用原语言**，改了才覆盖）。围栏由程序重建 ⇒ 模型不必也不该手写 ```。**② 三条新校验**：`unsupported_kind`（**表格本轮不做**：§7 5.2 写明"人 UI 也还不能写表格、先补人 UI"，给 agent 单开一条人改不动的路径只会制造"坏了没法修"的死角）/ `block_kind_mismatch`（声明 mermaid 但围栏是 python 这类）/ `block_not_fenced`（区间没严丝合缝对上块边界 —— **不猜**，多一行少一行都拒）；另有 `content_breaks_fence`：`content` 里出现会**提前收尾**的行（``` / `$$`）即拒（那种内容会静默改变文档结构，不替它转义）。**③ 顺序/合并规则沿用**：`upsert_block` 归入 `BODY_EDIT_OPS` ⇒ 受"改正文必须排在按行号锚定的 op 之后"约束、与其它改正文合并进**同一次** `edit_body`（内部从大到小）、预览给出整块的逐行 before/after（并额外给 `entry.block={kind,lang}`）。**④ 验收**：`tests/test_agent_body_edit.py` **+9 例**（代码块重建 + 围栏不多不少 + 撤销逐字节回原、公式块、mermaid（语言标记由程序保留）、六类拒绝码参数化、编译产物仍是 `edit_body`）⇒ `pytest -q` **495 passed**；`py_compile` 全过。**⑤ 未做**：表格（理由见上）、图片 op（`kb.image.*`，§7 4.x，人选定顺序第三刀）。登记 `conventions/docs-management.md §4.2`；`docs/todo.md` 未编辑（另一写者并发重写中） |
| 2026-09-21 | **W 线第五步：图片引用 op `insert_image_ref`（§7 的 4.1）—— 入库/改属性/删除三件事有意不做** | 人选定顺序的第三刀（图片）。**只做"往正文插一行图片引用"**：`file` / `after` / `expect`（同行级原语口径）/ `path`（**库内相对**图片路径，通常 `.memoria/images/x.png`，必须已存在）/ 可选 `alt` / 可选 `attrs`（显示属性，写 title 位）。行由 `file_ops.image_ref_line()` 拼（**照 `preview-formats.md` §4.2/§4.3 的既有语法**：知识库根相对路径、含空格/中文用尖括号 `<…>`、属性在 title 位），再交给 `body_edit` 的 `insert` 落地 ⇒ 又一条"只多一层语义校验、落盘复用既有原语"的 op（编出来仍是 `edit_body`）。**三条有意不做（含理由，请人复核）**：① **图片入库**（`import_image`）—— 目标文件名要**先去重**才知道（重名自动加序号），而 pre-image 备份必须在**写前**拿到目标路径 ⇒ 现有备份集机制装不下它，需要一个设计决定（例如"入库按目录级备份"或"入库不进 plan、仍由人 UI 做"）；② **改 alt / 改属性** —— 属性语法（`"k=v,…"` 键级合并、保序、保留未知键）的**权威实现是人 UI 的 `image-tools.js`**，后端没有同一份；在拿到"同一套实现"之前重写一份必然漂移（§2.3.4 的硬不变量）；③ **移动 / 删除图片** —— §7 4.5 属后线（要同步全部引用正文）、4.6 删除类默认关。**验收**：`tests/test_agent_file_ops.py` **+6 例**（插一行 + 尖括号 + 属性 + 预览 diff + 撤销逐字节回原、五类路径拒绝：`../` / 绝对路径 / 不存在 / 非图片扩展名 / 空）⇒ `pytest -q` **501 passed**。登记 `conventions/docs-management.md §4.2`；`docs/todo.md` 未编辑（另一写者并发重写中） |
| 2026-09-21 | **事故修复：写路径全废（`TOOL_FAILED —— unexpected indent (body_edit.py, line 1)`）+ 连带查出两处真实缺陷 + 加「源码完整性」机械守卫** | 人报"写入工具仍然是坏的，四次提议全废（含只建 KP 的一次）"⇒ 我不接受"已完成"，逐层取证。**① 硬故障（真因）**：`body_edit.py` **磁盘上第 1 行多了一个前导空格** ⇒ `IndentationError: unexpected indent` —— 写路径里所有 `edit_body` 调用在**导入**这一步就炸，工具层又把它包成一句与参数无关的 `TOOL_FAILED`（调用方完全看不出是代码坏了）。已去掉空格；**根因未能定位**（`git show HEAD:` 的 blob 是干净的、工作区是脏的、且磁盘 mtime **早于**提交时刻 ⇒ 疑似"保 mtime 的写入/拷贝"，但**查不出是谁写的**，如实登记）。**② 机械守卫（防复发）**：新增 `tests/test_source_integrity.py` —— 遍历 `src/**/*.py` 逐文件 `compile()`（= 解释器真正加载时做的事）+ 断言**首行不许有前导空白 / 文件不许带 BOM**（正是本次事故的形态）；这条守卫以后让此类故障在**测试期**就红，而不是等用户撞上。**③ 连带缺陷 A（下一步必然撞上）**：`_check_expect()` 用 `.strip("\n")` 归一 `expect` ⇒ **"正文第 1 行是空行"的文件永远匹配不上**（用户 `trial.md` 正文正是 `"\n相关"`，`expect: "\n相关"` 被判 `expect_mismatch`，回显看着一模一样）⇒ 改为**只容忍"结尾多一个换行"**，开头的空行必须显式写出；同时补一条：**编辑到 EOF 时 `text` 显式以换行结尾 ⇒ 补上收尾换行**（原来会把这一个换行吞掉，"全选粘贴"型写入会丢结尾换行）。**④ 连带缺陷 B**：`_rel()` 只对根做了 `realpath`、目标没有 ⇒ Windows **8.3 短名**（本机 `%TEMP%` 就是 `C:\Users\LAMTIM~1\…`）下 `relpath` 算出 `../../../../…` ⇒ apply 判 `path_rejected`（**正文那批整批拒**）；改为**两侧都 `realpath`**，并补"受影响文件集必须是库内相对路径"的不变量测试。**⑤ 工具层可读性**：`TOOL_FAILED` 文本补上异常类名（`执行失败（IndentationError）—— …`），让"代码故障 vs 参数故障"一眼可分（等量替换，`registry.py` 锚点零漂移）。**⑥ 验收**（用**用户给的那两批内容**在临时库跑真实落地路径）：正文批（`replace_lines` 把 `"\n相关"` 换成 7 行两节 + 互挂跳转）**ok**、KP 批（`upsert_kp` ×2）**ok**、sidecar 里两节的 `snippet`/`line_hint` 与用户写的一致（`## 天气`/1、`相关：[[mcdonalds|麦当劳]]`/3、`## 麦当劳`/5、`相关：[[weather|天气]]`/7）；**逐个事务撤销**后文件**逐字节**回到 `b'\n相关'`、新建的 sidecar 也被撤掉。`pytest -q` **743 passed**（含新守卫的参数化 239 条 + 4 条回归：`expect` 首行空行可匹配 / 只容忍结尾换行 / EOF 换行、受影响文件集不越界）。**⑦ 如实**：用户的 `trial.md` 我**全程只读、没动过**；那两批内容是在**临时库**里验的；①的写入者**未定位**（只能靠守卫兜住）。登记 `conventions/docs-management.md §4.2`；`docs/todo.md` 未编辑（另一写者并发重写中） |
| 2026-09-21 | **真机诊断：`agent 只建跳转、不建 KP`与`跳转没配链接`是**同一个原因**（KP 批从未落地）+ 修「分批断在半路」的机制缺口 + 登记 §4 Q8** | 人报两条症状，逐层取证后结论如下。**① 事实**（读用户库与会话日志）：`ui-plan.jsonl` 里**只有 4 条 apply** —— 3 次 `edit_body` 成功 + 1 次 `stale_write` 拒，**从来没有 `upsert_kp`**；库里**没有 `.memoria/sidecars/` 目录**；`trial.md` 已经是那 7 行（正文批确实落了）。会话日志显示：含 2 个 `upsert_kp` 的那一批、以及另外三次正文提议，**全部撞在同一个 `IndentationError`** 上（上一行已修）；模型最后一轮明确写着"KP 元数据待正文落定后再建"，然后就**停了**（它没有下一轮的入口）。**② 结论一：两条症状同因** —— KP（sidecar）从未写入。而正文里的 `[[mcdonalds|麦当劳]]` **是符合库规范的写法**：`resources/agent-prompts/kb-spec.zh-CN.md` §4 明确"**悬空链接**（指向尚不存在的 id）保留为**虚链**、`links[]` 可由产品「构建」从正文同步"，§1 明确"**KP 只认 sidecar**：只改 md 不改 sidecar，产品里看不到 KP" ⇒ 所谓"跳转内没有配置链接"= 目标 KP id 还不存在（虚链），不是链接语法错。**③ 结论二（本轮新发现的机制缺口）**：确认卡是**用户手动点**的，点「应用」**不会**开启 agent 的下一轮 ⇒ "先改正文、再按新行号建 KP"这类**天然分两批**的写入**必然停在半路**，而 agent 自己以为说清楚了、用户以为它没做完。**④ 本轮落地（缓解，两条）**：㈠ 工具回文与写侧提示词都加上"**若这批只是第一步，请在回答里明确请用户「应用后回复继续」** —— 应用卡片不会自动开启你的下一轮"（同一句话两处，模型每轮都能看到）；㈡ 确认卡把**当前对话的 session id** 一并带给 `agent_plan_apply`（`drainProposals(res.session_id)` → `open(plan,{sessionId})` → `apply(..., sessionId, ...)`）⇒ 写入审计落在**那次对话**里（此前一律落 `ui-plan` 伪会话，模型与人都看不出"这批是谁提的、谁确认的"）。**⑤ 登记待讨论**：真正让 agent**无需用户催促就续批**需要把"应用结果"变成**会话可见事件**（回灌），这动到会话契约（事实源）⇒ 记入 **§4 Q8**（三个选项 + 前置），**不擅自改**。**⑥ 验收**：`pytest -q` **743 passed**（接线不变量 +2：`drainProposals(res.session_id)` / apply 带 session id；工具文本 +1：必须含"回复「继续」"）；`node --check` 通过。登记 `conventions/docs-management.md §4.2`；`docs/todo.md` 未编辑（另一写者并发重写中） |
| 2026-09-21 | **写机制全线放开：删掉两条"为审批而拆批"的顺序限制，改为「批内按 op 顺序视图推进」；审批降级为后续运行模式（口径已追加进 §4 Q7）** | 人报「因为审计机制，创建知识点加挂链竟然要分两步操作」⇒ 查证：**卡人的不是审批，是校验器只认"盘上原文"** ——① `file_op_alone`（`create_file` / `rename_file` 必须单独成批）② `body_edit_order`（同一文件的"改正文"必须排在按行号锚定的 op **之后**）+ 同文件多条改正文**区间不得重叠**。三条合起来使"**先写正文 → 再按新行号建点 → 再挂链**"这类天然有先后的意图**只能拆批**，而确认卡是人工点的 ⇒ 必然断在半路（上一行刚诊断出这个缺口）。**① 新机制 `plan._View`（批内顺序视图）**：`validate_plan()` 与 `preview_plan()` 共用同一个 `_View`，**按 op 顺序**推进"这一刻的正文行 + 文件存在性"；每个 op 面对的是**前序 op 生效之后**的正文。校验通过后立刻把该 `edit` 经**同一个 `splice()`** 施加到视图（`put_edit`）；`create_file` 用初始正文 `seed()`（其 **stem 同时进入 `pending_targets`** ⇒ 同批 `attach_links` 指向新文件也能解析）。**② `compile_plan()` 对齐同一顺序**：改正文不再"攒到末尾按文件合成一次"，而是**一 op 一次 `edit_body`、就地插在原位置**（每条编辑面对的都是前序 op 落地后的盘上正文）⇒ 校验期算出的行号与逐 op 落地看到的行号是**同一套语义**（预览 = 落地）。**③ 两条限制删除、一条保留**：`file_op_alone` **删除**（`create_file` 可同批）；`body_edit_order` 与"区间不得重叠"**删除**（按序施加时重叠有定义：第二条面向第一条的结果）；**只留 `rename_must_be_last`** —— `rename_file` 必须收尾（它重写全库 `[[旧名]]` 引用并搬路径，之后再有 op 会让"路径 / 引用"同时活在两套坐标系里；这是**语义**约束，不是审批约束）。**④ 行为变更（如实登记）**：批内行号语义由"**批首快照**"改为"**逐 op 当前视图**" —— 旧写法"先插一段、再按**旧**行号删一行"现在会被 `expect_mismatch` 拒（`test_apply_delete_and_insert_lines` 已按新语义调换 op 顺序，另新增 `test_apply_uses_the_batch_view_line_numbers` 正面钉住新语义、`test_anchor_op_with_the_old_line_number_is_rejected` 钉住"不猜"、`test_body_edit_then_anchor_op_uses_the_new_line_numbers` 钉住"先改正文后挂链"）。**⑤ 文案同步**：`propose_write` 的工具描述与回文、`prompt.py` 的 `_WRITE_HINT_WRITE` 一律改成"**一批里按顺序把有先后的动作写全**"（并举出两个典型顺序），旧句"改正文必须排在锚定 op 之后 / 文件级必须单独成批"删除。**⑥ 兜底（人要求"像 git commit 一样"）**：本轮**未新增**机制 —— 逐条核对既有链路已覆盖放开后的全部形态：写前 pre-image 快照（新建文件记 `existed:false`，撤销 = 删除）→ post 哈希 → 撤销逐字节还原 + 外部改动保护 fail-closed + 每批 txid 与卡片上的「撤销这一批」；且 `affected_files()` 早已把 **sidecar + manifest + pending** 算进备份集。新测试实测"**新建 + 建点 + 挂链**"这一批**整批撤销后逐文件回到写前**（新文件与其侧车被删、`a.md` 的侧车与 manifest / pending 条目一并还原）。**⑦ 验收**：`pytest -q` **746 passed**（`test_agent_body_edit.py` 三改二增、`test_agent_file_ops.py` 一改二增 + 一处工具说明断言改口径）；`py_compile` 全过。**⑧ 未做（如实）**：`rename_file` 的批内级联**没有**做视图模拟（因此保留"收尾"限制）；同一批里"先 `attach_links` 再 `detach_links` 同一锚"仍按**盘上** sidecar 校验（视图不建模 `links`，行为未变）；审批档位（`manual` / `auto` / `custom` / `all access`）**本轮不实现、只登记**（见 §4 Q7）；`docs/todo.md` 未编辑（另一写者并发重写中） |
| 2026-09-21 | **人报「你没有放开写啊，agent 还是给我审计面板让我确认写入」⇒ 纠正上一轮的误读：真正要放开的是「人工闸门」，本轮把 `propose_write` 改成调用内直接落盘** | 上一轮我把"审计机制"读成了"为审批而拆批"，只删掉顺序限制 —— **闸门还在**（工具只产 plan、落盘要人点「应用」），所以用户看到的仍是确认卡。本轮按原话落地："搁置审计的设计，全线放开写机制；审计作为以后的运行模式之一（`manual approval` / `auto approval` / `custom` / `all access`），兜底靠像 git commit 一样的备份 + 撤回"。**① 审批默认策略全线放行**（`approvals.DefaultApprovalPolicy`）：读类 / 写类都 `allowed-once`（旧行为是 `read_only=False` 一律拒）；更严的档位改为**注入式**（`AskPolicy` 逐条确认 / `NeverPolicy` 确定性拒绝），模块 docstring 同步改写。**② `propose_write` 改成"调用内写"**：`validate → preview → apply_plan` 三步都在**同一次工具调用**里完成；工具声明改为 `read_only=False`（它确实改库）；回文从"已提议（尚未写入）"改为"**已写入** N 项（txid …）"，失败则回 `WRITE_FAILED`（附 `stale_write` 的"重读后重提整批"指引）。写入**不在** `kb_read_only()` 内（那层专门屏蔽库内落盘）。**③ 会话归属与缓存正确性**：`build_kb_tools(kb_path, *, session_id, service)` 新增两个可选参数 —— 前者决定审计落**哪次对话**（缺省 `agent-auto`），后者是**应用侧已装载的 `DocumentService`**（复用实例 ⇒ 写后 `recover_after_write()` 清它的解析缓存，否则面板重开文件读到的还是旧解析）。链路打通：`ui.agent_ask_start` → `ask_stream.start(service=…)` → `AskJob.service` → `ask(service=…)` → 工具集；`ask()` 里会话创建**提前到工具集构建之前**（工具需要它的 id）。**④ 前端：确认卡 → 回执卡**：`agent_plan_pending` 改回 `{records:[{plan,preview,result,session_id}], plans:[…]}`（`plans` 为旧调用方保留）；`plan-confirm.js` 新增 `openReceipt()` —— 摊开同一套 diff 后**原地换成**「已写入 …（txid）」+「撤销这一批」，**没有勾选、没有「应用」**；卡片角色标签按状态取「已写入」（`roleLabel`），成功时另加一行 `plan.autoNote`（中英成对）说明"由智能体直接写入、可撤销"；回执落地时若人正开着被写的文件则 `refreshOpenDoc()` 立刻刷新。**⑤ 提示词**：`_CAPABILITY_WRITE` / `_WRITE_HINT_WRITE` 改为"你可以**直接写入**…一次调用即落盘；**以工具回文为准**"，删掉"你没有落盘权 / 须用户确认"。**⑥ 审计标注**：`apply_plan(auto=False)` 新增该可选参数，`stale_write` / 备份失败 / 原语失败 / 成功四条返回都带 `auto: true`（payload 只增），审计里能一眼分出"自动写入"。**⑦ 验收**：`pytest -q` **747 passed**（9 处旧断言按新语义改写：工具声明、默认策略、回文、RPC 返回形状、能力自述；**新增 1 例端到端** —— 真注册表 + 真默认策略 + `FakeProvider`：模型一次 `propose_write` ⇒ 侧车**当场落盘**且 `capability/apply` 落在指定会话）；`node --check` 3/3；`scripts/i18n_selftest.js` 全 PASS。**⑧ 如实交代（本轮取舍与缺口）**：㈠ 人工闸门去掉后，"人正在编辑时 agent 抢先写"只剩**版本哨兵**兜底 —— 人保存时命中 `stale_write` 并弹「重载 / 以我为准（先备份再覆盖）/ 稍后」，**数据不会静默丢**，但"覆盖发生"本身不再被事前拦住（此前手动 apply 有 `waitForIdleEdits` + 忙位；自动写入发生在后端工作线程，前端拦不到）——是否补"编辑中则 agent 暂缓写"待人定；㈡ `manual approval` / `custom` 档**未实现**（只给了可注入的 `AskPolicy` / `NeverPolicy` 原语）；㈢ 前端仅做**代码级**验证（`node --check` + 接线不变量测试），**未跑 L4 实渲染**；㈣ `delete_file` 仍不在 op 表（默认关）；㈤ `agent_plan_apply` RPC 与"计划"手动入口**保留**（调试 / 未来手动档可用）。登记 `conventions/docs-management.md §4.2`；`docs/todo.md` 未编辑（另一写者并发重写中） |
| 2026-09-21 | **撤销改为「对话粒度」：一次对话只留一个撤回点**（人：「撤销还保留着之前审计的分批设计，现在以对话为粒度，每次对话保留一次撤回就 ok」） | **① 数据面**：新增 `backup.restore_session(kb_path, session_id)` —— 列该会话全部批次、**逆序**逐批 `restore_batch(audit=False)` ⇒ 等于回到"这次对话开始前"。逆序是**必须**的：每批撤销都要过"当前 sha256 == 该批 `post.json` 的**写后**哈希"这道外部改动保护，只有从最新一批往回撤，盘面才正好等于那一批的 post 状态（较新批的 pre 状态正是较旧批的 post）。任一批被拒 ⇒ **停在那批**并如实回报 `undone` / `stopped_at`，不继续、不掩盖、不静默覆盖。全部成功记**一条**汇总 `capability/undo`（不再每批一条碎片）。**② RPC**：`agent_plan_undo` 省略 `txid` ⇒ 走 `restore_session`（撤整个会话）；给了 `txid` ⇒ 只撤那一批（细粒度入口保留给调试与将来的手动档）。**③ 前端**：撤销按钮不再往下传 `txid`（原来是"每批一个按钮"，即分批时代遗留）；文案改「撤销这次对话的改动」；新增 `markSiblingsUndone()` —— 撤销成功后把**同一会话**的其它回执卡也标成「已撤销」，界面不再自相矛盾；已撤销文案换成带批次数的 `plan.undoneN`，回执卡的 `plan.autoNote` 同步改口径（中英成对）。**④ 验收**：`pytest -q` **750 passed**（新增 3 例：backup 层"两批逆序全撤回到最初 + 只有一条 undo 审计"、backup 层"任一批被拒即停且**不覆盖人的改动**"、RPC 层"一次对话两批 ⇒ 一次撤销全还原，再撤明确报错"；`plan-confirm.js` 接线不变量 +4：不传 `txid` / `markSiblingsUndone` / `openReceipt` / `roleLabel`）；`node --check` 通过；`scripts/i18n_selftest.js` 全 PASS。**⑤ 如实交代**：撤销**不删备份批次**（既有纪律：每会话只留最新 5 批、FIFO）⇒ 一次对话若写了 6 批以上，被淘汰的最早那批撤不回来（能撤到保留边界为止）；重复点撤销会命中 `external_change`（盘面已不等于该批 post 哈希），前端靠"按钮消失 + 同会话卡全标已撤销"避免；`manual` / `custom` 档仍未实现。`docs/todo.md` 未编辑（另一写者并发重写中） |
| 2026-09-21 | **撤销重做：整会话撤销由「逐批逆序回放」改为「还原到会话起点」**（人：「你的设计根本和 git 的机制不同」） | **① 人的批评成立**：旧实现逐批逆序 `restore_batch`，每批都要过"当前盘面 == 该批 `post.json` 写后哈希"—— **中间任何一批之后被别的写者改过**（产品「构建」同步 sidecar 就是这种情况），**整条链就卡在那儿**；人真机撞到"普通撤销被拒 → 强制撤销也不成功"（错误是 `撤到批次 … 时被拒`，说明那次 `force` 根本没走到）。**② 新语义（与 git 的 `revert`/`checkout` 同构：目标是**状态**，不是回放路径）**：`restore_session()` 重写为三步 —— ㈠ **算起点**：升序遍历会话批次，取每个文件**最早**出现的那份 pre-image（= 对话开始前那一版）；㈡ **判一次**：外部改动保护只比**一遍**（拿"会话内该文件**最后一次**写后的哈希"对当前盘面），不匹配的列进 `stale`；㈢ **一次写回**：按起点版本写回（起点 `existed:false` ⇒ 删除）。⇒ 不再"撤到一半停住"，也不再受中间批次影响；返回带 `origin`（每个文件取自哪一批，便于人工核对）。**③ 被拒也留痕**：`external_change` 分支现在也记 `capability/undo` —— 真机教训是人点了两次撤销而**事后无迹可查**（与 apply 的 `stale_write` 留痕同口径，§2.6）。**④ 与既有设计的关系**：[agent-capabilities.md](./agent-capabilities.md) §2.3.2 第 5 条写的是"整会话 = 会话内全部批次**倒序**"（该文已声明**不再是事实源**）⇒ **以本行修订为准**；**单批撤销**（`restore_batch()` + 前端「只撤这一批」）**保持原语义**不变。**⑤ 验收**：`pytest -q` **754 passed**（`tests/test_agent_backup.py` 两例改写：「两批 ⇒ 一次回到起点，`origin` 指向最早那批」「被拒 ⇒ 零写入且**留痕** ⇒ force 一次到位 ⇒ 兜底那版还能原样拿回」）。**⑥ 如实**：本轮**未**替人执行任何撤销（改他的库必须由他确认）；`agent-panel`/`plan-confirm` 的前端**需要完全重启应用**才会加载到新按钮（人前一次可能就是用旧前端点的） |
| 2026-09-21 | **撤销机制 + 对话栏 UI 一并重做**（人：「仍然撤销失败，整个撤销机制以及 ui 位置都得重写」） | **① 撤销改成"回到对话起点"**（与 git 的 `revert` 同构：**指向一个状态**，不是回放路径）：`apply_plan()` 在**第一次写入前**把那一版固化进 `<session>/origin/`（`ensure_origin()`）的**起点快照**，它**不参与批次 FIFO 淘汰** ⇒ 撤销永远回到"这次对话开始前"，不再受"只留 5 批"限制、也不再逐批过 `post.json` 校验。`restore_session()` 重写为三步：**读起点 → 自动兜底 → 一次写回**。**② 不再有"被阻止"**（人：「一键生效」）：撤销时**自动**把将被覆盖/删除的**当前版本**另存为 `manual-force` 批次（返回 `safety_backup`）⇒ 覆盖 ≠ 丢数据，因此**删掉** `external_change` 拒绝路径；`force` 参数随之取消（`agent_plan_undo(kb, session_id, txid)`，`txid` 仅留调试）。**③ 旧对话如实**：没有起点快照（旧版本写的对话）⇒ 回 `no_origin` 并说明，不假装成功。**④ UI 按人定稿重排**：㊀ **助手消息去掉气泡外观**（无边框 / 无底色），用户消息保留浅底；㊁ 每条消息外面包一层 `.-agent-turn`，**复制按钮移到气泡外的操作行**；㊂ **只保留一个「撤销」**（删掉「只撤这一批」「强制撤销」，**不做 redo**），且**只挂在最后一条助手消息下方**；㊃ 写入明细改为**可展开的状态条**（`.-agent-write`：一行"已修改 N 个文件" + ▾，展开列出文件与变更行数）—— 不再单独插回执卡。前端仍用**包装**手法（`pushMessage` / `renderMessages` / `finalizeMessage` 重新赋值 + 新门面 `MemoriaAgentPanel.setWriteState()`）⇒ `agent-panel.js` 上方锚点**零漂移**。**⑤ 验收**：`pytest -q` **755 passed**；`node --check` 4/4；`scripts/i18n_selftest.js` 全 PASS；**L4 实渲染验证**（临时演示页 + 本地静态服务 + 浏览器实测）：**9 项断言 8 项符合** —— 助手 `border:0px` / 背景 `rgba(0,0,0,0)` ✅、操作行 `parent = .-agent-turn`（确在气泡**外面**）✅、两个「复制」都在 assistant ✅、`.-agent-write` 唯一且位于**最后一个** assistant turn ✅、点击后展开出 2 个文件行且 `aria-expanded=true` ✅、三个 turn 纵向无重叠 + 无横向溢出 ✅、控制台 **0 报错** ✅；唯一不符项是"用户消息浅底"—— **演示页没加载 `theme/*.css`**，`--accent-soft` 计算值为空 ⇒ 属演示页环境问题（真实应用加载主题 CSS），**非代码缺陷**。**⑥ 如实交代**：起点快照只在**新版本**写入时产生 ⇒ **人现有的旧对话（含 boonie-bears 那些批次）没有起点**，撤销会明确回 `no_origin`（要么继续用逐批撤，要么让 agent 重新写一次以生成起点）；`pending.json` 这类"产品随时会重写"的文件现在会被**无阻碍地覆盖**（但有 `manual-force` 兜底）。 |
| 2026-09-21 | **修复：撤销按钮 / 写入状态条**完全不出现**（`setWriteState` 注入方式错误）** | 人报「agent 回话下面只有复制没有撤销，而且最后一次对话下方什么都没有」⇒ **先取证**：后端**是新的**（`<session>/origin/` 起点快照已生成、14:33 / 14:44 两批写入都在）⇒ 问题在前端。**根因**：`agent-panel.js` 是 `window.MemoriaAgentPanel = (function () { … })()` 形式，而我上一轮把接口注入写成了 **IIFE 内部的 `Object.assign(…)`** —— 执行那一刻全局名**还没被赋值** ⇒ 只创建了一个**临时对象**，随后被 IIFE 的 `return {...}` **覆盖** ⇒ 方法丢失；`plan-confirm.js` 那侧 `?.setWriteState?.()` 于是静默 no-op（**无任何报错**）。**修复**：把 `setWriteState` 并入 IIFE 的 `return {...}`（与 `init` / `open` / `clear` 并列），删掉那行 `Object.assign`；并给 `publishWriteState()` 加"接口不可用"的 **`console.warn`**（曾经静默失过一次，这类失败必须可见）。**加守卫**：`test_message_actions_and_write_bar_wiring_is_intact` 断言 `setWriteState: setWriteState` 在 `return` 里、且源码中**不再出现** `Object.assign(window.MemoriaAgentPanel`。**验收**：`pytest -q` **755 passed**；`node --check` 通过；**L4 实渲染（真实 `app/index.html`，不是演示页）**：`Object.keys(MemoriaAgentPanel) = [init, open, onKbChanged, clear, setWriteState]`、`setWriteState` 实测 `"function"`、状态条**唯一**且挂在**最后一个** assistant turn（索引 2/2）、点击展开出 **2** 行文件（`animation/boonie-bears.md 3 行` 等）、点「撤销」回调计数 **0→1** 且返回 `false` 时按钮正确复位、状态条与相邻 `.-agent-msg-actions` 无重叠且不溢出容器、控制台 **0 error / 0 warn**、i18n 文案正常（`已修改 2 个文件` / `撤销`）。**教训（写给后来者）**：本仓前端模块大量采用 `window.X = (function(){…})()` 形式 ⇒ **给这类模块加接口只能改它的 `return`，不能在 IIFE 内部 `Object.assign` 到那个全局名**。 |
| 2026-09-21 | **UI 第三版（人定稿）：顶栏重排 + 副标题行写入状态栏 + 复制图标** | 人：「当前会话的名称改成在 head 显示（纯会话名），历史按钮放这里，不用显示模型行名；history 这栏显示状态，而且改名不叫 history 叫 subhead」「只保留复制，但是改成复制的图标，鼠标悬浮显示复制」。**实现**：`restructureHeader()` 在页面加载（IIFE 末尾）**只搬 DOM**（会话名 `#agent-history-current` 与「历史」`#agent-history-open` → `.-agent-head`；`#agent-model-label` 与 `.-agent-history-label` 隐去；`.-agent-history` 补名 `-agent-subhead`）⇒ **`index.html` 零改动、锚点零漂移**；写入状态栏**从"消息下方"搬到 `.-agent-subhead`**（两态：纯 bar / 点击展开；三态文案：`无改动` → 不渲染 / `已修改 N 个文件` / `已撤销`），撤销**只在这里出现一次**；`markWriteUndone()`（也经 `return` 暴露）让撤完短显「已撤销」再 4s 收起；复制按钮改**内联 SVG 图标**（`-agent-act--icon`），悬停 `title` 就是「复制」，成功/失败改 `title` + 变色（不再改文字）。**验收**：`pytest -q` **755 passed**；`node --check` ×3、`i18n_selftest` 全 PASS；**L4 实渲染（真实 `app/index.html`）**：`.-agent-head` 子节点 = `[#agent-model-label(hidden), #agent-history-current, #agent-history-open]`、`.-agent-history` classList 含 `-agent-subhead`、状态栏 `.-agent-write-bar` 父节点即 `.-agent-subhead`、`已修改 2 个文件` 并可展开（`hidden` true↔false、2 行明细）、点撤销回调计数 0→1 且返回 false 时按钮复位、`markWriteUndone()` 后文案 = `已撤销` 且撤销按钮 `disabled`、head/subhead 上下相邻不重叠、无横向溢出、控制台 0 error（1 条为探针脚本自身）。**如实交代**：真实消息里的图标按钮**未在本轮实测**（该环境无后端 ⇒ 无法产生消息；同一位置的按钮在上一轮文字版已实测存在）。 |
| 2026-09-21 | **待办（人已定方向，待设计确认）：写入状态栏改「栈」+ undo/redo** | 人：「采用 stack 设计，这样可以 undo/redo，如果产生新对话，就在当前栈指针上新加，旧的栈顶可以不实际删，因为根据栈的设计可以改真栈顶的方法限制 redo 范围；状态栏有两种状态，一个是纯 bar 显示一个是点击后展开显示」。**关键约束**：现有备份只存**写前**镜像（`<txid>/files`）⇒ 撤销一步可行（`restore_batch`），但**重做一步需要"写后镜像"**（`<txid>/after/`），现在**没有** ⇒ 需给每批补一份写后副本，备份体积约翻倍。**建议方案**：① `after/` **只存内容事实源**（`*.md` + sidecar），`manifest.yaml` / `pending.json` 由产品重建（省一半体积）；② 栈指针落盘 `<session>/stack.json`（刷新后仍准）；③ 状态栏 bar 态显示 `栈 2/3` +「撤销一步」/「重做一步」，展开态显示栈轨迹（每步时间 / intent / 文件数，当前指针高亮，可点击跳转）；④ **撤销语义二选一**（待定）：主按钮 = 「撤销一步」（栈语义）还是 = 「回到对话开始前」（现语义，栈仅在展开态提供）。 |
| 2026-09-21 | **栈式 undo/redo 落地 + 写后镜像「分层存储」（人定稿「采用 stack 设计」「近邻不压缩，深的压缩」）** | **① 写后镜像**（重做一步的依据）：`backup.snapshot_after_images()` 在每批写入成功后按 `journal.json` 的文件集把**写完之后**的字节存进 `<txid>/after/<rel>`（`existed:false` 也记 ⇒ 重做 = 删除），元数据 `<txid>/after.json`；**fail-open**（写已成功、无法回滚 ⇒ 失败只记进 `warnings`：该步不可重做，撤销仍可用）。**② 分层**：`compact_after_images(keep_plain=2)` 把**离栈顶超过 2 步**的批次压成 `<txid>/after.zip`（`ZIP_DEFLATED`；幂等、已压的跳过、单批失败只跳过它），近邻保持**原始文件** ⇒ redo 零解压；`apply.py` 每次写入后调一次。**③ 会话栈** `<session>/stack.json`（`{txids, cursor}`）：`push_stack()` 新写入**从当前指针截断**旧 redo 分支（批次目录与备份**都不删**，只是不再可达 —— 即人说的"旧的栈顶可以不实际删"）；没有 `stack.json` 的旧对话由 `read_stack()` 按批次表**合成**（指针在最新一批）⇒ 老对话照样能一步步撤。**④ 一步撤销 / 重做**：`undo_step()` 用当前指针那批的 **pre-image**、`redo_step()` 用指针 +1 那批的**写后镜像**（无镜像 ⇒ 如实回 `no_after`，不假装成功）；两者与 `restore_session()` 同口径 —— 覆盖前**先自动兜底**（`manual-force` 批次）⇒ 覆盖 ≠ 丢数据、**不再有 `external_change` 拒绝**。**⑤ RPC**：`agent_plan_stack` / `agent_plan_undo_step` / `agent_plan_redo`（`ui.py` **末尾追加** + 运行期挂到 `UIAPI` ⇒ 既有 `ui.py:<行>` 锚点零漂移）。**⑥ UI**：状态栏 bar 态显示「已修改 N 个文件  栈 p/t」+「撤销一步 / 重做一步」（可用性由 `can_undo` / `can_redo` 决定），展开态显示**栈轨迹**（每步一行、当前指针高亮 `is-current`）+ 本步文件明细。验收：`pytest -q` **765 passed** —— `test_agent_backup.py` 新增 **11 例**钉住：分层（近邻原文件 / 深的 zip / 幂等）、压缩后仍可重做、`push_stack` 截断且不删备份、一步撤销与重做**逐字节互逆**、新建文件撤回=删除且重做可复原、栈两端 `stack_bottom` / `stack_top`、无镜像 `no_after`、`stack.json` **不会被当成批次**、旧对话合成栈、撤销前兜底可原样拿回；`node --check` 通过；`scripts/i18n_selftest.js` 全 PASS |
| 2026-09-21 | **修复：写入状态栏展开态纵向溢出，盖住 `.-agent-head` 与消息区（L4 实渲染测出）** | L4 实测（真实 `app/index.html`）：bar 高 **121px**，而宿主 `.-agent-subhead`（= `.-agent-history`）被 `--bar-h-b` 钉成 **34px** ⇒ bar 上下各溢出约 **45px**，盖住 `.-agent-head` 与 `.-agent-messages` 顶部（横向 0、页面级溢出 0）。**修法**：展开时由 JS 在宿主上打 `-agent-write-host--open`（收起时与每次重绘都移除），配套 CSS `.-agent-write-host--open { height: auto; align-items: flex-start; }`（同特异度、位于 `app.css:5160` 之后 ⇒ 覆盖固定行高）；消息区 `flex:1; min-height:0` 自然让出空间。**复验**：`barOverflowsHead=false`、`barOverflowsMessages=false`、`barInsideSubhead=true`、页面级 `overflowX/Y=0`；收起后 open 类移除、行高回 31px 基线，与上下区块首尾相接无重叠；撤销 / 重做各命中一次且按钮正确复位；无新增 console error。机械守卫：`test_agent_panel_token.py` 新增三条断言（JS 打类 / 重绘清类 / 配套 CSS 规则在场） |
| 2026-09-21 | **修复：「agent 没有做出行动、卡在思考」的真因 = 备份 rename 撞瞬时锁 + 工具失败在前端完全不可见** | 人报「AAA_Vocab 最后一次对话仍然是没有做出行动，卡在思考」。**真机取证**（`docs/example/AAA_Vocab/.memoria/agent/sessions/session-20260921T111525Z-ab1ac72c.jsonl`，会话时间 2026-09-22 00:26，184 行）：最后一轮（iteration 8）模型**既说了话也调了工具** —— `content` =「…我按本库词条模板写入…」+ `propose_write`（建 `vocab/plummet.md` 并补反向链接），但整批被拒：`capability/apply {status:"error", code:"backup_failed", message:"[WinError 5] 拒绝访问。: '…20260921T162629Z-01.tmp-42604' -> '…20260921T162629Z-01'"}` ⇒ 工具回 `WRITE_FAILED`，库内**一个字节没变**；随后 `loop/end {stop_reason:"max-iterations"}`（该轮 `answer` 非空 ⇒ 上一轮的 `stopNote()` 按要求**不**多说）。**① 根因（后端）**：`backup.py` 的批次目录 `os.replace(tmp_dir, final_dir)` 是**唯一**没走"瞬时锁有界重试"的落盘点（`storage/atomic_write.replace_with_retry` 早就为同一类 `WinError 5` 而建）⇒ 本次把它**全模块**接通：`backup.py` 顶导入 `replace_with_retry`，目录 / journal / post / after / stack 共 10 处 `os.replace` 全部改为带重试版本（只对 `WinError 5/32` 退避 5×50ms，耗尽仍抛 ⇒ 保持 fail-closed）。**② 根因（前端）**：轮询载荷**一直**带 `tool_calls[]`，但前端 `agent-panel.js` **一行都没读过它** ⇒ `propose_write` 失败在人眼里等于"什么都没发生"，于是结论必然是"agent 没有做出行动、卡在思考"。修法：`ask.py` 给 `tool_calls[]` 每项补 `message`（工具结果首段、空白折叠、截断 240 字，追加在文件尾的 `_tool_message()`），前端在**气泡末尾**渲染工具摘要条 `.-agent-tools`（成功只报总数；**失败逐条红字**列「名称 + 代码 + 原因」，原因过长 `title` 看全）。**验收**：`pytest -q` **771 passed**（+3：`test_agent_backup.py` 用 monkeypatch 造"前两次 WinError 5、第三次成功"钉住目录 rename 被救回且不留临时目录；`test_agent_loop.py` 的 `_tool_message()` 折叠/截断与 ask 载荷含 `message`；`test_agent_panel_token.py` 的工具条接线 + CSS + 载荷契约守卫；i18n 键 +3）；`node --check` / `i18n_selftest` 全 PASS。**如实交代**：**未**替人重放到他的库上验证（写他的库必须他确认）；本轮只证明"瞬时锁会被救回"（有界重试）与"失败必然显形"，若那次 `WinError 5` 是**确定性**成因（例如目标目录已存在），则会由新的红字失败条如实报出，不再无声无息。 |
| 2026-09-21 | **状态行撤除 + 用量行定位修正（补命中率）+「没有最终答复」如实说明**（人一次性报三件事） | 人：「`<div id="agent-status">` 对话框下面这个能不能去掉，而且 agent 最后一次回话下方没有显示 token 使用情况以及命中率；我发现 agent 有时候思考就卡在思考了，然后没有后文，比如 AAA_Vocab」。**① 状态行撤除**（人在两问中选定「整行删掉，错误改走顶部 toast」）：`index.html` 那行**改成注释**（**保留行数** ⇒ 文档里 `index.html:175/323/360` 等锚点不动），`app.css` 的两条样式同样原地留注释作废（保 `app.css:5160` / `5615-5641` 锚点）；`agent-panel.js` 末尾追加块**接管** `setStatusText()` —— 元素既不存在，原实现自动空转，接管后只记状态（`statusBarDotState()` 改读内部布尔，原先读它的 `-agent-error` 类），并把原本**只**显示在那行的提示（未输问题 / 换库丢弃 / 超时 / 配置读取失败）改走 `showFlashError`。去重：`setStatusText(..., true)` 与显式 `showFlashError()` 常成对出现且两种先后都有 ⇒ `flashOnce()` 300ms 闸门（一次失败只弹一个 toast）；「断网」**不弹**（它是状态：状态 bar 有红字槽 + 发送键已禁用 + 悬停说明）。**② 用量行定位修正 + 命中率**：根因是**序位错位** —— `renderTurnUsage()` 按"assistant 序位 + `querySelectorAll('.-agent-msg--assistant')`"取元素，而计划 / 写入回执卡也带这个类（`plan-confirm.js:212`）却**不在** `messages` 里 ⇒ 一旦出现过写入卡，序位整体偏一格，**用量行挂到那张卡上、最后那条回话下方反而空白**（正是人看到的现象）。改法：每个消息元素打 `data-msg-index`（包装 `messageEl()`，不改本体 ⇒ 锚点不动），定位统一走新增的 `assistantWrapFor()`（`assistantOrdinal()` 删除且不许复活；`generatingHost()` 的同款坑一并修）；文案补命中率 `agent.usage.rate`（口径 `hit / prompt`，与底部 `#status-agent` 同源，`hit` 未知则不写该句）。底部窗口状态栏保持原样 ⇒ **两处都有**（人的选择）。**③「卡在思考没有后文」**：真机取证（`docs/example/AAA_Vocab/.memoria/agent/sessions/session-20260921T111525Z-ab1ac72c.jsonl`）—— `loop/end {stop_reason:"max-iterations", iterations:8}`，最后一轮 `assistant/message.content` 是**空串**（只有 tool_calls）⇒ 后端 `answer=""`，而前端**完全不读** `stop_reason`（该字段 RPC 一直在回传）⇒ 气泡全空。改法：`finalizeMessage()` 在"没有正文"时补 `stopNote()` 一句话（`agent.stop.maxIterations` / `maxTokens` / `filtered` / `emptyAnswer`，含"回复「继续」"的可行动作）；**正常收尾但正文为空**（`test_agent_thinking_stream.py` 明确允许 `answer==""`）同样要说一句。**验收**：`pytest -q` **768 passed**（`test_agent_panel_token.py` **+3**：`stopNote()` 从源码抽出交 node **实跑**逐 reason 断言、用量锚点与"序位法禁复活"、状态行撤除与 toast 接管的机械守卫；i18n 键清单 +4）；`node --check` / `scripts/i18n_selftest.js` 全 PASS；**L4 实渲染（真实 `app/index.html`）**：`#agent-status` 实测 `null`；空问题点发送 ⇒ `#-flash-host` 出现「请输入问题」（**证明提示没随状态行一起消失**）；历史会话渲染后三条消息**全部**带 `data-msg-index`（含真实 assistant `index=2`）；合成一张 `-agent-plan-msg` 卡后**旧序位法恰指向那张卡、新选择器仍指向真实消息**（原 bug 精确复现）；无新增 console error。**如实交代**：「用量行真的出现在最后那条回话下方」仍**未在真机实测**（harness 无模型 ⇒ 产生不了真实轮次；已用源码 / node 实跑 / 选择器三层证据钉住）；从历史**回放**旧会话时不显示用量行（`lastUsage` 只由本轮 poll 写入，与原设计一致）——若也要看历史轮次用量，需另取会话里的 `loop/end.usage` |
| 2026-09-21 | **修复：「撤销之后栈状态栏就没了，我无法重做」= 把"指针到最底"误当"该收起来"** | 人报原话即标题。**根因**：`afterStep()`（撤销 / 重做一步后的统一收尾）的条件是 `stack.cursor < 0 → panel.markWriteUndone()` —— 可撤到最底时步骤**全都还在栈里**（`total > 0`）且 `can_redo === true`，那条「已撤销」短显态会把整条状态栏收掉，**连重做入口一起收掉**。**修法**：① **删除「已撤销」短显态**（`markWriteUndone()` 函数 + `writeUndone` 状态 + `agent.writeUndone` / `plan.undoneTitle` 两个键）：栈语义下它没有任何正当用途，撤到底的自然显示就是「栈 0/N」+ 撤销禁用 + **重做可用**；② 状态栏去留只由"**栈里是否真有步骤**"决定（`total === 0` ⇒ 清，否则照常渲染，可用性交给 `can_undo` / `can_redo`）；③ 顺带修同族的两处不一致：**手动预览卡点「应用」以前根本不发布状态栏**（那条路径既看不到栈位置、也没有重做入口）⇒ apply 成功后也调 `publishWriteState()`，且会话 id 回落链补上**回执里的** `session_id` → `ui-plan`（后端 `agent_plan_apply` 省略 session_id 时的伪会话，少这一环就查不到栈）；卡片上的撤销入口文案由「撤销这次对话的改动 / 回到对话开始前」改为「撤销一步」（它调的就是 `agent_plan_undo_step`，旧文案是谎）。**验收**：`pytest -q` **771 passed**（接线测试改为**反向**断言：`markWriteUndone` / `writeUndone` / `stack.cursor < 0` 一律不许复活 + 手动 apply 必须发布 + sid 回落 + 旧文案不许出现）；`node --check` / `i18n_selftest` 全 PASS；**L4 实渲染两轮**（真实 `app/index.html` + 临时副本 KB + 真实 `agent_plan_apply/undo/redo`，零 LLM 成本）：① 手动应用 ⇒ `afterManualApply.barExists=true`、`已修改 1 个文件  栈 1/1`、撤销可用；② 点「撤销一步」⇒ `barStillThere=true`、`栈 0/1`、**`redoDisabled=false`**（人报的 bug 不再复现）；③ 点「重做一步」⇒ 回到 `栈 1/1`；④ 复核计数一致性 ⇒ `afterApply` 与 `afterUndo` **都是「已修改 1 个文件」**（此前 `afterStep` 改用 `res.files` 会从 1 跳成 4 —— 那是把 `manifest.yaml` / `pending.json` 等派生文件也数进去了，已改回按"这次写入的清单"计数）；两轮均 **0 新增 console error**。**如实交代（尚未处理的同族小瑕疵）**：展开态栈轨迹的行文案仍来自后端 `stack_state()` 的 `files`（= 该批 journal 的文件数，**含派生文件**）⇒ 轨迹里写「第 1 步 · 4 个文件」而折叠态写「已修改 1 个文件」，同一屏两个口径；要彻底一致需让后端区分"用户关心的内容文件"与否，属另一件事，待定。 |
| 2026-09-21 | **回合过程折叠（对齐上游 dsh 的 Turn Process Folding）+ 工具行边跑边出**（人：「agent 好像干活内容写在思考里面了？我想要 agent 像 trae 一样，工作过程中思考归思考、输出归输出，调工具就显示调用工具的气泡，结束时自动收起，用户想看细节可以点击对话顶部展开，上游好像也有这样的设计」） | **上游口径（已核对源码）**：`dsh-src/packages/client/ui-chat/README.md:54-58` + `TurnProcessNodeView.tsx` —— 回合进行中过程节点（推理 / 工具行 / 最终答复之前的更早助手正文 / 重试行）**全部展开**；`turn/end` 后，**仅当**末步构成"最终答复"（有非空正文且**不含 tool-call 块**）时，把此前的过程行折成**一行摘要按钮**（`N tool calls · N messages`，全 0 时 `Thought for a while`），点它展开；**关闭但没有最终答复的回合，过程证据全部保留可见**；工具行 = 工具名 + 参数摘要 + `running/ok/error/stopped`，点行才展开 IN/OUT；默认档 `Compact`（另有 `Normal` 不折叠）；**没有**全局"展开全部"。**人已拍板四条**：工具行**边跑边出** / 思考**始终默认折叠**+末行摘要 / **连 `Normal`-`Compact` 设置项一起做** / **回放也按上游做**（⇒ 推理必须随步骤落盘）。**① 后端**：新模块 `services/agent/turn_process.py`（过程行的**唯一**形状来源：`row_detail` / `arg_summary` / `tool_row` / `step_row` / `process_items`）；`AskJob` 增 `process` 缓冲 + `process_delta`/`process_cursor`（`agent_ask_poll` 加**第 4 个可选参数** `process_cursor`，避免每帧重放）；`ask()` 暴露 `on_event` 并与落盘**链式**组合（先 `session.append`、再 panel 回调；panel 抛错不影响落盘）；`loop.py::_stream_once` 返回 4 元组，把该步推理写进 `assistant/message.reasoning`（**只增不改**、空则省略该键；`build_history()` 不读它 ⇒ **不污染模型请求**）；回放 `conversation_messages()` 给 assistant 记录补 `process`（**无过程内容则不带该键**）；配置键 `transcript_mode`（`normal`/`compact`，缺省 `compact`，非法值 `ConfigError` 且不落盘）。**② 前端**：思考行**默认折叠**（`thinkEl(false)`）+ 摘要（流式取**末行**、定稿取**首行**）；气泡内 `.-agent-process` 过程行（`.-agent-step` = 更早助手正文、`.-agent-tool` = 名称·参数摘要 + 状态点 `data-state` + 点行展开 `.-agent-tool-output`）；compact 档且末步是最终答复 ⇒ `.-agent-process-fold`（**真实 `<button>` + `aria-expanded`**，**按轮记忆**、新回合默认折叠），摘要 = `N 次工具调用 · N 条过程消息 · N 次失败`（为 0 的段省略、全 0 回落「思考了一会儿」）；normal 档永不折叠；设置弹窗「对话」页签新增 `#agent-transcript`；回放用**同一渲染器**；**删掉**上一轮的 `toolsStrip()` / `.-agent-tools`，改用 `synthesizeRows(rec)` 从 `result.tool_calls` **兜底合成**过程行 ⇒ 「工具失败不可能隐形」这条底线不退化。实现仍守"**包装门面 `call`**（补第 4 个游标参数）+ **末尾追加块**"⇒ `agent-panel.js:3958` 等既有锚点**逐字未动**。**③ 顺带修一个真 bug**：`finalizeMessage()` 用 `box.replaceChild(rebuilt, wrap)` 重建气泡，但 `.-agent-turn` 包装后 `.-agent-msg` **已不是** `#agent-messages` 的直接子节点 ⇒ 抛 `NotFoundError`（**定稿重建静默失败**：错误条 / 「（已停止）」/ 答案重绘都可能没生效）；等量改为 `wrap.parentNode.replaceChild(...)`。**验收**：`pytest -q` **796 passed**（后端 +17：新增 `tests/test_agent_turn_process.py` 16 例（`arg_summary` 挑键/回落/坏 JSON、`row_detail` 折叠截断、`tool_row` 同 id 两态、`step_row` 计数与空推理省键、`process_items` 顺序与跳过末条、链式发射器落盘优先且 panel 抛错不影响、推理落盘/无推理无键/`build_history` 不带推理、回放带与不带 `process`、`transcript_mode` 缺省与非法值、`agent_get_config` 读回）+ `test_agent_loop.py` 的真实增量流式例；前端 +6 例，其中 4 例 **node 实跑**纯函数：`thinkSummaryLine` / `processSummaryText` / `mergeToolRows` / `finalAnswerFromRows`）；`node --check` ×2、`scripts/i18n_selftest.js` 全 PASS；**L4 实渲染**（harness 8644 + `AAA_Vocab` **临时副本**，用的就是那份会话里**真实**的 `tool/call`/`tool/result` 事件）：18 个 turn / 5 个折叠容器 / **10** 条工具行（修复前实测 **20** ⇒ 说明当时"每次调用出两行"，故补 `mergeToolRows` 按 id 合并）、点摘要行 `aria-expanded=false→true` 且 body 显示、点工具行展开 `output.hidden=false`、切 `normal` ⇒ `fold=0` 且过程块直接位于正文之前、切回 `compact` ⇒ **按轮记忆**（手动展开过的那轮仍展开）、另在临时副本里造了一份带推理的合成会话 ⇒ 思考行 `open=false`、摘要取**首行**、折叠容器内且**刷新页面后同形重现**、全流程 **0 新增 console error**。**如实交代**：**实时（真 LLM）通道未真机跑**（harness 无模型）—— 靠"后端增量测试 + 前端纯函数/接线守卫 + 回放同渲染器"三层证据；**回放折叠判据是启发式**（`agent_session_load` 不带"末步是否最终答复"的信号，按"末行是已收尾的工具行"推断 ⇒ 极端情形如 `max-iterations` 收尾在回放里可能与实时口径不同，实时的 `rec.processFinal` 是精确的）；工具行**只有鼠标点击**（无 `tabindex`，折叠摘要行是真实 `button`）。 |
| 2026-09-21 | **修复：设置里新加的 `<select id="agent-transcript">` 违反 UI 主题**（人：「`select` 样式设计违反 ui 设计主题」） | **根因**：上一轮给 `#agent-refresh` 补的那套 select 外观是**按 id 写死**的（`app.css:5512` 原文 `#settings-body-agent #agent-refresh { … }`）⇒ 新控件 `#agent-transcript` 完全没被覆盖，露出浏览器原生外观。**修法**：选择器改成两个 id **共用一条**（`#settings-body-agent #agent-refresh, #settings-body-agent #agent-transcript { … }`，**等量 1 行**，注释同步改写 ⇒ `app.css:5615` / `:5702` / `:5865` 等既有锚点零漂移）；并**在文件末尾追加**一条聚焦态 `#settings-body-agent select:focus { border-color: var(--theme-color); outline: none; }`（与 `.-agent-field input:focus`（`app.css:419`）对齐 —— 原先两个 select 都漏了聚焦态，Tab 进来会露默认焦点环）。**验收**：`pytest -q` **796 passed**；**L4 实渲染**（harness 8645 + 临时副本 KB，**只读、未改任何设置值**）：`#agent-refresh` 与 `#agent-transcript` 的 `border / radius(4px) / background / color / fontFamily / fontSize(13px) / padding` **逐项相同**、两者同为 `490×31` 且 `overflowsParent=false`、option 为 `normal=完整（不折叠）` / `compact=紧凑（回合结束折叠过程）`、**0 新增 console error**。 |
| 2026-09-21 | **对话栏按钮改圆角**（人指着回合过程摘要按钮：「`button` 改成圆角」） | `.-agent-act`（复制 / 撤销·重做 / 写入栏切换 / 回合过程摘要按钮共用这一族）的 `border-radius` 由 **3px → 6px**（**等量 1 行**替换 ⇒ `app.css:5512/5615/5702/5865` 等锚点零漂移）。取 6px 的理由：`ui-visual-language` 已定"圆角收敛为 **4/6/8/12/999**"，而 3px 不在档位内、看着就是方角；这是**内部控件**档 ⇒ 6px（横向内距同为 6px ⇒ 不裁字）。**同族一起改**（不单改过程折叠那一个），避免同屏两种圆角。**验收**：`pytest -q` **796 passed**；静态核对确认 `.-agent-msg .-agent-process-fold > .-agent-process-toggle` 那条规则**不覆盖** `border-radius` ⇒ 该按钮继承 6px（`. -agent-act` 族内无第二处 radius 声明）。**如实**：本轮**未跑 L4**（纯 1 行 CSS 圆角值变更、且继承链已静态核实），请人重启应用后目视确认。 |
| 2026-09-21 | **每次对话结束不再"工作区不刷新"**（人：「每次对话结束 memoria 没有刷新，程序上先挂一个刷新」） | **根因**：刷新原来只有一条路 —— `plan-confirm.syncAfterWrite()`，且它由"**收到写入回执**"触发（`propose_write` 每次确实都会登记回执，见 `tools/kb.py:2966`），可 agent 现在是**调用内直接落盘**：只要这条链路没走到（本回合只是读+答、提议没被 drain、回执渲染失败…），文件树 / 待确认 / 图谱 / 当前文档就停在本回合之前的样子。**修法**：在**回合收尾**（`finalizeMessage()`，`done` 与 `error` 都走）**无条件**刷一次 —— `agent-panel.js` 新增 `refreshAfterTurn(result)`：① 三个 KB 视图（`refreshFiles` / `refreshKbPendingSummary` / `loadGraphData`）无条件刷；② 当前文件**只在"本回合写过库"**（`turnWroteSomething()` = `propose_write` 成功过一次）**且没有未保存编辑**（`__memoriaHasPendingEdits()`）时重开，绝不覆盖人正在写的编辑；③ 与 `syncAfterWrite()` 共用 **1.5s 去重窗口**（`A().__kbRefreshAt`，纯函数 `shouldSkipKbRefresh()`）：视图可跳，但那条**精确**的"确知被写就重开当前文件"永远执行 ⇒ 同一回合不会把图谱连算两遍。**验收**：`pytest -q` **798 passed**（`test_agent_panel_token.py` **+2**：`shouldSkipKbRefresh()` node 实跑窗口断言；收尾刷新的机械守卫 —— 钩子在场、三个视图调用、`propose_write` 判据、脏编辑不重开、两侧共用同一窗口且 `syncAfterWrite` 只跳两组视图）；`node --check` ×2、`i18n_selftest` 全 PASS。**如实交代**：**未跑 L4**（需要真实回合才会触发），且**重开当前文件会重置该文档的滚动/光标位置**（`openFile(skipNav)` 的既有行为）—— 如果你觉得这在"只是读+答"的回合里也不该发生，我们下一步就把"写过库"的判据换成**后端回传的 touched files**（更精确）。 |
| 2026-09-21 | **修复：助手气泡里的公式不渲染**（人：「agent 回复的内容公式没有被渲染，它的渲染不是采用和 memoria 一样的吗」） | **答案当时是"不一样"**：气泡正文只走 `marked.parse()`（`renderAssistantBody()`），**完全没有**预览那条公式管线 ⇒ `$…$` / `$$…$$` 原样留在文本里。而 MathJax 的**全局配置本来就认这两个定界符**（`index.html:24-33` 的 `inlineMath` / `displayMath`），且 `skipHtmlTags` 已排除 `pre`/`code`（代码里的 `$` 不会被当公式）⇒ 缺的只是"**归一化 + 排版**"两步。**修法（两步都复用预览那一份实现，不复制第二套规则）**：① 交给 `marked` 之前先 `agentMathSource(src)` = `MemoriaMathNormalize.normalize()`（`markdown-preview.js::normalizeBody()` 用的同一个模块，顺带把"没写定界符的旧式公式"按同一标准收进 `$$…$$`）；② 净化**之后** `typesetAgentMath(el)` = `MemoriaMarkdownPreview.initMathJax()` + `MathJax.typesetPromise([el])`（**同一个 MathJax 实例 / 同一份配置**）。两个次序都刻意：**排版必须在净化之后**（`sanitizeHtmlInto()` 会剥掉一切未知标签与属性，而 `<mjx-container>` 是 MathJax 的产物，净化在前会被削掉）；**排版必须防抖**（流式每帧重渲染，`AGENT_MATH_DEBOUNCE_MS = 300` + `el.isConnected` 守卫 ⇒ 只在停下来那一帧排一次、游离节点直接跳过）。**验收**：`pytest -q` **800 passed**（`test_agent_panel_token.py` **+2**：`agentMathSource()` node 实跑"有模块走它 / 缺失或抛错一律原样返回、绝不吞正文"；正文走同一管线 + 净化先于排版 + 防抖与游离守卫的机械断言）；`node --check`、`i18n_selftest` 全 PASS；**L4 实渲染**（harness 8646 + 临时副本里一份**合成会话**，正文含 `$a^2+b^2=c^2$` 与 `$$\int_0^1 x^2\,dx=\frac{1}{3}$$`）：`mjxTotal=2`（**inline 1 + display 1**）、`hasRawDollar=false`、`merror=0`、首个子节点实证为 `MJX-CONTAINER.MathJax`（不是 CSS 伪排版）、**0 新增 console error/warn**、也没有 `agent-math` 相关警告。**如实交代（仍未与预览完全一致，皆属有意）**：① **代码块没有语法高亮**、且 `MD_DROP_TAGS` 会丢掉 `IMG`/`SVG`、保留标签的属性**一律剥掉**（`on*`/`href`/`src` 一并不留）—— 因为**气泡内容是模型输出、不受信**，而预览渲染的是你自己写的文档；② **mermaid 不渲染**；③ **过程行里的"更早的助手正文"（`.-agent-step`）与思考行仍是纯文本**（不走 markdown/公式管线）—— 要不要一起走，待你定（会给那两处加上块级排版，属视觉变更）。 |
| 2026-09-21 | **库内图片也渲染 + 思考/过程正文同样走 markdown（并修掉它暴露的回放缺口）**（人：「img 怎么会被丢，都是交给 memoria 渲染管线渲染的，只要有本地图片文件就会渲染出来，思考可以渲染吧，不然公式这些用户都看不懂，上游中思考部分你渲染吗」） | **① 图片（人对）**：气泡的净化器原先把 `IMG` 整棵丢（连同 `on*`/远程出网一起防），但**库内图片本来就该渲染**。改法：`agentRichHtml()` 在 `marked` 之后调用预览的 `rewriteLocalImagePaths()`（**同一份实现、同一个库根**：`.memoria/images/x.png` → `/files/...`），净化期新增 `appendLocalImage()`：**只**放行 `isLocalImageSrc()`（`/files/` 前缀）的图片、**只**搬 `src`/`alt`/`title`（`on*` 不抄），远程 / `data:` / `javascript:` 一律丢；渲染后 `attachAgentImages()` 复用预览那套**灯箱**（双击放大）。**② 思考与过程正文（有意超出上游）**：`thinkEl()` 的非空文本走 `renderAssistantBody()`、过程行的「更早助手正文」（`.-agent-step`）同样 ⇒ 归一化 + marked + 净化 + MathJax 全套；配套 CSS 撤掉这两个容器的 `white-space: pre-wrap`（同 `.-agent-msg-body.markdown-body` 口径）。**关于上游（人问）**：`dsh-src/packages/client/ui-chat/src/client/chat/ReasoningRow.tsx:60` 是 `<div className={css.thinkBody}>{text}</div>` —— **纯文本**，第 30 行还专门 `replaceAll('**','')` ⇒ **上游不渲染思考**；本仓思考里满是公式，故**有意超出**。**③ 顺带修一个被 ① ② 暴露的真缺口**：回放时**最终答复那一步**的 `reasoning` 会整体丢失 —— `turn_process.process_items()` 有意跳过"构成最终答复的那条 `assistant/message`"（免得正文在过程列表里重复），而单步回合里它同时也是唯一带思考的那条 ⇒ `process` 为空 ⇒ 前端连思考块都不建。改法：`conversation_messages()::_flush_turn()` 把该步的 `reasoning` **单列到气泡记录**上（`record["reasoning"]`，`history.py` 行数在文档锚点 `:506`/`:514`/`:527` 处保持不变），前端 `msgReasoning()` 增加读取它。**验收**：`pytest -q` **804 passed**（前端 +3：库内图片放行/属性白名单/灯箱复用、`isLocalImageSrc()` node 实跑九例安全边界、思考与过程行走同一管线 + CSS 收口；后端 +1：最终答复步的思考必须落在记录上）；`node --check`、`i18n_selftest` 全 PASS；**L4 实渲染**（harness 8647 + 合成会话：正文含**库内** `![photomode](.memoria/images/photomode_21072025_214606.png)` + **远程** `![remote](https://example.com/foo.png)` + `$E=mc^2$`，思考含 `$a^2+b^2=c^2$` 与 `$$S=\pi r^2$$`）：`imgCount=1`、`src=/files/...`、**`complete=true`/`naturalWidth=1920`**（真加载出来了）、`remoteImgCount=0`（远程被丢、且没有任何 `example.com` 请求）、`bodyMjx=1`、**`thinkBodyExists=true` 且 `thinkMjx=2`（块级 1）**、`thinkHasRawDollar=false`、`merror=0`、展开思考 `bodyHeight=72`、**0 新增 error/warn**（`[img-debug]`/`[img-rewrite]` 0 条）。 |
| 2026-09-21 | **气泡里的图片按对话栏宽度自适应缩放**（人：「agent 对话的图片自动按照对话栏宽度对图片大小后处理自适应缩放」） | **真机症状**（上一轮 L4 的副产品）：库内图**按原始像素**铺开 —— 1920×1080 的图在 `paneWidth=351` 的对话栏里渲染成 `w=1920` ⇒ 横向溢出/被裁。**修法**：`app.css` 末尾追加一条**三容器共用**的规则（`.-agent-msg-body.markdown-body img` / `.-agent-think-body.markdown-body img` / `.-agent-step.markdown-body img`）：`max-width: 100%`（撑满不越界）+ `height: auto`（保比例）+ `border-radius: 4px`（取预览 `.-preview img` 同一档，`app.css:4255`）。注意**不照抄预览的 `max-width: 35%`** —— 那是文档内图片的口径，对话栏要求的是"占满栏宽"。**验收**：`pytest -q` **805 passed**（`test_agent_panel_token.py` **+1**：三容器选择器 + `max-width:100%`/`height:auto`/`4px` 断言）；**L4 实测**（harness 8648 + 同一张 1920×1080 库内图）：`imgRenderedWidth=331` = `bodyWidth`（≤ `paneWidth=351`）、`imgNaturalWidth` 仍 **1920**（证明是 CSS 缩放、不是换小图）、`aspectRendered=0.5625` **等于** `aspectNatural=0.5625`（不变形）、`horizontalOverflow=0`、`cssMaxWidth=100%`、`cssHeight=186.188px`（由宽度等比推出）、**0 新增 error/warn**。 |
| 2026-09-21 | **输入框：空框 ↑/↓ 翻「发过的内容」+ Ctrl+Enter 框内换行**（人：「输入框空的时候按上下键要可以切换"发过的内容"，crtl+enter输入框内换行」） | `#agent-input` 本来就是 `<textarea rows="2">`（`index.html:234`）⇒ 换行只需**放行**：Enter 处理器加上 `e.ctrlKey \|\| e.metaKey \|\| e.altKey` 三个排除项（**净 1 行**替换 ⇒ 全部锚点零漂移），Ctrl/Cmd+Enter 于是走浏览器默认 = **框内换行**；无修饰 Enter 行为不变（仍是发送）。**历史翻页**：追加块里 `sentTexts()`（取自本地 `messages` 里 `role === "user"` 的非空文本 ⇒ **含载入的历史会话**）+ 纯函数 `composerHistNext(cursor, dir, n)`（首次 ↑ = 最新；到最旧**停在最旧**；越过最新 ⇒ `null` = 回到空框）+ `composerHistStep()` 落值并把光标放到**末尾**、`afterComposerEdit()` 同步 chip；`input` 事件与 `ask()` 包装都会复位游标。**关键判据（L4 打回后修）**：守卫**不是"框空不空"**而是 `值非空 && composerHistCursor === null` —— 历史填充后框也非空，只判空会让第一次 ↑ 之后后续 ↑↓ 全被挡（**一次性 bug**）。**验收**：`pytest -q` **807 passed**（+2：`composerHistNext()` node 实跑九例；Ctrl/Cmd 排除项 + 历史接线机械守卫）；`node --check` 通过；**L4 实渲染两轮**（harness 8649 + 三条人发消息的合成会话）：第一轮抓出上述"一次性"bug（`a=[第二条,第二条,第二条,第二条,第二条]`）；修后第二轮真实按键**七步全中** —— `["Enter探针","第二条历史消息","第一条历史消息","第一条历史消息","第二条历史消息","Enter探针",""]`（含"最旧处停住"与"越过最新回空框"），`draftProbe="草稿ABC"`（有草稿按 ↑ **不被覆盖**），无修饰 Enter 实测 `userBubbles 2 → 3`（仍是发送），**0 新增 error/warn**。**如实交代**：本 harness 的键盘**默认动作不可用**（`document.hasFocus()` 恒 false —— 连对照组 Shift+Enter 都不插换行）⇒ "Ctrl+Enter 真的插入了换行"**没能在真机按键下证明**；现有证据是三条：① 合成 `keydown(key=Enter, ctrlKey=true)` 的 `defaultPrevented === false`（**没有任何监听拦截它**）、② **没有发送**（两次 Ctrl+Enter 前后 `userBubbles` 恒为 2，硬实测）、③ 真实输入路径能把 `\n` 写进该 textarea（控件本身支持多行）⇒ 请你在真机上确认一次。另：**未加**自动增高（输入框已有 `max-height: 10rem` + `resize: vertical`，自动增高会和手动拖高打架）。 |
| 2026-09-21 | **修复：写闸门死锁 —— "库里既有残缺"会让所有修复性写入都写不进去**（人：「经常失败，这说明设计存在问题」） | **真机取证**：agent 连提 **4 批** `propose_write` 全 `WRITE_FAILED`，报的永远是同一批**既有**问题（某文件两个 KP 的 `end.snippet` 早年丢了）；"只更新一个 KP"的探针批**也没写进去**，只是报错列表里不再点名它 ⇒ 看起来像"放行了"，是假象（agent 据此还推断成"校验是应用后检查"，方向被误导）。**根因**：写闸门（`document.py` **11 处**同形 + `save_document` 一处）**逐原语**跑在**整份** sidecar 上，且 `validate_sidecar` 的 `kp_range_missing_snippet`（`sidecar_validate.py:260-272`）是 **error** 不是 warning ⇒ 「任何 error 都拒写」+「库里已有一处残缺」= **任何修复性写入都写不进去**（不可修的死锁）。**修法（单一规则放在校验器里，闸门只传基线）**：`validate_sidecar(…, baseline=…)` 新增参数 —— 与基线**同签名**（`_issue_sig` = `code` + `kp_id` + `kind` + `params`，**刻意不含 `line`**：正文一改行号天然漂，含行号会把既有问题误判成新引入）的问题**不算本次引入** ⇒ 放行并如实放进新的 `pre_existing[]`；**新引入的**照旧 `ok=False` 拦住；`baseline=None` 与旧口径逐字一致。基线从**盘上那份**读（`document.py` 末尾新增 `_sidecar_on_disk()`）：闸门都跑在"内存已就地改完、还没落盘"的时刻，而盘上此刻**必然**还是写前状态；读不到 ⇒ 空 dict ⇒ `validate_sidecar({})` 无错误 ⇒ **退回旧口径（宁严不松）**。落地方式：11 处同形闸门 **`replace_all` 等量替换**（每处仍是 1 行 ⇒ 全部行号锚点零漂移）。**验收**：`pytest -q` **812 passed**（新文件 `tests/test_sidecar_write_gate.py` **5 例**：基线只降级既有、新引入的照拦、写前写后同状态不拒；`confirm_kp_range` 修 a 不被 b 的既有残缺挡住**且 b 不被顺手改**；**批级**复刻真机那一批 —— 同文件 3 条 `upsert_kp` 一次补齐 3 个残缺 KP ⇒ 整批 `ok` 且落盘后 0 error；正文编辑同样不被别处既有残缺挡住）；`py_compile` 通过。**如实交代**：① 这 5 例在修前**必然失败**（旧口径下就是 `配置校验失败`）；② **没在你的真实库上复现**（`channel-utilization-and-delay.md` 不在仓库内，我不动你的库）——复刻用的是等价合成状态；③ 修完之后**库里那处既有残缺仍在**（规则是"不阻塞"，不是"顺手修"）—— 下次让 agent 补那两条 `end` 锚点即可写进去；④ **未**把 `pre_existing` 一路透传到工具回执（`confirm_kp_range` 的成功返回仍是原样），所以 agent 目前**看不到**"库里还剩哪些既有问题" —— 要不要加这条回报，你说一声。 |
| 2026-09-22 | **审批档位移植（`dsh-permission-presets` 的会话级档）+ 逐条确认打通**（人：「继续移植上游」；四问拍板：英文机器键 + 中文显示名 / `auto` 保留闸门 / 默认档 = **自动审批**且**按 agent 配**（设置面板可改）/ 手动档**做通逐条确认**） | **上游最小核心**：① 会话级**可回放**的旋钮状态（`permission/preset` + `approval/policy`，生效值 = 最后一个覆盖事件 ?? 组合默认）；② 统一审批闸（四值封闭词汇 `allowed-once`/`rejected`/`cancelled`/`unavailable`，**只有 `allowed-once` 放行**，`unavailable` 一律 fail-closed）；preset 只是把旋钮打成一个**用户可见命名档**。**本地裁剪**：`sandbox/mode` 那一半**不移植**（无 shell / 无进程隔离 / 网络不在其词汇内），故只剩一个旋钮；`custom` 按上游口径做成**派生只读态**（可展示、**不可作切换目标**、不进事件载荷）；`session/end-seed`（子代理播种）不移植（本地无子代理）。**三档**：`manual-approval`（`ask`：「手动审批」= 每次写入都挂起等你确认）/ `auto-approval`（`auto`：**默认**，「自动审批」= 常规写自动放行、命中**风险条件**才问一次）/ `all-access`（`never`：「完全访问」= 不问、一律放行）。**与上游的语义偏差（如实登记）**：上游 `never` 是「不询问 ⇒ 需审批者一律拒绝」（它有 sandbox 档兜底，`danger-full-access` 时无物需审批），本地无 sandbox 维度 ⇒ 落地为「从不询问 = 一律放行」，兜底回到**写前备份 + 可整批撤销**。**风险条件**（`approvals.RISKY_OPS`）：写计划含 `delete_lines` / `rename_file`，或**形状认不出**（缺 `ops` / 空表 / 项没 `op` 动词 / 换了新写工具）一律按风险；**批量大小不算风险**（每批 all-or-nothing + 备份 + 可整批撤销）。**新增/改动**：`services/agent/permission_presets.py`（档位表 `PRESETS`、`fold()`/`derive()`/`set_preset()`/`pin_and_current()`/`policy_for()`/`catalog()`）、`services/agent/approval_bridge.py`（**进程内信箱**：登记待批项 → 面板轮询可见 → 回填裁决唤醒；超时 120s ⇒ `unavailable`、取消 ⇒ `cancelled`、**未挂载**（CLI）⇒ 应答者缺失 ⇒ 立即拒绝**不空等**）、`approvals.GuardedPolicy` + `write_is_risky()`（**末尾追加** ⇒ `ApprovalOutcome`(50)/`DefaultApprovalPolicy`(102)/`AskPolicy`(128) 锚点零漂移）、`ask.py` 末尾追加 `_bind_permission()`（`ask()` 内**等量替换 1 行**：`approval=approval` → `approval=_bind_permission(root, session, approval)`，行数不变 ⇒ `ask.py:445` 等锚点零漂移）、`ask_stream.py`（`_run` 里 `attach()/detach()`、`cancel()` 里 `abort()`、`snapshot()` 追加键 `pending_approvals` —— 只增不改）、`llm/config.py`（`permission` 进白名单 + 文件尾追加 `PERMISSION_KEY`/`_coerce_permission()`/`permission_presets_map()`）、`presentation/api/ui.py` 文件尾追加四个 RPC（`agent_permission_get` / `agent_permission_set_session` / `agent_permission_set_default` / `agent_approval_answer`）+ 运行期挂载、前端 `agent-panel.js` 末尾块（输入区档位选择器 `#agent-permission`、设置页 `#agent-permission-default`、逐条确认卡 `.-agent-approve-msg`、facade 轮询包装消费 `pending_approvals`）、`index.html:345` 同一行追加第三个 `<label>`（行数不变 ⇒ 锚点零漂移）、`app.css`/`zh-CN.js`/`en.js` 末尾追加。**验收**：`pytest -q` **853 passed**（新文件 `tests/test_agent_permission_presets.py` **37 例**：折叠取最后覆盖 / 派生态不可作切换目标 / 切换幂等 / 钉盘只补缺失事实 / 按 agent 的默认档含坏值回落 / 三档策略与风险判据 / 确认桥的挂起·回填·超时·取消·摘除 / 快照带 `pending_approvals` / 四个 RPC 的各自拒法 / 前端接线不变量）；`node --check` ×3 通过；`scripts/i18n_selftest.js` PASS。**L4 实渲染（harness 8655 + 临时库 + 脚本化 provider，不联网）**：① 后端闭环（HTTP 直打 `/rpc`）—— 默认 `auto-approval` 下**风险写真的被拦**（`pending_approvals` 带工具名与整批 `ops`）→ 回填 `allowed-once` → 工具成功且**盘上真的改名**（`perm-a.md` 404 / `perm-b.md` 200）；切 `all-access` 后同一批**不再挂起**且照样写成功；切 `manual-approval` 后回填 `rejected` ⇒ 工具结果 `DENIED`（fail-closed 落地）；会话 JSONL 里三条整值事件可回放（`auto-approval`/auto → `all-access`/never → `manual-approval`/ask）。② 浏览器内（DOM 真实值 + 截图）—— 选择器 3 项可切换 + `custom` **disabled**、设置页同族行在位、卡片渲染与「允许一次/拒绝」按钮可用、点击后写盘验证通过。**L4 抓出并修掉的两个真 bug**：**㈠** 待批卡原先按 `call id` 单键去重，而 call id 只保证**一轮内**唯一（网关会跨轮复用，脚本化 provider 每轮都发 `call-l4-1`）⇒ 同 id 的**新**待批项被静默跳过 ⇒ 无人应答 ⇒ 挂起 **120s** 后判 `unavailable` 拒绝，**写入静默死掉**；改为 `call id + 后端 created_at` 复合键（同一条跨帧仍只建一张卡、新的待批项必出新卡），复验：第二次发送后卡片计数 **1 → 2**，两卡 key 后缀 `…762274` / `…816542`。**㈡** 点按钮写下的终态（「已允许一次」/「已拒绝」）会被下一帧（250ms）对"不再待批"卡片补的通用「已处理」**覆盖** ⇒ `markApprovalCardDone()` 加"已有终态即返回"守卫；复验：跨多个 250ms 帧后两卡分别稳定显示 `Allowed once` / `Rejected`。**如实交代（未做/未验）**：① 上游的 `approval/asked` / `approval/decided` **审计事件未落盘**（本轮的可见面是工具结果 `DENIED` + 面板确认卡）；② 档位表是**代码常量**，未做上游那种"部署可配置档位表"；③ 上游 `/permission <preset>` **命令面**未移植（本地是选择器 + RPC）；④ 真机真实模型端点下的确认卡观感未验（L4 用的是脚本化 provider）。 |
| 2026-09-22 | **修复：输入框三条真机报障**（人：「输入框内 ctrl+enter 没有换行，而：光标显示在几个空格之后，实际编辑位置和显示位置出现几格错位；agent 生成的时候我无法网上滚动，因为它会频繁刷新到下面」） | **① Ctrl+Enter 真机不换行**：旧实现只做"不 preventDefault、交给浏览器默认"，而 **Windows Chromium 的 textarea 对 Ctrl+Enter 没有默认动作**（2026-09-21 那条注释是错的）⇒ 真机点了什么都不会发生。改为**自己插**：`composerEnterKey()` 收口 Enter 语义（裸 Enter = 发送 / Shift+Enter 放行给浏览器 / Ctrl（Cmd）+Enter 走 `composerInsertLineBreak()`：在光标处插 `\n`、光标移到其后、派发 `input` ⇒ 镜像同步与历史游标复位随既有监听一起跑）。**② 输入框文字与光标错位几格**：引用 chip 的**镜像层**原先用 `offsetWidth/offsetHeight` 定尺寸，而 textarea 一旦出现**纵向滚动条**（内容超过 `max-height: 10rem`），可用文本宽度就窄一个滚动条（L4 实测 12px）⇒ 镜像换行点与 textarea 不一致、光标落在可见文字右侧几格。改用 `composerMirrorWidth/Height()` = `clientWidth/clientHeight`（已排除滚动条）**+ 两侧边框宽（`getComputedStyle` 读，不硬编码 —— 0.5px 边框在本机取整为 1px）**；两处 `syncComposerChips` 都走它（**等量改写** ⇒ 锚点零漂移）。**③ 生成中无法上翻**：每个分片/过程行都无条件 `scrollTop = scrollHeight`，把用户翻页每帧拽回底部。改为**粘底**语义：`stickToBottom` 只由**滚动位置上移**关掉、由"回到近底"打开（`stickAfterScroll()` 纯函数），跟随落在唯一入口 `messageBoxScrollTo()`；`scrollToBottom(true)` 只留三处「必须看到」（整串重绘 `renderMessages` / 用户发问 `pushMessage` / 审批确认卡进场）并**重挂**粘底。**L4 第一轮抓出一个自造的假阴性（已修）**：第一版用"离底 < 48px"判"用户还在底部"，但滚动事件是**异步**派发的 —— 发问后「生成中…」尾行 ≈50px 落地，事件到达时距离 51 > 48 ⇒ 把**从没碰过滚轮**的人误判成"上翻了" ⇒ 整轮回答都不再跟随（实测距离一路涨到 3180）；改为只看位置上移后复验：**全程 138 条 100ms 采样，离底距离恒为 0、`dist>60` 的采样 0 条**。**验收**：`pytest -q` **856 passed**（`test_agent_panel_token.py` +3 例：Ctrl/Meta+Enter 真插换行（node 实跑真源码，含光标中间插入 / 选区替换 / 裸 Enter 走发送 / Shift、Alt、输入法、Ctrl+Shift 放行）、粘底判据与 `stickAfterScroll()` 状态机（含"位置变大不许关跟随"这条 L4 事故的回归钉子）、镜像层走内容盒尺寸）；`node --check` 通过、`i18n_selftest` PASS。**L4 实测（harness 8655 + 临时库 + 脚本化 provider，不联网）**：① Ctrl+Enter 两连击 ⇒ `abc` → `abc\n` → `abc\n\n`（光标 4 / 5、`defaultPrevented=true`），Shift+Enter 值不变且未 preventDefault；② 长文本 + 引用 token ⇒ `offsetWidth-clientWidth=12`（确有滚动条）、`mirror.style.width=492 = clientWidth(490)+边框(1+1)`、**`mirror.scrollHeight === input.scrollHeight`（369 = 369，换行点一致）**、`mirror.textContent === input.value`；③ 生成中把 `scrollTop` 置 0 后 33 条采样恒为 0（离底 29107）、发问后整轮贴底（终态 dist=0）。 |
| 2026-09-22 | **修复：agent 读不了非 markdown 文件**（人：「我发现 agent 读不了非 markdown 文件」） | **根因（读面一处不对称）**：`glob`/`grep` 走 `_walk_kb_files()` + `_safe_rel_any()`（**不限扩展名**）⇒ 模型**看得见** `data.csv`，而 `read_document` 走 `_safe_rel()`（**要求 `.md` 后缀**，`:160-163`）⇒ 真去读就回「path 必须是…相对 `.md` 路径」；同时工具描述与 `@路径` 段都写着"Markdown 文档"⇒ 模型压根不去试（报障的另一半）。**修法（读面放宽、写面不动）**：`_read_document_call()` 里**等量替换 1 行**，改走新增的分流 `_read_text_or_document()` —— `.md`/`.markdown` 仍走既有 `_read_document()`（**语义一字未改**：frontmatter 剥离 + sidecar 知识点清单 + `文件:行号` 锚点），其它路径走新增 `_read_plain_text()`：纯文本窗口（**不剥 frontmatter、不查 sidecar**、无知识点段，窗口/续读沿用 `_read_window()`）、**二进制（含 NUL）明确拒 `BINARY_FILE` 并指路 `read_image`**（不把 PNG 当文本灌给模型）、编码 UTF-8（`utf-8-sig`，**吃掉 BOM**）→ GB18030（Windows 中文文本常见）→ 有损兜底、单次只读前 `PLAIN_READ_MAX_BYTES`（1 MiB）并如实标注。路径守卫一分未松（越界 / 上跳 / 绝对路径仍 `INVALID_ARGUMENTS`，不存在仍 `NOT_FOUND`）。**同轮同步模型面文案**：`read_document` 的工具描述改成"工作区内一篇**文本文件**…Markdown 额外给知识点清单"、`path` 描述加「不限扩展名」，`prompt.FILE_REFERENCE_SECTION` 第 2 条补「**不限 `.md`** —— `.txt`/`.csv`/`.json`/源码等文本文件都能读」（三处都是**等量改写**，锚点零漂移）。**顺带核实**：`resolve_reference` 本来就接受非 md（`@data.csv` / `@data.csv#L2` 实测 `ok`），挡路的只有 `read_document`。**验收**：`pytest -q` **865 passed**（`tests/test_agent_tools_read.py` **+9 例**：模型面文案必须说清"不限 .md"、`.txt` 可读且无知识点段、非 md **不剥 frontmatter**、分页/续读/越界与 md 同口径、二进制拒并指路 `read_image`、GBK 可读、**UTF-8 BOM 被吃掉**、超限只读前缀且如实标注、路径守卫不松）；`py_compile` 通过。**L4 实回合（harness 8661 + 临时库 + 脚本化 provider，不联网）**：真实回合里发 `read_document` 后把工具结果回显 —— `notes/data.csv`（`is_error=false`，正文 `name,score/alpha,91/beta,88`）、`notes/readme.txt`（**GBK 编码**，中文正确解出）、`notes/tool.py`（源码可读）、`notes/blob.bin`（`is_error=true` / `BINARY_FILE` 且指向 `read_image`）。**如实交代**：① 左侧**文件树仍只列 `.md`**（`document.list_files()` 用 `collect_md_files()`）⇒ 非 md 文件在树上**看不到、拖不进对话**（本次**未改**：那是"文件树是文档树还是文件浏览器"的产品口径，等你定）；② 文件引用**锚点**形态（`文件:行号`）仍限 `.md`/`.markdown`（本地无 sidecar/KP 的文件没有"行号锚点"语义）；③ `read_image` 仍只管图片（缺「多媒体眼睛」插件，行为未变）。 |
| 2026-09-22 | **去掉「连续调用工具的 8 轮上限」+ 回复末尾那一行补上复制按钮**（人：「怎么有连续调用工具的 8 轮上限，上游都没有；对话底部除了显示用量信息还需要复制按钮」） | **① 轮次预算回归上游**：上游 `agent-loop/README.zh.md:200` 明写「**没有内置轮次预算**：工具调用或 steering 会让当前轮次继续；限制失控轮次的策略必须从既有生命周期扩展点（如 `agent/turn-stopping`）执行取消」；本地 M1 却把 `max_iterations` 默认 **8** 当安全上界（`loop.py` 模块头自己登记为偏差），真机表现就是"连着调几次工具就被切断"。改为 **`DEFAULT_MAX_ITERATIONS = 0` = 无上限**（对齐上游）：`AgentLoop.__init__` 校验由 `< 1` 放宽为 `< 0`（负数仍拒，不静默当 0）；循环头**等量改写 1 行** `range(1, (self.max_iterations or UNBOUNDED_ITERATIONS) + 1)`，文件尾追加 `UNBOUNDED_ITERATIONS = 1_000_000`（永远到不了的数 ⇒ 保留 `StopReason.MAX_ITERATIONS` 这条**防御性**出口，也不引入 `while True`）；`> 0` 仍是调用方给的硬上限（测试/特殊场景照旧）。**配套**：面板 `POLL_TIMEOUT_MS` 5 分钟 → **30 分钟**（轮次放开后长任务可能是几十轮；真正出口是「停止」= 真取消）。**② 复制按钮**：回复末尾那一行原先只有**用量文本**（挂气泡内），而复制是气泡外那一行里的**纯图标**（`opacity .55`、文案只在悬停时给）⇒ 人没把它当按钮。现在合并成**同一行**：用量格改挂 `.-agent-msg-actions` 的**最前**（`usageRowEl()`/`usageRowHost()`），复制按钮**常显**（`opacity: .8`）且带「复制」二字（`copyButtonEl()` 收口构造）。**L4 顺带抓到并修掉一个真 bug**：助手消息是 `pushMessage("assistant", "")` **空文本**建的 ⇒ `actionsEl()` 建行那一刻返回空行，终答后 `finalizeMessage()` 只换气泡正文、`wrapTurn()` 又复用既有 `.-agent-turn` ⇒ **实时那一轮永远长不出复制按钮**（只有刷新页面走 `renderMessages()` 才有 —— 人看到的"底部只有用量、没有复制"正是这个）。修法：新增 `syncTurnActions(rec)`（有正文就补 `BUTTON.-agent-act--copy`、没正文就摘掉），并**链式包装** `finalizeMessage` 在终答落定后调用。**验收**：`pytest -q` **868 passed**（`test_agent_loop.py` **+2**：默认无预算时跑满 **12** 步工具调用再收尾（旧默认 8 下必失败，真正的回归钉子）+ 预算语义（0=无上限/正数=硬上限/负数报错）；`test_agent_panel_token.py` +1 例并更新 1 处旧断言：底部行是「用量 + 复制」同一行、按钮常显带标签、`syncTurnActions()` 补齐链在位）；`node --check` 通过。**L4 实渲染（harness 8662 + 临时库 + 脚本化 provider，不联网）**：① 12 轮工具调用**全跑完**（工具行 **12**、回合末折叠摘要「工具调用 12 次 · 过程消息 12 条」、`loop/end.stop_reason="final-answer"`、`iterations=13`、正文无任何"达上限"字样）；② 实时那一轮的底部行**同时**含 `SPAN.-agent-usage`（`用量 … tokens`）与 `BUTTON.-agent-act--copy`（`opacity=0.8`、`title=复制`），点击后 `title` → 「已复制」并在 ~1.2s 后复原；③ 全页 `.-agent-usage` 计数 = 1、0 JS 异常。**如实交代**：① 脚本化 provider 不报 cache 用量 ⇒ 用量文本里命中/未命中显示 `—`（正常）；② 刷新恢复的旧会话**没有**用量格（`lastUsage` 是会话内事实、不持久化，既有行为）；③ 真机真实端点下"几十轮不切断"的观感未验（L4 用的是脚本化 provider）。 |
| 2026-09-22 | **补齐「读后写守卫」+ 审批审计事件 + `never` 改名**（人从"我们改了上游设计"的清单里点名这三件）。**都是向上游靠** | **① 读后写守卫**（新 `services/agent/observation.py`，语义移植上游 `fs/fs-observation-policy`；上游 README 已逐条对照进模块头）：观察态三态 `unseen` / `confirmed absent` / `present@sha256`；`read_document`/`read_kp` 成功即记（`NOT_FOUND` 记"确认不存在" ⇒ 授权之后的 guarded create）；**只有 `create_file` 免读**，其余 op 未读 ⇒ `FS_NOT_OBSERVED`、读到过但当时不存在 ⇒ `FS_NOT_FOUND`、sha256 变了 ⇒ `FS_STALE_VERSION`（都给"重读再重提"的恢复指引）；**进程内、不跨会话**；**写成功后刷新观察版本**（= 上游 `fs/observed`，否则模型改完自己就撞 STALE）。落点 = 写工具处理器 `tools/kb.py::_observation_block()`（上游是 `fs/*` 事件闸 + provider 原子 CAS ⇒ 本地比较-写非原子，如实登记）。**② 审批审计事件**：`approval_bridge` 增 `EVENT_ASKED`/`EVENT_DECIDED`（载荷对齐上游 `{id, tool, reason}` / `{id, outcome}`，`id` = tool call id）+ `attach_audit()`；`ask._bind_permission()` 把本会话 `session.append` 装给桥 ⇒ **每次 ask 恰一条 decided**（决定/取消/超时/摘除同一出口；log-only 不进模型请求；审计回调抛错不影响裁决）。**③ `never` → `allow-all`**：本地该档是"不问 ⇒ 一律放行"，上游 `never` 语义相反（"不问 ⇒ 需审批者一律拒绝"，靠 sandbox 兜底）⇒ **同名反义**易误判而改名；旧会话事件里的 `never` 用 `LEGACY_APPROVAL_POLICIES` **只读兼容**（日志 append-only 不重解释）。**顺带修**（L4 暴露）：同一秒第二次 `propose_write` 撞批次目录（txid 写死 `-01`）⇒ `WRITE_FAILED (backup_failed)：批次已存在`；改 `_next_txid()` 递增。**验收**：`pytest -q` **891 passed**（新 `tests/test_agent_observation_gate.py` 15 例；`test_agent_permission_presets.py` +5 例；`test_agent_plan_rpc.py` 越界路径用例拆成"闸更早拦 + 编译器兜底"两例）、`node --check`、`i18n_selftest`、锚点零漂移；**L4 真实回合**（harness 8663 + 脚本化 provider）：未读就写 ⇒ `FS_NOT_OBSERVED` + 盘上零写入、先读再写 ⇒ sidecar 真的落 `demo-kp`、新会话改名 ⇒ 又 `FS_NOT_OBSERVED`、人工放行 ⇒ JSONL 落 `approval/asked`+`approval/decided`。**台账**：`docs/todo.md` 新增 `AG18`（人明确要求"登记台账"）。**如实交代**：① 无应答者（CLI 未挂载）不经审批桥 ⇒ 无审计事件（可见面是工具结果 `DENIED`），与上游"无 handler 也记 `unavailable`"有差；② 档位表仍是代码常量；③ 真机面板观感未验。 |
| 2026-09-22 | **补 `delete_file` op（写面 2.5 落地）+ 把"库里还剩的既有问题"透传进写后回执**（承接上一轮留下的两点待定） | **① `pre_existing` → 工具回执**：写成功后**重跑** `validate_sidecar`，把"库里还剩的既有问题"（写前就在、与本批无关）如实列进回执（`tools/kb.py` 末尾追加 `_remaining_issues()` + `remaining_issues_text()` + `REMAINING_ISSUES_MAX = 5`），并明写"这些是**用户**的存量问题、**不要**因此重提本批" ⇒ 补上上一轮如实交代的那条缺口（agent 从此看得见"库里还剩哪些既有问题"，而不是只看到自己这批被放行）。**② `delete_file` op**（§7 的 2.5 从"默认关"转为提供）：`plan.OP_DELETE_FILE` 进 `KNOWN_OPS` / `COMPILED_OPS` / `FILE_OPS`；**只读预演** `file_ops.delete_plan()` 算 `ids` = 这次删除会让**指向它的 wikilink 失去落点**的 id（文件 stem **加上它自己侧车里定义的 KP id**，扣除"别处还有一处定义"的 —— 同 `rename_plan()` 的 `kp_shadow` 判据换个方向）+ `referrers_md` / `referrers_sidecar` / `affected`（= 被删正文 + 它自己的侧车，**引用者不入备份集**：删除不改写它们）。**拦/警口径**：正文里仍写着 `[[id]]` 的**别的**文档 ⇒ **硬拦** `delete_referenced`（按**批内视图**再判一次 ⇒ 同批先 `replace_lines` 摘掉引用即可放行，不误杀合法意图）；侧车里还挂着指向它的链接/边 ⇒ 只**警告** `delete_leaves_edges`（派生数据，`validate_kb` 会报出来，清理手段与正文引用不同）。**顺序**：与 `rename_file` 同一条**语义**约束 ⇒ 必须收尾（`delete_must_be_last`，`_check_rename_last()` 同时管两个），它之前的 op 照常可以同批。**落地**：`apply.py` 新增原语 `delete_file`（薄包装 `DocumentService.delete_file()` —— `document.py` **一行未动**，`:782` 锚点零漂移）+ 编译分支（备份集含**它自己的侧车**，否则撤销写不回来）；`approvals.RISKY_OPS` 加 `delete_file`（**风险 op** ⇒ `auto` 档下逐条确认；`all-access` 档不问，但写前备份 + 可整批撤销照旧）；`propose_write` 工具说明补 ⑪（并写明"被拒时怎么改"）；`preview_plan()` 给 `entry.delete={path,ids}` + 路径级 diff（确认卡能显示"删哪篇、哪些 id 会失去落点"）。**③ `kb.file.move` 本轮**仍不做**（**按设计 §6 R2 未改**）**：正文内**相对链接 / 图片路径**不随文件移动改写（`path_cascade` 明确不碰正文），且服务层只有**同目录** `rename_file` 与 `rename_dir` ⇒ 为它新开一条"移动后静默打断相对引用"的写路径与既定结论冲突；等 R2 落地再进工具集（本轮**不动**，只在本行登记）。**验收**：`pytest -q` **897 passed**（`test_agent_file_ops.py` 删掉"删除不提供"那条旧断言、新增 **5 例**：预演列引用者与备份集 / 正文引用被拦且**零写入** / 同批先摘引用再删**放行**且撤销**逐字节还原** / 只能收尾 / 侧车引用只警告 + 预览形状；`test_agent_permission_presets.py` **+1 例**：`delete_file` 命中风险条件）；`node --check` ×3、`scripts/i18n_selftest.js`、`tests/test_doc_anchors.py` **全 PASS**。**L4 真实回合（已完成，2026-09-22）**：新证据 `artifacts/agent/verify/l4-delete/{roundtrip.py,report.json}` —— 本进程内**脚本化 provider**（手写 SSE，不联网）+ `%TEMP%` **临时库**（不碰用户库），驱动真实 `UIAPI.agent_ask_*` 走完整回合：① `read_document notes/target.md` ⇒ ok；② `propose_write` 只提 `delete_file` ⇒ **被拦**（`INVALID_PLAN` / `delete_referenced`，指名 `notes/ref.md` 且给出"放在同一批里、这条删除之前"的恢复指引），**盘上文件仍在**；③ `read_document notes/ref.md`；④ `propose_write` [`replace_lines` 摘掉 `[[target]]`, `delete_file`] ⇒ **已写入**（txid `20260922T102211Z-01`，回执"影响 2 个文件"），`notes/target.md` 真的消失、`ref.md` 引用已摘；⑤ `agent_plan_undo` ⇒ `ok`，**两篇都逐字节还原**（`target_restored` / `ref_restored` 均 true）。**顺带**：第一版脚本忘了先读 `ref.md`，第④步被闸如实拒成 `FS_NOT_OBSERVED` —— 反向证明读后写闸在真回合里确实生效。**如实交代**：① 撤销链路已有单测（`post.json` 记 `existed:false` ⇒ 撤销 = 文件写回）+ 上面的 L4，但"撤销后再**重做**"未单测；② 工具面文案（`⑪`）已改，**真机真实模型**是否会自己采用"先摘引用再删"的两步批仍未验（L4 用的是脚本化 provider，脚本是**照着工具说明**写的，这只证明"说明可执行"，不证明"模型会照做"）。 |
| 2026-09-22 | **修复（真机反馈驱动）：同批「先 `create_file` → 再给这个新文件建点 / 挂链」被读后写闸挡成 `FS_NOT_OBSERVED`** | **来源**：真机实测报告（新建 `tmp-delete-test.md` ⇒ 读回 ⇒ 删除），顺带报出"同批先建后删会被守卫拒绝"。**查证 ⇒ 升级为真 bug**：`observation.write_targets()` 只跳过了 `create_file` **那一行**，没有跳过**它建出来的那个文件** ⇒ 同批后续针对新文件的 op（`upsert_kp` / `attach_links` / 改正文）被当成"没读过就写的**既有**文件"，整批拒。这与 `propose_write` 的工具说明**直接冲突**（「先 create_file 新建 → 再给它建点 / 挂链」都放**同一批**，不要拆批），而既有用例 `test_create_file_may_share_a_batch_with_kp_and_links` **测不到**它 —— 那条走的是 `apply_plan` 直调，**绕过**了落在工具处理器里的闸。**修法**：`write_targets()` 收集本批 `create_file` 建出的路径集合并从目标里扣除（= `plan._View` 的**同一套批内视图**口径：新文件的正文是**模型自己在这一批里给的**，不存在"凭印象改一个没看过的文件"的风险；上游 `write` 的新建路径本就不要求观察）。**同批「先建后删」仍被拒，但拒在**计划校验**（`file_not_found`）而不是闸** —— `delete_plan()` 读的是**盘上**真实文件，而新文件此刻只在批内视图里；那种批是**净零**，拒得合理（真要"建了再删"分两批即可），已在用例里钉成分层口径。**验收**：`pytest -q` **901 passed**（`test_agent_observation_gate.py` **+4 例**：本批新建文件不算"没读过"（`create_file` + `upsert_kp` 同批真落盘）/ 同批先建后删被**计划**拒（非闸）/ 既有文件的删除**照样**要先读（放宽面没被放大）/ `write_targets()` 纯函数面）；`test_agent_file_ops.py` 的既有"新建 + 建点 + 挂链同批"用例（走 `apply_plan`）继续通过 ⇒ 两侧口径一致。**教训（登记）**：新加一道闸，必须同时钉住"闸与**工具说明**是否自洽"，并让用例走**真实工具入口**（`ToolRegistry.invoke`）而不是直调底层 —— 否则闸与说明的分歧无人发现。 |
| 2026-09-22 | **补 `move_file` op（跨目录搬整篇 = `kb.file.move`）＋ 推翻"按 §6 R2 不做"的旧判定**（来源：**产品内 agent 的真机报障** —— 它按目录方案要把 3 篇挪进 `expressions/phrases/`，却发现「写接口不能跨目录搬文件：`rename_file` 只改文件名、保持所在目录，也没有改 sidecar `file:` 字段的接口」，只能提议"人在文件树里拖"或"新建→删旧代搬"） | **① 重新推演 R2（关键判断）**：旧判定「正文内相对链接未随移动改写 ⇒ `kb.file.move` 不进工具集」**过宽** —— ① `[[id]]` / `[[stem]]` 都是 **id / stem 寻址**，移动**不改 stem** ⇒ 引用天然不断（按 id 索引的数据如 `errors.json` 也不受影响）；② 图片引用按规范是**库根相对**（`.memoria/images/x.png`）⇒ 同样不受移动影响。R2 真正管的是**"文件相对"写法**（`![x](img/a.png)`、`[y](../b.md)`）⇒ 落法改为**拦**而非改写：`move_plan()` 用「**按库根解析不到、按文件所在目录解析得到**」这条可判定规则把它们挑出来，写前拒（`move_breaks_relative_refs`）—— 既不带病上线，也不必等 R2 落地。**② 服务层**：`document.py` **末尾追加** `move_file_document(service, rel, target_dir)`（**不做成类方法**：类体铺到文件末附近，插方法会让其下**约 30 处** `document.py:<行号>` 锚点漂移，含 `rename_file`(:583)/`delete_file`(:782)/`create_file`(:794)）= 物理 `os.rename`（**目标目录自动建**，同 `create_file`）+ 补齐**旧版同目录侧车**搬迁（`path_cascade` 只搬镜像布局，不补会留下孤儿 KP）+ `apply_path_move()`（镜像侧车 / `file:` 字段 / pending / manifest）+ 图片注册表增量 + 解析缓存失效 + `invalidate_kp_targets`。**③ 写面**：`plan.OP_MOVE_FILE` 进 `KNOWN_OPS`/`COMPILED_OPS`/`FILE_OPS`；`file_ops.move_plan()` / `apply_move_file()`；`apply.py` 原语 `move_file`（备份集 = **源与目标两侧**正文 + 侧车，否则撤销不完备）；`approvals.RISKY_OPS` 加 `move_file`；工具说明补 ⑫。**④ 顺序规矩一并放宽**（原"文件级 op 只能有一条"会让"一批移 N 篇"被迫拆 N 批）：新增 `PATH_MOVING_OPS` 与 `file_ops_must_be_last` —— **一旦出现 `move_file`/`rename_file`/`delete_file`，其后不得再有非文件级 op**；`move_file` **可以连排多条**（各自动自己那一篇，不改写别人正文），`rename_file`/`delete_file` 仍须**最后一条**；`create_file` **不在其中**（它只是把新文件种进批内视图，"同批建点/挂链"必须继续放行）。**⑤ 顺带**：`path_cascade._relocate_sidecar_file()` 只处理镜像布局 ⇒ `rename_dir()` 有同样的旧版侧车盲点（本轮**只**在 `move_file` 路径补齐）。**验收**：`pytest -q` **961 passed**（`test_agent_file_ops.py` **+8 例**：预演 from/to/两侧备份集与三类拒 / **核心往返**（正文一字未改 + 侧车跟到新镜像路径且 `file:` 更新 + `[[a]]` **不改写** + 撤销逐字节还原）/ 目标目录自动建 / 文件相对引用被拦 / 库根相对引用放行 / 一批连移两条 + 混批报 `file_ops_must_be_last` / 预览 `entry.move` / 工具说明；`test_agent_permission_presets.py` +1 例）；`tests/test_doc_anchors.py` 通过（`document.py` 类体**未动**）。**顺带修掉一个真·脆弱测试**：`test_two_writes_in_the_same_second_do_not_collide` 原先断言"三批 txid 同一秒"（取决于墙钟，本轮实测到一次假失败）⇒ 改断言**真正的不变量**「三个批次目录互不相同」。**真机路径实测**（临时库 + 真工具入口）：`read_document` → `propose_write[move_file]` ⇒ **已写入**（txid `20260922T113527Z-01`）、新位置在 / 旧位置没了 / 引用方正文的 `[[flee-the-nest]]` **原样**。**如实交代**：① 只做**文件级**移动，**目录级移动**（把整个目录挪进另一个目录）仍无（`rename_dir` 只能原地改名）；② 不支持"移动 + 同时改名"（改名用 `rename_file`，两者都要收尾 ⇒ 实际得分两批）；③ 无 L4 实回合（未验真模型自己会不会用 `move_file`）。 |
| 2026-09-22 | **S1 开篇：能力插件「声明面」落地（`services/agent/plugins.py`）** —— 来源 = 人「请你做成一个插件补充视觉能力」（先侦察 `D:\AAA_Courses` 教材库的既有管线 = MinerU 三后端 + `content_list` 元素重建 + 50 余脚本；**视觉能力本体由核心宿主模块承担，插件只做声明与启停** —— 守 §1 铁律「插件目录内不含可执行代码」）。本轮**只做声明面**：`parse_declaration()` 逐条落实 §1「校验规则」列（`v==1` 否则拒载；`id` kebab、**重名 ⇒ 装载失败**；`name` 缺省回落 `id`；`provides.tools[].tool_id` **必须**在核心原语目录；`gate ∈ {always, kb_has_sidecar}`；`permissions.read/write` 必须是**库内相对 glob**（绝对/盘符/上跳一律拒）；**安全下限** `write` 非空 ⇒ `approval.write ≠ auto`；**边界** `permissions.write` 不得含**核心独占**路径 `.memoria/agent/{sessions,backups}/**`）、`load_declarations()`（三来源 `builtin`/`user`/库级内联，同名即失败）、`kb_enablement()` / `set_enabled()`（§1 库级 `enabled[]`：**条目存在即启用**；`on=False` 只置假便于回切；**不整文件覆写**，其它键原样保留）。§2 暂缓字段与未知字段一律**告警但容忍**（守「只增不改」）。**命名桥**：`TOOL_CATALOG`（`kb.*` → `apply.PRIMITIVES` 的代码原语名）单列一处；**设计已列、代码未实现**的两个（`kb.link.create` / `kb.link.set_type`）单列 `PLANNED_TOOLS` ⇒ 专门错误码 `tool_not_implemented`（不混成"不在目录里"）。**同轮查出两处文档漂移（登记待人订正）**：① §1 内置实样第三条写 `kb.link.create`，与同文件 `:306` 的映射表（应为 **`kb.link.attach`**）不一致 ⇒ 已钉成用例 `test_design_exemplar_drift_is_pinned`（原文照抄必须被拒）；② 代码原语 `delete_link_route` **尚无权威 `tool_id`**（设计表未列）。**验收**：新增 `tests/test_agent_plugins.py` **42 例**（含「命名桥两侧都活着」的交叉校验：目录里每个原语名都要在 `apply.PRIMITIVES` 里找得到）；`pytest -q` **1040 passed**。**未做（如实）**：① **zip 导入 / 解包 / 路径穿越与校验和**（属导入器，下一步）；② `gate` 求值与 `constrain` 的应用点（分别在工具门控与 `validate_plan`）；③ 网关 RPC 与「功能导入」UI；④ 视觉插件本体（**PaddleOCR-VL-1.6 主打 + MinerU 可选**、**代码包 + 首次按需下载模型**，人 2026-09-22 拍板）。**待人处理**：`AGENTS.md §1` 需登记三处新事实源（`resources/agent-capabilities/**` 内置声明、`config/plugins/<id>/plugin.json` 用户导入、`<库>/.memoria/agent/capabilities.json` 库级启用位）—— 该文件写权限属人，我不动。台账：`docs/todo.md` **AG24** |
| 2026-09-22 | **库级 op `rebuild_manifest` 落地（§7 的 6.3 / 原语 `kb.manifest.rebuild`）** —— 来源 = 人「我不是要你帮忙修改，而是**要你完善 agent 的能力**」（承接产品内 agent 的真机报告："剩下 49 条 + 2 个错误全是**清单陈旧**，**只能点「构建」**"） | **缺口**：agent 侧此前**一个能碰 `manifest.yaml` 的 op 都没有** ⇒ 报告里最大的一堆账（新文档未记入清单 / 指纹 sha 过期 / 历史改名留下的 `manifest_removed`）只能靠人点按钮。**落地**：`plan.OP_REBUILD_MANIFEST`（**零参数、不带 `file`** ⇒ 与 `rename_kp` 同族：拦在 `_safe_rel()` 之前分流、天然豁免读后写闸）+ `_validate_rebuild_manifest()` + `apply._call_rebuild_manifest()`（薄包装 `DocumentService.sync_manifest()`，**不另写重建逻辑**）+ 命名桥 `kb.manifest.rebuild` + 工具说明 ⑯ + i18n `plan.op.rebuild_manifest`。**三条口径**：① **前置硬闸与「构建」按钮完全同一道** —— `detect_path_moves()` 非空即拒（`manifest_blocked_by_path_moves`），且**挡在计划期**（比落到 apply 才整批回滚好：模型能拿着错误直接改意图：同批先 `move_file`/`rename_file` 把路径定下来，或请人点「修复路径」）；② **必须收尾**（新错误码 `manifest_must_be_last`）—— 它把"这一刻的文件集"写成清单，后继任何 `create_file`/`rename_file`/`delete_file`/`move_file` 都会让它立刻过期，那正是它要消的那类警告；③ **不进 `RISKY_OPS`**（与 §7 该行"建议 `confirm`"**故意偏差**：manifest 是可再生索引、且自带一道更贴问题的前置硬闸；再加逐条确认只会把它变回"等人点"，与"完善 agent 能力"的诉求相反）。**验收**：新增 `tests/test_agent_manifest_op.py` **5 例**（陈旧清单归零 + 撤销逐字节还原 / 路径变更被计划期拒且给"修复路径"指引 / 收尾规矩两向 / **真实工具入口**一次调用即落盘 / 工具说明含 `manifest_blocked_by_path_moves`）；`pytest -q` **1084 passed**（原 1079）；`node --check`×2、`i18n_selftest`、`test_doc_anchors.py` 全 PASS。**未做（如实）**：① 无 L4 实回合（未验真模型会不会主动用它收账）；② `preview_plan()` 对**没有 `file` 的库级 op**（`rename_kp` / `rebuild_manifest`）会把它们归进 `files[].file == ""` 那个桶 ⇒ 确认卡上"路径"显示为空（既有形状问题，本轮未动）；③ 报告里 2 个 error 属"清单记的旧路径"，若 `detect_path_moves()` 也把它们判成 move，本 op 会**如实拒绝**并指向「修复路径」——正确顺序是**先修复路径再重建** |
| 2026-09-22 | **sidecar 结构 op 三件套落地（`upsert_edge` / `delete_kp` / `rename_kp` = §7 的 1.2 / 1.7 / 1.8）+ 修「删行要人点确认」这条摩擦** —— 来源 = 人：「我看到 agent 说它没有改 sidecar 相关操作，怎么还没做，这个是 memoria 维护技能板块的内容」 | **查证：agent 那句是老实话** —— `D:\AAA_Courses\软件工程概论` 的会话日志（`session-20260922T065347Z-2f5e2e75.jsonl`）显示它只做了正文级的 `<details>` 残渣清理；`delete_kp` 当时**不在 `KNOWN_OPS`**（提了就是 `unknown_op`）、`rename_kp` **在表但不在 `COMPILED_OPS`**（提了只发警告、编译不出动作）、纯边 `upsert_edge` **根本没有**。而**库自己的规范**（`kb-spec.zh-CN.md:105`「改 KP id / 删除文件·KP / 批量重组 → 先问用户」、`prompt.zh-CN.md:30`）要求这类动作"先列清单请用户确认"，工具面却没有对应的落地物 ⇒ agent 只能答"我没动 sidecar"。**① `upsert_edge`**（→ 原语 `create_edge` → `document.create_edge()`，`:2885`）：只写 sidecar `edges[]`、**不碰正文**；四条前置在**计划期**判（`source_id` 必须是**本文档**的 KP —— 那是 `create_edge()` 的硬约束 / `target_id` 全库可解析 / `edge_type` 只收 `reference`·`extend`（`contain` 由标题层级推导、禁手标）/ 同源同目标不重复建 ⇒ 计划期就报 `edge_exists`，不必等落盘整批回滚）。**完全自包含** ⇒ **不进** `RISKY_OPS`。**② `delete_kp`**（→ `kb.kp.delete` → `document.delete_kp()`，`:1875`）：只删 sidecar 里那一条、正文一字不动；别处仍指向它的引用（正文 `[[id]]` 与各侧车）**只警告** `delete_leaves_dangling`（点名篇数与前 5 篇 + 可行动的清理手段）—— 库规明确允许悬空虚链（`kb-spec` §4），这与 `delete_file` 对正文引用**硬拦**是**有意不同的分层**（那里整篇消失，这里只少一个可解析目标）。新增只读扫描 `file_ops.kp_referrers()`（文件末追加，零漂移；探针用 `replace_link_id_in_markdown` / `migrate_sidecar_kp_refs` ⇒ 与落地同一套判据）。**审批**：进 `RISKY_OPS`（影响面超出本文档）。**③ `rename_kp`**（→ `kb.kp.rename` → `document.rename_kp_id()`，`:1937`）：全库改 KP id（正文 wikilink + **各侧车**的 `knowledge_points[].id` / `edges` / `links` 引用一起改）。为它给 `kp_rename.rename_kp_in_kb()` 新增 **`dry_run`**（**同一份实现**、只是不落盘；dry-run 里**跳过 per-file `validate_sidecar`** —— 正文此刻还是旧 id、内存 sidecar 已指向 new_id 会报假冲突，而真正的硬闸 old/new 唯一性在进循环前就用 `build_kp_index()` 判完了）⇒ 校验期给出的 `md_files`/`sidecar_files` **就是** apply 的 pre-image 备份集，撤销能**逐字节还原全库**。它**没有 `file` 字段** ⇒ 必须拦在 `_safe_rel()` 之前（同 `OP_CREATE_FILE` 的位置理由），也因此**天然豁免读后写闸**（`observation.write_targets()` 按 `file` 收集目标）—— 那道闸管"别盲写你没读过的那个文件"，全库级联不可能要求读完每一篇；它的闸是**审批卡**（进 `RISKY_OPS`）+ **必须收尾**（并入 `PATH_MOVING_OPS`，其后只允许文件级 op，理由 = 它改写正文而**批内视图不跟着改**）。**④ 修「删行要人点确认」**（人同轮点名）：`delete_lines` **移出** `RISKY_OPS` —— 判据是"不可逆"，而它有逐字 `expect` + 写前备份 + 整批撤销，可逆性与 `replace_lines`（一直免问）**没有区别**；设计里"不设 `auto`"针对的是**删除类**（`kb.file.delete`/`kb.kp.delete`），不是"改正文"。**真机后果**（同一次会话实测）：连续 **3 次** `propose_write` 被拒，每次都是**挂满 `APPROVAL_TIMEOUT_S`(120s)** 才判 `unavailable`（时间戳差 120.2s）—— 整理教材残渣这类**纯删行**任务每批都弹确认卡、没人点就死等 120 秒。**⑤ 命名桥同步**：`plugins.TOOL_CATALOG` 加 `kb.link.create`（→ `create_edge`）/ `kb.kp.delete` / `kb.kp.rename`；`PLANNED_TOOLS` 收缩为只剩 `kb.link.set_type`（"原地改边类型"代码侧仍无等价原语，只有删了再建）⇒ 原先那条"实样原文照抄必须被拒"的漂移钉子**到期**，改成 `test_link_create_and_attach_are_different_actions`（两个 id 各归各的原语；实样原文现在**装载得过**）。**⑥ 工具面与前端**：`propose_write` 说明补 ⑬⑭⑮（含"纯边 vs 挂链"与"改 id ≠ 改显示名"的分界）+ 收尾规矩那句补 `rename_kp`；两份 i18n 的 `plan.op.*` 从 3 条补到 15 条（原先 9 个 op 在确认卡上显示**裸英文动词**）。**验收**：新增 `tests/test_agent_kp_ops.py` **21 例**（纯边往返 + 撤销逐字节 / 五类拒 / 重复边 / 删 KP 的警告与无警告两况 + 撤销 / 未知 id / 全库级联往返 + **撤销还原全库** / 两类拒 / 顺序规矩两向 / 观察闸与 `write_targets()` 面 / `preview_plan` 形状与零落盘 / `kp_referrers` 纯函数面 / **真实工具入口 3 例**：工具说明含三个新 op、读后写闸下的工具往返、`rename_kp` 不需要先读）；`pytest -q` **1068 passed**；`test_agent_plugins.py` 的 `kb.link.create` 用例改写；`test_agent_permission_presets.py` 两处改用 `delete_file` 当风险样例；`py_compile`、`node --check`、`i18n_selftest`、`test_doc_anchors.py` 全 PASS。**未做（如实）**：① **`merge_kp` 仍未做** —— 本地只有建议面（`suggest_kp_merge`），**没有合并动作的服务实现**，且"合并后用哪个 id / range / description / tags 怎么合一"属**产品语义**，不该由我替你定 ⇒ 留待拍板；② 无 L4 实回合（未验真模型会不会用这三个 op、也未验真机确认卡观感）；③ `kb.link.set_type`（原地改边类型）仍无原语；④ 目录级移动（`move_dir`）仍未做（人此前明确选择不做）。 |
| 2026-09-22 | **思考行三处小改：撤掉摘要预览文字 / 正文一律走渲染 / 未展开时可在气泡内滚轮**（人三句：「思考过程旁边不用展示文字」；「而且思考过程里面要渲染，不是裸文本」；「思考过程没展开鼠标在气泡内区域应该可以滚轮滚动内部信息」） | **① 撤除摘要预览文字**：`thinkEl()` 不再建 `-agent-think-tip`（摘要行只剩标签 + 自画三角）；`updateThinkRowEl()` 里对它的同步逻辑、以及随之**失去调用点**的纯函数 `thinkSummaryLine()`（连它那条 node 实跑单测）一并删除；`app.css` 里 `.-agent-think-tip` 的两条规则作废（原地留注释，**等量占位**保下方锚点）。**② 思考正文一律走渲染**：原先"流式期间用 `textContent` 顶一下、收尾才走 markdown"两分叉 ⇒ 生成中用户读到的是 `**` 这类记号；现改为**同一路** `renderAssistantBody(body, shown)`（`shown = text || T("agent.thinkLive")`）+ `body.__thinkText !== shown` 去重 —— 与助手正文的流式**同口径**（那边本就是每次增量重渲染，见 `renderAssistantBody(streamingEl, parts.safe)`）。**③ 未展开时可在气泡内滚轮**：`.-agent-think-body` 的 `overflow: hidden` → `overflow-y: auto`（+ `scrollbar-width: thin`）⇒ 超出 `max-height: 8rem` 的部分**不再被裁掉**，鼠标悬在正文上即可滚轮翻看；气泡仍由 `max-height` 截断、「展开全部」按钮仍由 flex `flex: 0 0 auto` 钉在底边（**不影响** 上表 2026-09-22 那两处真机几何修复）。**验收**：`pytest -q` **1100 passed**；`tests/test_agent_panel_token.py` 改建 1 条（摘要行只有标签、`-agent-think-tip` 与 `thinkSummaryLine` 在面板源码里**彻底不存在**、`.-agent-think-body` 末条声明为 `overflow-y: auto`）+ 新增 1 条（流式也走渲染的机械钉子）；`node --check`、`scripts/i18n_selftest.js` 全 PASS。**L4 实渲染（harness 8645 + `docs/example/AAA_Vocab` 临时副本，只读回放同一份 17 轮会话）**：109 条思考行里 `-agent-think-tip` **0** 个、`summary` 子节点数 **1**（文案「思考过程」）、三角是 `::before` border 三角（折叠 `transform: none` / 展开 `rotate(90deg)`）；三条最长思考行正文 `markdown-body = true`、渲染出的元素 **1525 / 1030 / 822** 个（155k~362k 字符级）、`overflow-y: auto`、`scrollTop` 可置 **150**（= 真滚动容器）、按钮高 **19px** 且**在气泡可见带内**（距底 7px）；气泡 `rgb(51,51,51)` / 圆角 6px / 折叠 `max-height 128px` → 展开 `none`。**如实交代**：① 残余裸 `**` 仍存在（三条分别 29 / 654 / 640 处，**绝大多数落在 `code`/`pre` 内** —— 模型复述的 `docs/**` glob 与代码块原文），另有少量是模型自己写了**不成对**的 `**`（同段里的 `docs/**` 把后面的 `我**没动它**` 配成了强调边界）⇒ 属**源文本/CommonMark 配对**问题，与助手正文走同一条管线、表现一致；② **实时流式**下的"边生成边渲染"未在真 UI 跑（harness 无模型端点），证据 = 源码钉子 + 回放与实时**共用**同一渲染器；③ 仍**无截图人眼复核**；④ 同一份口径另一处过期事实：本节 2026-09-21 行里的"摘要（流式取**末行**、定稿取**首行**）"**自本轮起作废**（摘要预览文字已整体撤除），历史行不追改、以本行为准。 |
| 2026-09-22 | **思考气泡「展开」只变高一点（`--full` 留上限）** —— 来源 = 人：「思考气泡要滑倒最底下才能收起很麻烦，不全部展示，改成展开后变高一点」 | **问题**：`.-agent-think-bubble--full` 是 `max-height: none` ⇒ 展开后气泡等于全文（同一会话最长三条实测 **90149 / 75601 / 55304 px**），而收起按钮**钉在气泡底边**（上一轮为"按钮不被裁"刻意做的 flex 布局）⇒ 想收起得先把对话栏滑到那几万像素后的末尾。**改法（1 行常量 + 1 个文案）**：`--full` 由 `none` 改 **`20rem`**（8rem ⇒ 20rem ≈ 2.5 倍；正文仍 `overflow-y: auto` ⇒ 看不完的部分在气泡内滚动，**不丢内容**）；`agent.thinkMore` 由「展开全部 / Show all」改「**展开 / Expand**」（它不再声称"全部"）。**验收**：`pytest -q` **1100 passed**（`test_agent_panel_token.py` 那条改写："折叠态 8rem / 展开态 20rem / **两态都不许出现 `max-height: none`**"+ 两份语言包的文案钉子）；`node --check`、`i18n_selftest` 全 PASS。**L4 实渲染（harness 8646 + 临时副本，真实鼠标点击：先点摘要开气泡 → 再点「展开」）**：`isFull = true`、`computed max-height = 320px`、气泡高 **128 → 320**、文案 →「收起」、正文 `clientH 283 / scrollH 2902`、按钮距气泡底 **7px**、**按钮在对话栏可视带内**（`592~611` vs 底 846）。**如实交代**：① 「20rem」是**取值判断**（不是唯一解）—— 想要更高/更矮改这一行即可；② **实时流式**下未在真 UI 跑（harness 无模型端点）；③ 仍**无截图人眼复核**；④ 本轮取代了本节 2026-09-22 前一条里的「`128px → none`」与「展开全部」两处描述（历史行不追改）。 |

---

## 6. 迁移清单（自 [agent-capabilities.md](./agent-capabilities.md) 迁入本文）

> **兜底规则（迁移完成前一律适用）**：凡两份文档**冲突**处，**以本文为准**；本节标 `未迁` 的内容**仍以 `agent-capabilities.md` 原文为工作依据**（本文只登记"要迁什么"，不复制正文 —— 禁并行事实源）。
> **本轮（2026-09-20）不搬任何正文**：先讨论 §7 的写场景；迁移动作由人逐步发起，每迁一节把该行改为 `已迁` 并在本节末尾登记日期。
> **编号纪律**：本文 §1–§5 **不复用、不重排**；迁入内容一律**追加**为 §9 起的**新节**（或并入现有新节），**不插进 §1–§5**。若某节迁入时确需重排 `agent-capabilities.md` 的编号，**旧 → 新编号映射必须写在本节该行里**。

| # | 来源（`agent-capabilities.md`） | 要迁入的内容（一句话） | 状态 |
|---|---|---|---|
| 1 | §2.1.1 | 三层术语（**原语 / 领域动词 tool / 技能 skill**）与"领域动词只产出 plan、不落盘"铁律 | 未迁 |
| 2 | §2.2 | **注册与装载**：两个位置（内置声明 `resources/agent-capabilities/<id>.json` / 库级 `<kb>/.memoria/agent/capabilities.json`）、发现顺序、冲突与重名、**不变量「新增插件不改核心」**、UIAPI 只加一个通用网关 | 未迁 |
| 3 | §2.3 | **写管线**六步（propose → dry-run diff → 逐条确认 → apply → 审计 → 撤销）+ 四条不变口径 | 未迁 |
| 4 | §2.3.1 | **唯一写者**：四原语（`save_document` / `save_sidecar_for_md` / `touch_manifest_entry` / `sync_pending_for_file`）+ realpath 前缀校验 | 未迁 |
| 5 | §2.3.2 | **备份与撤销**：pre-image 时机与粒度、`<txid>` 目录与格式、保留口径（5 批 / 10 会话 / 64 MiB）、撤销与外部改动保护 | 未迁 |
| 6 | §2.3.3 | **计划 API**：`{v,txid,intent,ops[]}` 信封、op 表与校验规则、**拒整批**失败语义、需新增的最小能力清单 | 未迁 |
| 7 | §2.3.4 | **编译器与校验器**：`validate_plan` / `preview_plan` / `resolve` / `audit_kb` 四个只读面 + "同一套校验器、两个消费者"硬不变量 | 未迁 |
| 8 | §2.4 | **权限与越界**：权限矩阵（含 `backups/**`、`images/**` 行）、违约硬拒 + 审计 + 零部分写、路径逃逸判定 | 未迁 |
| 9 | §2.5 | **W/N/S/H 四线如何落成插件族**（族 ≠ 枚举值；"能插的是声明与权限，不能插的是执行体"） | 未迁 |
| 10 | §2.6 | **M3 分期与验收门**：M3a/M3b 切片表 + **四条安全门** + 首批写原语目录 + "明确不做"清单 | 未迁 |
| 11 | §2.7 | **与上游 dsh 的关系**：借什么 / 本地发明什么（含"沙箱升级不吃、用声明式 `permissions` + realpath 替代"） | 未迁 |
| 12 | §8 阶段建议 | 与写模块相关的两行：**W1 = M3a**（计划 API 最小闭环）、**W2 = M3b**（其余 op + 撤销 + 权限档） | 未迁 |
| 13 | §9 风险 | 与写模块相关的四条：**R1**（写能力毁用户库）、**R2**（正文内相对链接未随文件移动改写 ⇒ 影响 §7 的 2.6 / 4.5）、**R4**（skill/插件变成任意代码执行面）、**R8**（装载器成为新的越权写入面）。**R2 的 2.6 那一半 2026-09-22 已处置**：文件移动按"**拦**掉文件相对引用"落地（`move_breaks_relative_refs`）⇒ `kb.file.move` 已进工具集（见 §7 2.6 行 + §8 依赖表）；**4.5（图片改名）仍待** | 未迁 |
| 14 | §10 待拍板 | 与写模块相关的项：**P1**（确认交互）、**P2**（批量提议）、**P7**（注册表承载位置）、**P8**（首批 op 个数，本文 §4 Q3 已引）、**P9**（`approval` 可否覆写，本文 §4 Q4 已引）、**P10**（撤销口径，本文 §4 Q5 已引）、**P11**（权限档是否随 M3b）、**P12**（plan 版本，本文 §4 Q6 已引）；**P3–P6 属 N/S/H 与基准线，不迁** | 未迁 |
| 15 | §0 / §1 中与写模块相关行 | §0 红线的"写前必留 pre-image、备份失败即不写"与"插件边界物理可证"两处；§1 现状盘点的"能力面极窄"结论（作为 §7 现状表的背景） | 未迁 |

---

## 7. 写场景清单（讨论中，2026-09-20）

> **这一节是本轮的主交付：只做讨论材料，不预设结论。**每行的「审批档」一律是**建议**且**待定**；「归属」是**建议切片**，不是承诺。
> **取证口径**：现状列全部**读码得到**（`file:line` 以仓库根为基准；`ui.py` = `src/memoria/presentation/api/ui.py`；前端 = `src/memoria/ui/static/app/**`）。渲染/书写格式的**语法权威**是 [preview-formats.md](../reference/preview-formats.md)，人 UI 的写入路径取证见 [agent-guide](../reference/agent-guide/README.md)。
> **两个反复出现的"底座"（本节多处引用）**：
> - **通用行级原语**（暂定 `kb.file.edit`）：按行区间替换/插入/删除正文行 —— 今天只存在**整篇**入口 `DocumentService.save_document`（`document.py:317`；RPC `ui.py:200`），**没有**行级/区间级入口。
> - **整块原语**（暂定 `kb.block.upsert`）：按块类型（code / table / math / mermaid）重建某个块 —— 今天**没有后端入口**，块编辑面板在前端用 `blockEl.textContent` + 下拉选择重建源码行（`edit-handler.js:1710-1744`）。

| # | 场景（用户 / AI 想做什么） | 今天 Memoria 的支持现状（`file:line` 或"无"） | 落到哪个 op / 需要哪个原语 | 是否需要**新原语**（暂定名） | 审批档（建议，**待定**） | 归属（建议） |
|---|---|---|---|---|---|---|
| 1.1 | 连边：把正文某处 `[[…]]` 挂到某个 KP | ✅ 有：`apply_link_instances`（`document.py:2493`，RPC `ui.py:564`）；目标不可解析即拒（`document.py:2536` 扫描 + 逐目标校验）；单点解挂 `detach_link_instance`（`document.py:2428`，RPC `ui.py:537`） | `attach_links` / `detach_links` → 薄包装原语 `kb.link.attach` / `kb.link.detach` | 否（原著 M3a 已定为新增薄包装） | `confirm` | **M3a** |
| 1.2 | 连边：建 KP↔KP 的**纯边**（不写正文） | ✅ 有：`create_edge`（`document.py:2885`，RPC `ui.py:780`），只写 sidecar `edges[]`；`reference` / `extend` 两型（`contain` 只由范围嵌套推导，`edge_types.py:61-66`） | `upsert_edge` → 原语 `kb.link.create` / `kb.link.set_type` | 否（原著 §2.6 附表已列）—— **2026-09-22 已落地**（op `upsert_edge`）：四条前置在**计划期**判（source 必须是**本文档**的 KP / target 可解析 / 边类型只收 `reference`·`extend` / 同源同目标不重复建）；`kb.link.set_type`（**原地改边类型**）仍留待拍板 —— 代码侧只有"删了再建"（`document.delete_edge`），没有等价原语。**审批**：`upsert_edge` 完全自包含（只往本文档 sidecar 加一条边）⇒ **不进** `RISKY_OPS` | `confirm` | **已落地**（2026-09-22） |
| 1.3 | 创建知识点（建 KP + 锚定 range） | ✅ 有：`confirm_kp_range`（`document.py:1360`，RPC `ui.py:220`）；起止行**非空**硬校验（`:1386-1399`）、跨库 id 唯一复核（`:1414-1419`）、终校 `validate_sidecar`（`sidecar_validate.py:133`） | `upsert_kp` → 原语 `kb.kp.create` | 否 | `confirm` | **M3a** |
| 1.4 | 改知识点：名称 / 区间 / 描述 | ✅ 有：`update_kp`（`document.py:1722`，RPC `ui.py:296`）改 name/description；`confirm_kp_range` 改区间；`set_kp_range` 属 M3b | `upsert_kp` / `set_kp_range` → 原语 `kb.kp.update` | 否 | `confirm` | M3a（名称）· M3b（单独调区间） |
| 1.5 | 完善 tag（选 tag / 加候选 / 调别名） | ✅ 有：`update_kp`（`document.py:1722`）整体覆写 `tags` / `tag_candidates` / `aliases` / `alias_candidates` / `description_candidates`（`:1769-1820`）；建议侧 `suggest_tags_api`（`document.py:1515`）、`suggest_description_api`（`:1544`）**只给建议** | `upsert_kp`（`tags`/`aliases` 字段） | ⚠️ **建议新增** `kb.kp.tag.set`（**增删单个 tag** vs 整体覆写，待定） | `confirm` | M3a（整体覆写）· M3b（增量） |
| 1.6 | 合并近重复 KP | ⚠️ **只有建议、无动作**：`suggest_kp_merge`（`document.py:1497` → `search_kernel.py:136`）；人 UI 也只展示建议（[05 §5 第 3 条](../reference/agent-guide/05-knowledge-points.md)） | `merge_kp`（第二批） | ✅ **需要**：`kb.kp.merge`（新 id + 重指 `links`/`edges` + 删源 KP） | `confirm` | M3b |
| 1.7 | 删除知识点（只删配置、不改正文） | ✅ 有：`delete_kp`（`document.py:1875`，RPC `ui.py:336`）；同时解除 `links[].source_id` 引用（`:1892-1896`） | `delete_kp` | ✅ **需要**：`kb.kp.delete` —— **2026-09-22 已落地**：别处仍指向它的引用（正文 `[[id]]` 与各侧车）**只警告**（`delete_leaves_dangling`）**不拦** —— 库规明确允许悬空虚链（`kb-spec` §4），这与 `delete_file` 对正文引用硬拦是**有意不同的分层**（那里整篇消失，这里只少一个可解析目标）。**审批**：进 `RISKY_OPS`（影响面超出本文档） | `confirm`（删除类建议不设 `auto`） | **已落地**（2026-09-22） |
| 1.8 | 改知识点名称 / id（**全库级联**） | ✅ 有：`rename_kp_id`（`document.py:1937` → `services/kp_rename.py`；RPC `ui.py:342`），影响面全库 | `rename_kp` | ✅ **需要**：`kb.kp.rename`（预览须给受影响文件清单，`services/kp_index.py:37` 的 `build_kp_index`）—— **2026-09-22 已落地**：`rename_kp_in_kb()` 新增 **`dry_run`**（同一份实现、只算不写）⇒ 校验期给出的清单**就是** apply 的 pre-image 备份集（撤销可逐字节还原全库）。**`rename_kp` 没有 `file` 字段** ⇒ 天然豁免读后写闸（那道闸管"别盲写你没读过的那个文件"，全库级联不可能要求读完每一篇）；它的闸是**审批卡**（进 `RISKY_OPS`）+ **必须收尾**（并入 `PATH_MOVING_OPS`，其后只允许文件级 op） | `confirm` | **已落地**（2026-09-22） |
| 1.9 | 处理待确认提议（确认 / 忽略 / 刷新） | ✅ 有：`sync_pending_for_file`（`storage/pending.py:273`）、`sync_kb_pending`（`:172`）、`dismiss_pending_item`（`:317`）；确认仍走 `confirm_kp_range` | `confirm_pending` / `dismiss_pending` | ⚠️ **建议新增**：`kb.pending.confirm` / `kb.pending.dismiss`（或复用 `upsert_kp` + 一个 dismiss 原语） | `confirm`（dismiss 建议 `confirm`） | M3b |
| 2.1 | 改正文段落（替换若干行） | ✅ 有**整篇**入口 `save_document`（`document.py:317`；RPC `ui.py:200`；tmp + `os.replace` `:341-347`，保留 frontmatter `:337`）；**无行级入口** | `replace_lines` → **通用行级原语** `kb.file.edit` | ✅ **需要** | `confirm` | M3b |
| 2.2 | 插入新段落（不覆盖他人内容） | ⚠️ 只能整篇重写（`save_document` `document.py:317`）；无插入原语 | `insert_block` / `insert_lines` → 复用 `kb.file.edit` | ✅ **需要**（若 `kb.file.edit` 已按行区间设计则可复用） | `confirm` | M3b |
| 2.3 | 删除段落 | ⚠️ 同 2.2：只能整篇重写 | `delete_lines` → 复用 `kb.file.edit` | ✅ **需要**（同上） | `confirm` | M3b |
| 2.4 | 新建 `.md` 文件 | ✅ 有：`create_file`（`document.py:794`；RPC `file_create` `ui.py:149`；父目录自动创建、自动补 `.md`、`touch_manifest_entry`） | `create_file` → 原语 `kb.file.create` | 否（原著附表已列，M3a 不进工具集） | `confirm` | M3b |
| 2.5 | 删除整文件（`.md` + sidecar） | ✅ 有：`delete_file`（`document.py:782`；RPC `file_delete` `ui.py:143`） | `delete_file` → 原语 `kb.file.delete` | 否（原著附表已列）—— **2026-09-22 已提供**（§5 末行）：风险 op + 正文悬空引用先拦（`delete_referenced`，可按批内视图消解）/ 侧车引用只警告 / 只能收尾 / 备份含其侧车 | `confirm`（**不设 `auto`**：`approvals.RISKY_OPS` 已含 `delete_file`） | **已落地**（M3b） |
| 2.6 | 重命名 / 移动文件 | ✅ 有：`rename_file`（`document.py:583`；RPC `file_rename` `ui.py:137`）——**含全库 `[[stem]]` 改写级联**（`:629-648`）与 sidecar/manifest/pending 迁移；目录重命名 `rename_dir`（`:697`，RPC `ui.py:161`）；**移动**（跨目录、保持文件名）由 `document.py` 末尾追加的 `move_file_document()` 提供（与 `rename_dir` 同一套 `apply_path_move` 级联） | `rename_file` → 原语 `kb.file.rename`；**新增** `move_file` → 原语 `kb.file.move` | `kb.file.move` **2026-09-22 已提供**（见 §5 本日末行）：改名用 `rename_file`、**换目录**用 `move_file`；R2 那条按"**拦**而非改写"落地（正文里的文件相对引用会被 `move_breaks_relative_refs` 拦下） | `confirm`（`move_file` 已进 `RISKY_OPS`） | **已落地**（M3b） |
| 2.7 | 改 frontmatter | ❌ **无**：`save_document` 只**原样保留** frontmatter（`document.py:337` `strip_frontmatter` → `compose_markdown`），没有写 frontmatter 的入口；且 KP 元数据权威是 sidecar、frontmatter **不参与解析**（[05 §5 第 10 条](../reference/agent-guide/05-knowledge-points.md)） | `set_frontmatter` → 原语 `kb.file.frontmatter.set` | ✅ **需要**（且需先定"frontmatter 与 sidecar 谁是权威"，[05 篇] 已判 sidecar） | 建议 `confirm`（**待定**） | 后线 |
| 2.8 | 新建 / 重命名目录 | ✅ 有：`create_dir`（`document.py:812`；RPC `dir_create` `ui.py:155`）、`rename_dir`（`document.py:697`；RPC `dir_rename` `ui.py:161`） | `create_dir` / `rename_dir` | ✅ **需要**：`kb.dir.create` / `kb.dir.rename` | `confirm` | M3b |
| 3.1 | 字体样式：粗体 / 斜体（md 原生） | ✅ 有：`format_text`（`document.py:2078`，RPC `ui.py:548`）支持 `bold`（`**…**` `:2098`）与 `italic`（`*…*` `:2100`）；**无**删除线、**无**行内代码 | `apply_style` → 原语 `kb.style.apply` | ✅ **需要**（今天只有人 UI 单行/单列区间调用，无 agent 面） | `confirm` | M3b |
| 3.2 | 字体样式：高亮 / 字色（**本地扩展语法**） | ✅ **是本地扩展、且可渲染**：`[[\h\|…]]` / `[[\h:bg\|…]]` / `[[\h:bg:fg\|…]]` / `[[\c:color\|…]]`（[preview-formats.md §4.2](../reference/preview-formats.md)）；写入侧 `format_text` 覆盖 `highlight`（`:2102-2107`）与 `fontcolor`（`:2108-2110`） | `apply_style` → `kb.style.apply` | ✅ **需要**（同上；注意 `[[\…]]` **不嵌套**，`]]` 按行内第一个收尾） | `confirm` | M3b |
| 3.3 | 字体样式：字号 / 下划线 / 上标 / 下标 | ⚠️ **能渲染、不能写**：`[[\s:size\|…]]` / `[[\u\|…]]` / `[[\sup\|…]]` / `[[\sub\|…]]` 只存在于**渲染侧**（`renderer.js:352-384`；语法见 [preview-formats.md §4.2](../reference/preview-formats.md)）；**`format_text` 的 `format_type` 枚举里没有它们**（`document.py:2098-2112`）⇒ 今天连人 UI 也无法应用这几种 | `apply_style` → `kb.style.apply`（扩枚举） | ✅ **需要**（且**先要补** `format_text` 的 `format_type`，否则连人 UI 也不支持） | `confirm` | 后线（先补人 UI） |
| 3.4 | 字体样式：**sidecar 记录样式**（带名高亮等） | ❌ **无**：全库 `src/**` 无 `highlights` 写入/校验路径（`sidecar_validate.py` 只校验 `knowledge_points` / `edges` / `links`，`sidecar_validate.py:180/283/327`）；`markdown-form-std.md:78` 与 `preview-formats.md §4.2` 提到的 sidecar `highlights[]` 在当前代码中**没有实现**（⚠️ 既有文档与代码的又一处不一致，如实记录） | 无 op（先定契约） | ✅ **需要**：**新存储**（sidecar `styles[]` / `highlights[]` 的字段契约）；按 [AGENTS.md §1](../../AGENTS.md) 若成为事实源须**先由人登记** | 建议 `confirm`（**待定**） | 后线 |
| 3.5 | 字体样式：整文级字号 / 字色 | ❌ **不是内容**：字号是**视图偏好** `--preview-font-size`，由「设置 → 显示 → 文字」滑块写 `ui-settings.json`（[preview-formats.md §5](../reference/preview-formats.md)；`display-settings.js:79-85`） | —（无 op） | ❌ 不建议 | — | **不建议给 agent 做** |
| 4.1 | 插入图片引用（正文新增 `![](…)` 行） | ⚠️ **仅前端**：`image-tools.js:108-161` 按锚点插入源码行（`![alt](<relPath>)`，尖括号包裹 `:83-90`）；**无后端入口** | `insert_image_ref` → 复用**通用行级原语** `kb.file.edit` | ✅ **需要**（行级原语）；另需 `kb.image.import` 入库 | `confirm` | M3b |
| 4.2 | 图片登记进 `.memoria/images/**`（与 manifest） | ⚠️ **部分**：`import_image`（`document.py:833`；RPC `ui.py:851`）做扩展名白名单 + **MD5 内容去重** + 重名编号，**只复制文件**；manifest **不索引图片**（`manifest.py:47 entry_for_md` 只对 `.md` 取值）；图片另有**独立注册表** `registry.json`，由 `rebuild_image_registry`（`document.py:1014`）重建、`_update_registry_for_doc`（`:1027`）增量维护 | `import_image` → 原语 `kb.image.import` | ✅ **需要**：`kb.image.attach`（入库 + 登记注册表）。⚠️ 澄清：**manifest 里没有图片条目**，别把"登记进 manifest"当成既有能力 | `confirm` | M3b |
| 4.3 | 改图片说明 / 替代文本（alt） | ⚠️ **仅前端**：`image-tools.js:515-519` 只改 `![…]` 内的 alt（保留 url 与 title），直接整行替换源码；**无后端入口** | `set_image_alt` → 复用 `kb.file.edit` 或 `kb.image.set_alt` | ✅ **需要**（可复用行级原语） | `confirm` | M3b |
| 4.4 | 改图片属性（`width` / `align` / `name-size` / `name`） | ⚠️ **仅前端**：`image-tools.js:486-512` 解析行尾 `"k=v,…"` 做**键级合并**（保序、保留未知键）后整行替换；语法权威见 [preview-formats.md §4.3](../reference/preview-formats.md) | `set_image_attrs` → 复用 `kb.file.edit` 或 `kb.image.set_attrs` | ✅ **需要**（可复用行级原语） | `confirm` | M3b |
| 4.5 | 移动 / 重命名图片资产（改路径） | ❌ **无**：图片侧只有 `import_image`（增）、`cleanup_unused_images`（`document.py:1104`，删）、`fix_unregistered_image_refs`（`:1236`，改引用格式）；**没有**改图片文件名的动作 | `move_image` → `kb.image.move` | ✅ **需要**（且需同步全部引用正文，属 [§6 第 13 行 R2] 未落地项） | `confirm`（**待定**） | 后线 |
| 4.6 | 删除图片引用（仅删引用行 / 删未引用文件） | ⚠️ **部分**：前端"删除引用"只删当前文档的引用行（`image-tools.js:410-436`，别处仍引用则**拒绝**）；真删磁盘文件走 `cleanup_unused_images`（`document.py:1104`，RPC `ui.py:872`）**不可恢复** | `detach_image_ref` / `delete_image` → `kb.image.detach` / `kb.image.delete` | ✅ **需要** | "仅删引用"建议 `confirm`；"真删文件"建议 `confirm` 且**默认关**（同 `kb.file.delete` 口径） | M3b |
| 4.7 | 修复"已引用但未注册"的图片引用（补尖括号） | ✅ 有：`diagnose_image_refs`（`document.py:1168`，RPC `ui.py:886`）+ `fix_unregistered_image_refs`（`document.py:1236`，RPC `ui.py:893`），把裸 URL 改写为 `<…>` 形式 | `fix_image_refs` | ⚠️ **建议新增**（可复用行级原语做等价改写） | 建议 `confirm`（**待定**，可用 dry-run diff 先行） | M3b |
| 4.8 | 清理未引用图片（真删文件） | ✅ 有：`unused_images`（`document.py:1082`）/ `cleanup_unused_images`（`:1104`）；另有**后台自动**路径 `image_registry_auto_check`（`:1047`，6 小时宽限 `:1071`） | — | ❌ 不建议 | — | **不建议给 agent 做**（见 §8 理由） |
| 5.1 | 插入 / 更新代码块（含语言标注） | ⚠️ **仅前端**：块编辑面板 `_BLOCK_TOOLS`（`edit-handler.js:1169` 起；语言下拉读取 `:1599-1604`）在退出块编辑时重建围栏行（`:1734-1740`）；**无后端入口**。渲染为 `<pre class="-code-block">` + `language-<lang>`，**无语法高亮**（[preview-formats.md §2](../reference/preview-formats.md)） | `upsert_block(kind=code, lang)` → **整块原语** `kb.block.upsert` | ✅ **需要** | `confirm` | M3b |
| 5.2 | 表格（新建 / 改单元格 / 加行列） | ❌ **人 UI 也不支持写**：块编辑的「+行 / +列」按钮（`edit-handler.js:1217-1218`）**无任何事件绑定**，退出块编辑时表格分支**显式不处理内容更新**（`edit-handler.js:1712-1714`）、且不标脏（`:1784`）；渲染侧支持 `<table>`（`renderer.js:207-235`） | `upsert_block(kind=table)` → `kb.block.upsert` | ✅ **需要**（且**先要补人 UI 的表格写回**） | `confirm`（**待定**） | 后线（先补人 UI） |
| 5.3 | 公式块 `$$…$$` | ⚠️ **能写但已知有缺陷**：块编辑退出时用 `blockEl.textContent` 取内容（`edit-handler.js:1710-1717`）重建 `$$…$$`（`:1741-1744`），而该 DOM 在 MathJax 排版后已被 `mjx-container` 替换 ⇒ 写回内容可能不是原始 TeX（[04 §10 第 1 条](../reference/agent-guide/04-preview-and-rendering.md)，**待运行时确认**） | `upsert_block(kind=math)` → `kb.block.upsert` | ✅ **需要**（且**先修人 UI 的写回缺陷**） | `confirm`（**待定**） | 后线（先修缺陷） |
| 5.4 | `<details>` 折叠块（及任意原始 HTML） | ❌ **不支持**：正文里的原始 HTML 标签**按字面文本显示、不生成 DOM**（[preview-formats.md §2](../reference/preview-formats.md)）；仅**渲染侧**对用户自己写的 `<details>/<summary>` 加了三角样式（`agent-panel.js` 末尾块，[04 §7](../reference/agent-guide/04-preview-and-rendering.md)） | — | ❌ 不建议（要先改渲染器放行 HTML） | — | **不建议给 agent 做** |
| 5.5 | Mermaid 图块 | ⚠️ **仅前端**：类型下拉（`edit-handler.js:1184` 起）；源码走 `code_block + lang=mermaid` 归一（`edit-handler.js:1332-1333`），重建与代码块同一分支（`:1734-1740`）；后端无入口。失败给红框（`preview.mermaidFail`） | `upsert_block(kind=mermaid)` → `kb.block.upsert` | ✅ **需要** | `confirm` | 后线 |
| 6.1 | KP 范围重锚（正文编辑后自动校正） | ✅ 有：`_resync_kp_ranges_after_edit`（`document.py:420`），在 `save_document` 内**自动**触发（`:376`）；编辑期另有 `resolve_kp_ranges` RPC（`ui.py:206`） | 派生动作 | ❌ 不新增（应**隐式发生**，不作为独立场景暴露） | — | 不作为独立场景 |
| 6.2 | pending 同步（KP 变更后刷新待确认） | ✅ 有：`sync_pending_for_file`（`storage/pending.py:273`），在 `confirm_kp_range` / `delete_kp` 内**自动**调用（`document.py:1440`、`:1913`）；全库 `sync_kb_pending`（`pending.py:172`） | 派生动作 | ❌ 不新增（同上，隐式发生） | — | 不作为独立场景 |
| 6.3 | 重建 / 同步 manifest（文件清单） | ✅ 有：`rebuild_manifest`（`manifest.py:183`）经 `sync_manifest`（`document.py:2120`，RPC `ui.py:414`）；⚠️ 被**路径移动**阻断（`document.py:2123-2131`，先要求"修复路径"）；读侧有 pending overlay（`manifest.py:86-99`） | `rebuild_manifest` | ✅ **需要**：`kb.manifest.rebuild` —— **2026-09-22 已落地**（op `rebuild_manifest`，**零参数 / 不带 `file`**）：落盘复用 `DocumentService.sync_manifest()`（本地不另写重建逻辑），**前置硬闸**沿用同一个 `detect_path_moves()`（有未修复路径变更 ⇒ `manifest_blocked_by_path_moves`，**挡在计划期** ⇒ 模型能据错改意图，不必等 apply 整批回滚）；**必须收尾**（`manifest_must_be_last`：它记的是"这一刻的文件集"）；**无 `file`** ⇒ 天然豁免读后写闸。**不进 `RISKY_OPS`**（可再生索引 + 自带前置硬闸，比逐条确认那道闸更贴问题）—— 这与本表「建议 `confirm`」的**偏差是故意的**：设计给的是"人点按钮也有确认"时代的建议，而 manifest 是唯一能靠"重建"消掉的那类警告，加确认只会把它变回"等人点" | `confirm` | **已落地**（2026-09-22） |
| 6.4 | 路径级联修复（批量改正文 + sidecar 路径） | ✅ 有：`repair_path_cascade`（`document.py:2054`，RPC `ui.py:426`），**默认 dry-run**（`apply=False`）；`reconcile_path_cascade` 实现 | `repair_paths` | ✅ **需要**：`kb.paths.repair` | `confirm`（**必须逐条预览**） | M3b |
| 6.5 | 批量重命名级联（改文件名 → 全库 `[[stem]]` 改写） | ✅ 有：见 2.6（`document.py:583`，含 `replace_link_id_in_markdown` 级联 `:637`） | 见 2.6 | ❌（复用 2.6） | `confirm` | M3b |
| 6.6 | KP id 全库级联改（正文 `[[id]]` + 各 sidecar 引用） | ✅ 有：见 1.8（`document.py:1937`） | 见 1.8 | ❌（复用 1.8） | `confirm` | M3b |
| 6.7 | 安装 / 刷新 KB Agent 文件（写 `<kb>/.memoria/agent/**`） | ⚠️ 有但**不同性质**：`install_kb_agent`（RPC `ui.py:1100`）向库内写提示词/规范文件，属**程序安装面**；且打开知识库时会**自动补写/刷新**，是**有意例外于"禁止 silent 写入"**（`docs-management.md §4.2` 2026-09-10 行） | — | ❌ 不建议 | — | **不建议给 agent 做** |
| 6.8 | 会话 / 偏好 / 模型配置类写入 | ⚠️ 存在但**不在内容面**：`agent_session_delete`（`ui.py:1329`，删会话 JSONL）、`save_ui_settings`（`ui.py:769`）、`agent_save_config`（`ui.py:1184`，写 `config/agent.json`）；会话目录与备份目录都**不在** §2.4 的插件 `permissions.write` 白名单内 | — | ❌ 不建议 | — | **不建议给 agent 做** |
| 6.9 | 备份与撤销（写前 pre-image / 撤销上一批） | ❌ **尚无**（M3a 待实现）：既有只有 `atomic_yaml.write_backup`（`storage/atomic_yaml.py:22`）留下的**单版本 `.bak`**，且 **fail-open**（`:41-43`），不按事务聚合；正文 `.md` 路径**完全没有**这层 | 由 apply 入口隐式做（§6 第 5 行） | ✅ **需要**（`snapshot_pre_images` + `agent_capability_call(action="undo")`） | 撤销本身仍 `confirm` | M3a |

---

## 8. 场景间的关系与排序建议（讨论用）

> 全部为**建议 / 待定**，**不替人定案**；本节只给讨论用的分组、依赖与候选顺序。

**A. 共用同一原语的场景（先定底座，多数场景不必各造原语）**

| 原语（暂定） | 被哪些场景共用 | 说明 |
|---|---|---|
| **通用行级原语** `kb.file.edit` | 2.1 / 2.2 / 2.3 / 3.1 / 3.2 / 3.3 / 4.1 / 4.3 / 4.4 / 4.7 | 这些场景在实现上**都只是"改写某几行"**（今天的写入口 `save_document` 是整篇，`document.py:317`）。若先有行级原语，字体样式与图片属性**无需各自的后端原语** |
| **整块原语** `kb.block.upsert` | 5.1 / 5.2 / 5.3 / 5.5 | 按块类型重建（code / table / math / mermaid）；5.2 / 5.3 先要修人 UI 的写回缺陷 |
| `kb.kp.*`（create/update/delete/rename/merge） | 1.3 / 1.4 / 1.5 / 1.7 / 1.8 / 1.6 | 1.5 建议先做"整体覆写"，"增删单个 tag"待定 |
| `kb.link.*`（attach/detach/create） | 1.1 / 1.2 | attach/detach 写正文，create 是纯边 |
| `kb.image.*` | 4.1 / 4.2 / 4.5 / 4.6 | 4.1–4.4 多数可落到行级原语 |

**B. 依赖关系（做后者之前前者必须成立）**

- **1.1 连边 → 依赖 1.3 / 1.4（KP 存在）**：`attach_links` 的每个 target 必须**可解析**（`document.py:2536`），否则整批拒。
- **2.6 / 6.5 文件改名 → 依赖正文改写能力**：`rename_file` 已含全库 `[[stem]]` 改写（`document.py:629-648`），但**正文内相对链接/图片路径**不在其中（§6 第 13 行 R2）。~~⇒ `kb.file.move` 因此**不进工具集**~~ **2026-09-22 订正：`kb.file.move` 已进工具集** —— 重新推演后 R2 并不挡它：`[[id]]`/`[[stem]]` 是 id/stem 寻址、移动不改 stem ⇒ 引用不断；图片引用按规范是**库根相对** ⇒ 也不受移动影响。R2 真正管的是**"文件相对"写法**，落法是**拦**（`move_breaks_relative_refs`）而不是改写 ⇒ 既没带病上线，也不必等 R2 先落地。
- **4.x 全部 → 依赖 2.1–2.3 的行级原语**（插入/改写图片引用行）。
- **1.6 合并 KP → 依赖 1.8 改名 + 1.7 删除**（合并 = 新 id + 重指 `links`/`edges` + 删源 KP）。
- **3.4 sidecar 记录样式 → 依赖"新存储契约"**：且按 [AGENTS.md §1](../../AGENTS.md)，若成为**事实源**须**先由人登记**；否则不得落盘。
- **6.3 manifest 重建 → 依赖 6.4 路径修复**：有 `path_moves` 时 `sync_manifest` **拒绝执行**（`document.py:2123-2131`）。
- **6.9 备份/撤销 → 是所有写场景的前置**（§6 第 5 行；apply 入口第一步取 pre-image）。

**C. 候选第一批（待你拍板）**

1. **1.3 建点 + 1.4 改点 + 1.1 连边（含解挂）** —— 即原著已定的 M3a 三个 op（`upsert_kp` / `attach_links` / `detach_links`，§4 Q3）。理由：三条都**映射既有服务层入口**（`document.py:1360/1722/2493/2428`）、可**幂等**证伪、可写前备份，且已覆盖"KP + 链接两个动作类、两个方向"。
2. **（可加）1.5 完善 tag 的"整体覆写"分支** —— 复用 `update_kp`（`document.py:1722`），增量原语留后。
3. **建议在建第二批之前先拍板"通用行级原语"** —— 它是字体样式、图片属性、段落增删三类场景的**共同底座**，先定它可一次性解掉 §7 的十余行。

**D. 建议今天不该给 agent 做的场景（各一句理由）**

| 场景 | 理由 |
|---|---|
| **4.8 未引用图片清理** | 真删磁盘文件**不可恢复**，且已有"6 小时宽限 + 后台自动"的路径（`document.py:1047-1078`）；让模型参与只会放大 data-loss 面 |
| **5.4 `<details>` / 任意原始 HTML** | 渲染器**按字面文本显示原始 HTML**（[preview-formats.md §2](../reference/preview-formats.md:36)）⇒ 写了也不生效；属"要先改渲染器"的前置问题 |
| **6.7 安装 / 刷新 KB Agent 文件** | 属**程序安装面**（写 `.memoria/agent/**`），且现路径是**有意例外于"禁止 silent 写入"**的自动补写；交给 agent 会与"边界可证"的口径冲突 |
| **6.8 会话 / 偏好 / 配置写** | 这些文件**不在** §2.4 的 `permissions.write` 白名单内（会话、备份目录都被显式排除），且属库外/程序面 |
| **3.5 整文级字号 / 字色** | 它是**视图偏好**（`ui-settings.json` 的 `--preview-font-size`），不是知识库内容；agent 改它=改用户界面设置，不是"写知识" |
| **2.7 frontmatter（可暂缓）** | KP 元数据权威是 **sidecar**、frontmatter **不参与解析** ⇒ 写入价值低、且要先定"谁权威"；建议留到 §7 其他场景之后 |
| **6.1 / 6.2（派生动作）** | 范围重锚与 pending 同步应**隐式发生**在写入事务内，不该作为独立 agent 场景暴露（否则出现"绕开事务的第二个写者"） |
| **5.2 表格 / 5.3 公式块（暂缓）** | 人 UI 自身尚未写好（表格无事件绑定 `edit-handler.js:1451-1452`；公式块写回可能损坏 TeX）⇒ **先修人 UI**，再考虑 agent 面 |

---

## 9. 写冲突优先级与并发保护（口径，2026-09-20 人已拍板）

> **触发**：用户问「你把用户修改和 agent 修改哪个定位更高优先？」。本节是**结论口径**。
> **实现落点**：`memoria/storage/file_version.py`（版本令牌）+ `services/agent/apply.py`（整批拒）；
> 口径条目属**人已确认**，Agent 只追加、不改写。

**三条规则（不设"谁的内容优先"，只设"不许基于过期版本写"）**

1. **盘上版本 = 唯一权威**：任何写者（人机保存 / agent apply / 级联修复）写之前必须确认
   "我读到的版本 == 盘上版本"；不一致 ⇒ **拒写**并回报结构化 `stale_write`（带期望/当前版本）。
2. **人的当下操作最高**：agent 的整批写不得趁人正在编辑同一文件时抢写 —— 该文件有未落盘编辑时，
   apply 应**等或拒**（不抢、不覆盖）。
3. **冲突由人明示决定**：`stale_write` 交前端弹三选一 —— ① 重载（丢弃我的编辑，跟盘上走）
   ② 以我为准（**覆盖前先把盘上那一版存进备份目录**，保证仍可回滚）③ 看差异（可后置）。
   **agent 不自动合并、不静默赢；人的过期 buffer 也不自动赢**。

**为什么不是"某一方优先"**：优先的是**人**，不是**人的过期 buffer**。让人的过期保存自动赢 =
静默抹掉 agent 那个**已审计、可撤销的事务**（审计链与盘上现实脱节）；让 agent 自动赢 = 吞掉人的
编辑意图。两者都禁止 ⇒ 只剩"**拒绝 + 明示选择**"。

| 场景 | 谁赢 | 依据 |
|---|---|---|
| 用户正在编辑 F，agent apply 改 F | **用户**（agent 等 / 拒） | 规则 2 |
| 用户 buffer 落后（F 已被 agent 改过），用户点保存 | **都不自动赢**：拒保存 + 弹冲突 | 规则 1 + 3 |
| 两个窗口（`open_new_window` = **独立进程**）同时保存 | 后写者被拒 + 提示 | 规则 1 |
| 两个 agent 会话同时 apply | 先到先得，后到拒 | 规则 1（版本令牌天然覆盖） |
| 用户手改后点「撤销」 | **拒绝撤销**（保留备份） | 与规则 1 同向；`post.json` 写后哈希校验（已实现） |

**实现形状（本轮已落后端一半）**

- **版本令牌**：`storage/file_version.py` 提供 `file_version` / `rel_version` / `with_version` /
  `guard_save`；`ui.py` 的 `load_document` 追加 `version` 字段（**只增不改**，旧读者忽略），
  `save_document` 追加可选 `base_version`（**空串 = 不校验 ⇒ 旧行为逐字不变**）。
  `services/document.py` **一行未动** —— 两个 RPC 用**等量替换**委托进新模块（零行漂移）。
- **agent 侧**：`preview_plan()` 返回 `base_versions`（= 用户看过的那一版）⇒ apply 时原样传回；
  `apply_plan()` **先比版本再谈 plan**（文件都变了还报"plan 非法"是误导），不一致 ⇒ `stale_write`
  且**尚未建备份、未写盘**；调用方没给基准时退化为"本次编译时那份"的最低限度自检。
- **未做（下一步）**：前端保存路径带上 `version` 并处理 `stale_write`（三选一弹窗 + i18n 中英）；
  apply 期间的前端忙位与"当前文件正被编辑 ⇒ 拒 apply"。
- **前端一半（同日落地，追加记录）**：`app.js` **IIFE 内四处同行内替换**（零行漂移，`git diff` 可证
  只有 `@@ -124 / -133 / -135 / -137 @@` 四个单行 hunk + 文件尾追加块）——
  `syncToDisk` ① 守卫行加 `MemoriaWriteGuard.deferIfBusy(markDirty)`（忙 ⇒ 重新入队让路）
  ② 保存调用带 `state.doc?.version` ③ 成功后把响应的新版本滚回 `state.doc.version`；
  并在**文件末尾追加** `(function memoriaWriteGuard(){…})()`：`setBusy`（供 agent apply 前后置位）、
  `deferIfBusy`、`onSaveFailed`（只接管 `stale_write`），以及**三选一弹窗**（复用既有
  `.-modal` 样式，与 `confirmTreeAction` 同构）：「重载（丢弃我的编辑）」→ `openFile(path)`；
  「以我为准（先备份再覆盖）」→ `save_document(..., force=true)`；「稍后」→ 关掉。
  **覆盖前先备份**由后端 `guard_save(force=True)` 保证（走 `services/agent/backup` 落
  `manual-force/<txid>` 批次 + `post.json` ⇒ 可直接 `restore_batch` 撤销；备份失败即拒写）。
  i18n 用**文件末尾 `Object.assign`** 追加 `writeConflict.*`（中英各 8 键，selftest 通过）。
  验收：`pytest -q` **409 passed**；`node --check` 3/3；`scripts/i18n_selftest.js` 全 PASS。
  前端接线已用测试钉住（`test_frontend_wires_version_token_and_conflict_dialog` +
  `test_conflict_dialog_keys_exist_in_every_locale`）。
- **仍未做**：apply 期间**前端忙位与 agent 写入口的接线**（等 ④ 的 RPC + 确认卡一起挂：
  届时 `MemoriaWriteGuard.setBusy(true/false)` 包住 apply，并在 apply 前查"当前文件是否正被编辑
  ⇒ 脏则拒 apply"，即规则 2 的另一半）；`_write_body` 的 `WinError 5` **已收口**：写路径统一走 `replace_with_retry`。

**明确否掉的两条路（防复发）**

- **多线程**：写路径本身就是**同步短事务**，现状**没有写锁**（现有锁只覆盖词法索引 / executor /
  kp_index / embedding / agent 作业）。开多线程只会把"文件级并发"升级成"进程内竞态 + 部分写"
  ⇒ **不做**；正确方向是**串行 + 版本校验**。
- **增量修改**：行级 patch 同样基于某个基准版本、对 lost update **不免疫**；它的价值在写入量 /
  备份体积 / diff 可读性 ⇒ 属优化，**后置**。

**已知环境噪声（2026-09-22 已收口）**：`_write_body` 的 `os.replace` 偶发 `WinError 5`
（外部 AV / 索引器瞬时持锁；`_write_body` 自身无句柄泄漏：`with` + flush + fsync → close → replace）。
现全仓写路径（含侧车 `atomic_yaml` / KP 级联 / 图片注册表）统一走 `replace_with_retry`（`atomic_write.py`）。
