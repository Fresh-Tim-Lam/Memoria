# 工作指令：Memoria「知识库与维护机制」现状代码扫描（准备作业）

你是一个**只读代码审计 Agent**。目标：不依据早期文档的断言，而是通过扫描源码/配置/测试库与可复现行为，**①核实** `docs/to-dolist.md` §12「施工总表」中 A–E 每项的**真实现状**，并 **②开放发现「尚未登记到总表」的维护类机制/任务**（候选新条目，供主 Agent 决定是否补登记）。输出可复核的证据报告。**不要修改任何源码、docs/example 内容、配置；不要 commit/push。**

## 0. 环境与纪律
- 仓库根：`d:\AAA_Jupyter\Memoria`；Python 用 `.venv\Scripts\python.exe`；前端为原生 JS（无框架）。
- 允许：Read/Grep/Glob/静态语义检索；只读命令（`git status`、`py_compile`、`node --check`）；可在 `artifacts/` 下建临时副本/脚本验证行为（用后删除）。
- 禁止：改动 `src/`、`docs/`（含 to-dolist/design/conventions）、`resources/`、`Package/` 内任何文件；不执行会写 KB/落盘的 RPC。
- 判定必须基于「符号存在 + 调用链 + 可行命令输出 + 行号引用」；文档与代码冲突时**以代码/行为为准**，并在报告中指出文档不一致处。
- 对每一项输出：`判定(✅/⚠️/❌/⏳仅设计) | 关键证据(文件:行 + 函数/调用链) | 可复现命令及输出摘录 | 缺口/风险说明`。
- 报告写入 `artifacts/_audit/summary.md`，单项细则按 ID 分文件或同文件分节；结果交由主 Agent 复核，你**不下最终结论**（状态以复核为准）。

## 1. 通用核实方法
1. 先建索引：读 `AGENT.md`、`src/memoria/ui/static/app/index.html` 的 script/css 加载顺序、`docs/to-dolist.md §12` 与 `docs/design/maintenance-jobs.md`（只作候选线索，不当作真相）。
2. 对每个 ID 按第 2 节的「核查要点」搜索符号→读实现→追调用链→必要时在 artifacts 临时库跑最小行为验证。
3. 涉及"前端行为"的（渲染、快捷键、弹窗）以代码证据为主；确需真机的条目单独标 `[需真机]`，不强行运行 GUI。

## 2. 分项核查要点（务必逐条回答）

### A. 一致性/注册内核
- A1 sidecar 镜像与 file 字段、级联
  查 `src/memoria/storage/sidecar.py`（`sidecar_path_for`/`legacy_sidecar_path_for`/`load_sidecar_for_md`/`save_sidecar_for_md`）、`storage/path_cascade.py`（`apply_path_move` 及其 侧车移动/file 字段/pending/manifest 五件事）。确认镜像命名规则（`<stem>.memoria.yaml` 位于 md 同层镜像）并用 `docs/example/rename-test/.memoria/sidecars` 抽查一致性。
- A2 manifest：`storage/manifest.py`（`load/save/build_manifest_entries/entry_for_md/touch_manifest_entry/ensure_manifest_baseline`）；列出哪些写路径会 touch/重建（save_document、rename、sidecar 写、delete）。
- A3 pending 路径同步：`storage/pending.py` + 引用点（`path_cascade._update_pending_paths`、`document.rename_file/rename_dir` 内的 load_pending/save_pending 段）确认 rename 时会更新 file 字段。
- A4 图片注册表：`services/document.py`（`_update_registry_for_doc/_scan_image_refs/_IMG_REF_RE/_IMG_MENTION_RE/list_images/rebuild_image_registry/image_registry_auto_check/unused_images/cleanup_unused_images`）与前端 `js/image-tools.js`（渲染/诊断/清理/registry 检查定时器）。确认保存只增量、删除只在显式/宽限期自动。
- A5 KP range：`range/locator.py`（`locate_snippet`/`resolve_range` 的 hint-window/前向语义）与 `services/document.py::_resync_kp_ranges_after_edit`（保存重锚 + end 结构重建）以及 `save_document` 是否返回 `ranges_resynced`。用临时副本验证一次"结尾行中间回车→保存→range 可重解析"。
- A6 词法/embedding 索引：grep `_rebuild_lexical_index`/`embedding_provider`/`warmup_embedding` 与 `_write_sidecar` 调用点；判断是否**同步**发生在保存/侧车写路径、以及全量/增量方式（为"作业化 P3"提供改造面证据）。
- A7 原子写：grep `os.replace`/`.tmp` 写盘模式（document/storage/manifest/sidecar/settings），汇总覆盖范围。

### B. 操作/作业
- B1 文件重命名：`document.rename_file` 全链（冲突/`kp_shadow`/`replace_link_id_in_markdown`/`migrate_sidecar_kp_refs`/manifest/pending/registry）；RPC `file_rename`；前端 `file-tree.js renameTreeFile`。
- B2 文件夹重命名：`document.rename_dir`（校验、收集 moved md、os.rename、逐文件 `apply_path_move`、registry 增量、errors）与 RPC `dir_rename`、前端 `renameTreeDir` + `applyRenameUi`；核对 to-dolist 声称"新增待验收"是否属实、有无明显遗漏（如非 md 文件移动后的 manifest 无关项、空目录、只读目标）。
- B3 F2：`file-tree.js init` 捕获阶段 keydown + `_lastSel` 记录点 + "仅可见弹窗"守卫；给出注册与守卫行号；标注 `[需真机]` 一项。
- B4 保存：前端 `syncToDisk/markDirty/flushSync` 与防抖常量、后端 `save_document`；确认 auto-save 是否默认触发、文件切换/关库前是否 flush。
- B5 KP 创建/更新：追踪 UI 上"创建/确认知识点"(如 `confirm_kp_range`)、"更新"(`update_kp`) 从前端调用到后端执行的**同步等待链**，标出耗时点（全量重建/校验/embedding 等），为作业化(P1)列出"哪些可在后台/哪些必须同步"。
- B6 md 相对路径链接改写：在前端 `resolveKbMdTarget/onPreviewMdLinkDelegate`（点击解析）与后端 rename 链路中检索是否存在对 `](...)`/`![...]` 相对路径引用的**改写逻辑**；结论若"无"，判定 `❌/部分` 并给出建议接入点（dir/file rename 后按"当前文档目录→目标旧前缀"重算）。
- B7 导出：检索是否存在 `export_knowledge_base/export_scan/export.*` RPC 或 service——预期仅 design 草稿，确认无实现。

### C. 视图/静默刷新
- C1：`file-tree.js remapPath/applyRenameUi` 与 app 侧 `refreshFiles` 的衔接（树展开集/tab/当前文件重映射后刷新）。
- C2：`app.js _silentRefreshKpPanel` + `syncToDisk` 中 `ranges_resynced>0` 触发点（防重入、只刷面板）。
- C3：预览范围带实现面（`highlightRange`/`highlightPreviewRange`/`dismissKpRangeHighlight`/`-preview-range-band`）：是否存在"不滚动不闪烁的静默重绘"路径；若无判 `⏳`。
- C4：图谱刷新触发面（构建按钮→RPC→graph 渲染；重命名/保存后是否有局部刷新钩子）；判 `⏳`/缺失。
- C5：仅 `docs/design/maintenance-jobs.md` 草稿，无内核实现（确认无调度器/队列代码）。

### D. 渲染/显示
- D1 图片缩略图：`image-tools.js` 卡片模板（无 `loading=lazy`、用 `new Image()` 后置 src）与 `app.css .-image-card`（无 `overflow:hidden`、子元素圆角补位）；给出 CSS/JS 行号；标注 `[需真机]` 视觉确认。
- D2 预览颜色 hover/选区：`app.js _applyColorTint/_clearColorTint/_captureTintAnchor/_restoreTintSelection` 及 `#preview.-color-tinting ::selection` 规则现状；确认无历史遗留（如 removeAllRanges 杀选区路径）。

### E. 测试/文档/规范
- E1：`docs/example/rename-test/` 结构（5 md/5 KP/空目录/图片/镜像 sidecars/manifest 与 `_gen_rename_test_kb.py`），并在 artifacts 临时副本跑一次生成脚本以验证"可重复再生成且校验通过"。
- E2：`docs/reference/preview-formats.md` §4.4 与 `resources/agent-prompts/organize.zh-CN.md` §7.3 是否存在且含 `../` 禁例；比对 `Package/resources/` 两份镜像是否同字节（只读对比）。
- E3：确认 `docs/design/export-plan.md`、`docs/design/maintenance-jobs.md` 存在及头部三要素/状态；不评价内容。
- E4：确认 `docs/to-dolist.md §12` 与 `docs/conventions/docs-management.md` 修订记录两处登记存在；可选跑 `scripts/scan_ui_strings.py` 报 rows=0。

## 2.5 未登记机制/任务的开放发现（必做）

> 目的：施工总表 A–E 可能漏掉了已存在或半成品的维护类机制。以"行为面扫描"发现候选，交给主 Agent 决定是否补登记为新条目（可能新增到 A–E 或开新组 F）。候选按置信度标注 `高/中/低`，不确定也要列出（宁多勿漏，主 Agent 负责过滤）。

1. **RPC 面枚举**：列出 `src/memoria/presentation/api/ui.py` 全部公开方法，按性质分类：读型 / 编辑型 / **维护型**（写盘、重建、清理、校验、同步、迁移、审计、刷新类）。把"维护型"中**不在总表 A–E** 的逐一提为候选。
2. **写/重建/失效行为扫描**（后端）：对 `src/memoria/` 以关键词全量检索并归类调用点：`rebuild|reconcile|detect_path|ensure_|validate|audit|cleanup|sync_|migrate|legacy|\.bak|os\.replace|\.tmp|touch_manifest|cache|warmup|check|sweep|pending|registry`。对每一处回答：是什么机制 / 谁触发 / 写什么 / 是否已登记。
   - 重点核对但**不受限**于：`.bak` 备份机制、路径漂移检测 `detect_path_moves/reconcile_path_cascade` 是否有 UI 入口、KB 检查 `kb-check` 与 check 报告、预览 `preview_body` 缓存与失效、undo/redo 状态、`ui-settings` 持久化与最近列表/新窗口环境、`mapper`/AST 相关缓存、词法索引之外的辅助索引（`search_aux/kp/*.json`、embeddings 缓存目录）、导入遗留清理、`.memoria/cache/` 各子目录生成者。
3. **前端后台行为扫描**：grep `setInterval|setTimeout|requestIdleCallback|schedule|flush|debounce|auto|silent|check|warm` 于 `ui/static/app/js/`；列出所有"定时/自动/后台/静默"任务（图片自动检查、kb 检查徽标、save/render 防抖等），将未登记项提为候选。
4. **文档交叉核对**：浏览 `docs/reference/` 与 `docs/design/` 中描述"机制/自维护/自动/一致性"的段落，挑出总表未覆盖的（如维护类规范、检查报告、路径级联既有入口），列为候选并标注出处文档；若文档与代码不一致，以代码为准并说明。
5. **孤儿/无主逻辑**：对疑似维护函数做调用点核验；若存在"无人调用"的维护逻辑，单独列出（可能是隐藏机制或死代码，交给主 Agent 判断）。

候选新条目的输出格式（每项）：
```
[候选] 建议编号(预留 Fxx 或 A–E 续号) | 名称 | 触发点 | 初步现状(存在/半成品/仅代码无入口/死代码) |
关键证据(文件:行 + 调用链) | 置信度(高/中/低) | 建议归属(A–E 某组或新组 F)
```

## 3. 输出物（交付给主 Agent 复核）
1. `artifacts/_audit/summary.md`：总表 A–E 判定汇总 + 与总表声明不一致清单（重点：任何文档声称✅而代码缺失，或反之）。
2. `artifacts/_audit/unregistered.md`：§2.5 未登记候选清单（含置信度与建议归属），供主 Agent 决定是否补入施工总表。
3. 每节含：判定 | 关键文件:行 + 函数 | 证据命令与输出摘录 | 缺口/风险 | `[需真机]` 标记。
4. 一个"复查建议"小节：给出你认为主 Agent 最该抽样复核的 5 处（如 B2 的 dir_rename、A5 heal、C2 触发、B6 缺口、D1 CSS），并另列"未登记候选中最可能应补录的 3 项"。
5. 用后清理你在 `artifacts/` 建的临时物（保留 `_audit/` 报告）。