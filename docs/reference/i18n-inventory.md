# UI 界面文案清单（i18n Inventory）

> **用途**：登记桌面应用**前端界面**全部含中文的用户可见文案与定位（文件+行号），作为 i18n 迁移（抽键→语言包）与后续新增 UI 自查的依据。
> **目标读者**：项目负责人 / AI Agent（迁移文案、新增 UI、审阅语言包前必查）。
> **关联文档**：[i18n.md](../conventions/i18n.md)（语言系统维护规范）；[docs-management.md](../conventions/docs-management.md)（docs 组织规则）。
> **范围/边界**：仅统计 `src/memoria/ui/static/app/` 下 index.html、app/js/*.js、app/css 的 `content:` 文案；**不含** lib/vendor/mathjax 第三方库、纯注释文件（lexer/parser/ast 等中文均为注释，不属界面文案）；后端 Python 返回给界面的消息（document.py/import_engine.py/ui.py 等）为另一分区，机制落地后另行登记。
> **生成**：`python scripts/scan_ui_strings.py`（确定性输出，可重复执行覆盖本文）。状态口径：`☐ 未迁移` 表示仍硬编码中文；迁移为 `t('key')` 后人工将对应文件状态置 `✔ 已迁移`（脚本重跑会重置，可在本文末尾「迁移记录」人工维护）。
> 生成日期：2026-09-04

## 统计总览

| 文件 | 含中文候选行数 | 状态 |
|------|------|------|
| `src/memoria/ui/static/app/js/app.js` | 496 | ☐ 未迁移 |
| **合计** | **496** | — |

## 逐文件清单

### src/memoria/ui/static/app/js/app.js

- L366: if (!a 丨丨 !a[fn]) throw new Error("API 不可用: " + fn);
- L450: setStatus("已加载知识库", state.kbPath);
- L464: setStatus("等待后端…");
- L527: setStatus("已打开知识库", path);
- L571: kpList.innerHTML = '<div class="empty">未打开知识库</div>';
- L576: setStatus("未打开知识库", "点「打开知识库」选择目录");
- L583: setStatus(res.message 丨丨 "列表失败");
- L713: { label: "重命名", action: () => renameTreeFile(item.dataset.path) },
- L714: { label: "删除", danger: true, action: () => deleteTreeFile(item.dataset.path) },
- L724: { label: "新建文件", action: () => createTreeFile(base) },
- L725: { label: "新建文件夹", action: () => createTreeDir(base) },
- L733: { label: "新建文件", action: () => createTreeFile("") },
- L734: { label: "新建文件夹", action: () => createTreeDir("") },
- L814: "button" class="-btn" data-act="cancel">取消</button>
- L815: rimary" data-act="ok">${esc(okLabel 丨丨 "确定")}</button>
- L867: promptTreeInput("重命名文件", "输入新文件名（自动补 .md）", oldName, "重命名", async (val) => {
- L871: setStatus(res.message 丨丨 "重命名失败");
- L874: setStatus("已重命名", res.path);
- L879: setStatus(`已重命名并同步 ${n} 处链接引用（${m} 个文件正文、${s} 个侧车）`, res.path);
- L895: confirmTreeAction("删除文件", `确定删除「${basename(relPath)}」？此操作不可恢复。`, "删除", async () => {
- L898: setStatus(res.message 丨丨 "删除失败");
- L901: setStatus("已删除", basename(relPath));
- L911: promptTreeInput("新建文件", "输入文件名（自动补 .md）", "", "创建", async (val) => {
- L916: setStatus(res.message 丨丨 "创建失败");
- L919: setStatus("已创建", res.path);
- L927: promptTreeInput("新建文件夹", "输入文件夹名", "", "创建", async (val) => {
- L932: setStatus(res.message 丨丨 "创建失败");
- L935: setStatus("已创建文件夹", res.path);
- L1007: label: "全部",
- L1008: title: "全部节点群（横向瀑布流）",
- L1014: e: `${g.hubName 丨丨 g.hubId} · ${g.size} 节点 · ${g.edgeCount} 边`,
- L1226: setStatus("构建中…", "同步链接配置并生成图谱");
- L1230: setStatus(res.message 丨丨 "构建失败");
- L1239: const msg = res.message 丨丨 "构建完成";
- L1241: ? `${msg} · ${res.warn_count} 处待人工配置`
- L1243: setStatus("构建完成", stats);
- L1249: if (kinds) setStatus("构建完成", `${stats}（${kinds}）`);
- L1252: setStatus("构建失败", String(e.message 丨丨 e));
- L1541: setStatus("导入预扫描中…");
- L1544: setStatusError(scan.message 丨丨 "预扫描失败");
- L1555: setStatusError("导入失败", e.message);
- L1565: setStatus("导入中…");
- L1568: setStatusError(result.message 丨丨 "导入失败");
- L1575: setStatusError("导入失败", e.message);
- L1590: setStatus("导入完成", `${result.files_written 丨丨 0} 文件 · ${kp} KP`);
- L2084: html += `<p>共 <strong>${esc(String(scanResult.total_files 丨丨 0))}</strong> 个文件，`;
- L2085: nResult.total_sections 丨丨 0))}</strong> 个段落，`;
- L2086: .total_kp_declarations 丨丨 0))}</strong> 个 KP 声明。</p>`;
- L2087: html += `<p class="-stat-error">检测到 <strong>${esc(String(conflicts.length))}</strong> 处 KP id 冲突：</p>`
- L2102: html += `<span class="-muted">来源: ${source}:${line} → 已存在: ${existFile} (${existName})</span>`;
- L2105: "import-res-${i}" value="skip" checked> 跳过</label>`;
- L2106: me="import-res-${i}" value="overwrite"> 覆盖</label>`;
- L2107: name="import-res-${i}" value="rename"> 重命名</label>`;
- L2108: " data-conflict-idx="${i}" placeholder="新 KP id">`;
- L2153: html += `<p class="-stat-ok">导入成功</p>`;
- L2154: html += `<p>写入 <strong>${esc(String(result.files_written 丨丨 0))}</strong> 个文件，`;
- L2155: html += `导入 <strong>${esc(String(result.kp_imported 丨丨 0))}</strong> 个 KP，`;
- L2156: html += `覆盖 <strong>${esc(String(result.kp_overwritten 丨丨 0))}</strong> 个，`;
- L2157: html += `重命名 <strong>${esc(String(result.kp_renamed 丨丨 0))}</strong> 个，`;
- L2158: html += `跳过 <strong>${esc(String(result.kp_skipped 丨丨 0))}</strong> 个。</p>`;
- L2161: at-error">${esc(String(errors.length))} 个错误：</p><ul>`;
- L2184: setStatus(res.message 丨丨 "图谱加载失败");
- L2201: setStatus("图谱加载失败", String(e.message 丨丨 e));
- L2367: el.innerHTML = '<div class="empty">无 Markdown 文件</div>';
- L2493: close-btn" data-tab-close="${i}" title="关闭">×</span>
- L2532: setStatus("加载中…", relPath);
- L2535: setStatus(res.message 丨丨 "加载失败");
- L2617: points.length} KP · ${res.lines.length} 行`;
- L2621: length) parts.push(`${sv.errors.length} 错误`);
- L2622: ngth) parts.push(`${sv.warnings.length} 警告`);
- L2623: stats += ` · 配置 ${parts.join(" ")}`;
- L2943: nnerHTML = '<p class="-preview-loading">预览模块未加载</p>';
- L2949: nnerHTML = '<p class="-preview-loading">渲染中…</p>';
- L3028: nnerHTML = `<p class="-preview-loading">预览失败: ${esc(String(e))}</p>`;
- L3133: el.innerHTML = `<span>⚠ 链接一致性：${n} 个配置入口未在正文中挂接为可点击 [[…]] · 请打开「配置 → 链接」点击「挂接…」</span>`;
- L3145: state.currentPath 丨丨 "就绪",
- L3146: `预览 OK · 公式 ${report.mathRendered}/${report.mathExpected}`
- L3154: : ["预览未完全成功"];
- L3156: `<span>⚠ 预览自检：${esc(parts.join("；"))}</span>` +
- L3158: ? `<details><summary>TeX 错误</summary><pre>${esc(report.mathErrors.join("\n"))}</pre></details>`
- L3161: state.currentPath 丨丨 "预览",
- L3162: `公式 ${report.mathRendered}/${report.mathExpected} · 有问题`
- L3189: (el) el.innerHTML = '<div class="empty">请选择文件</div>';
- L3203: el.innerHTML = '<div class="empty">无知识点 · 右键条目或点「配置」</div>';
- L3208: `<div class="empty">无正式 KP · ${proposals.length} 个标题提议<br><span class="-muted">点「配置」→ 待确认<
- L3260: return { text: "未配置", ok: false };
- L3279: <span class="-muted">本文件 · 可合并</span>
- L3280: lass="-btn secondary -btn--sm" disabled>合并</button>
- L3285: <span class="-muted">q-learning.md · 跨文件</span>
- L3286: lass="-btn secondary -btn--sm" disabled>创建引用</button>
- L3291: <span class="-muted">正文 L42 未绑定</span>
- L3292: lass="-btn secondary -btn--sm" disabled>创建路由</button>
- L3296: ? "模糊匹配 · 创建 / 合并知识点（M4）"
- L3297: : "模糊匹配 · 推荐链接路由（M4）";
- L3300: ? "M4 接入 SearchKernel 后：按 name、tags、description Lexical 精排；近重复合并在此展示。"
- L3301: : "M4 接入后：扫描正文 [[…]] 与配置差异；未绑定链接与多目标一并列出。";
- L3335: { id: "kp", label: "知识点", count: counts.kp },
- L3336: { id: "links", label: "链接", count: counts.links },
- L3337: { id: "pending", label: "待确认", count: counts.pending },
- L3352: tn primary -btn--sm" data-config-new-kp>新建知识点</button>
- L3353: <span class="-muted">空白新建，或在「待确认」Tab 配置标题提议</span>
- L3356: html += `<p class="-muted">暂无正式知识点 · 在「待确认」Tab 配置并确认提议</p>`;
- L3372: btn--sm" data-kp-config="${esc(kp.id)}">配置</button>
- L3373: " data-kp-delete="${esc(kp.id)}" title="从配置中删除该知识点">删除</button>
- L3391: h('<span class="-link-audit-bad" title="配置已有，正文无 [[]] 入口">缺正文入口</span>');
- L3393: lass="-link-audit-bad" title="instances 行号与正文不一致">实例失效</span>');
- L3395: ('<span class="-link-audit-warn" title="部分实例未挂接">部分挂接</span>');
- L3398: ('<span class="-link-audit-warn" title="跳转目标无法解析">目标未解析</span>');
- L3411: k-audit-fix="${esc(first.anchor_text)}">打开首个问题</button>`
- L3414: <strong>链接一致性</strong>
- L3415: <span>${n} 个跳转入口存在一致性问题 · 在列表中点「匹配」勾选位置后，再点面板内「确认并包裹所选」（仅保存路由不会改写正文）</span>
- L3427: <label class="-link-search-opt" title="如「深度RL」可匹配正文「深度 RL」">
- L3429: 忽略空格
- L3431: <label class="-link-search-opt" title="输入时可推荐已有链接文本、标题或正文匹配">
- L3433: 模糊推荐
- L3435: rch-opt -link-search-opt-future" title="预留 · 后续接搜索引擎">
- L3437: 忽略大小写
- L3496: ? '<p class="-muted -link-match-empty">填写匹配文本后扫描正文</p>'
- L3500: rn '<p class="-muted -link-match-empty">扫描中…</p>';
- L3507: `共 ${m.matches.length} 处 · 已挂接 ${attached} 处 · 排除 ${excluded} 处 · 已选 ${
- L3525: ? (row.block_reason 丨丨 "与其它跳转占用区域重叠") + " · 点击定位到该行"
- L3527: ? "子串匹配，不可挂接 · 点击定位到该行"
- L3529: ? "已排除 · 点击定位到该行"
- L3530: : "点击整行切换勾选并定位到该行";
- L3550: tn" id="config-link-match-close" title="收起">×</button>`
- L3556: econdary" id="config-link-match-cancel">取消</button>
- L3557: m" ${m.selected.size ? "" : "disabled"}>确认并包裹所选</button>
- L3571: const allBtnLabel = allSelected ? "取消全选" : "全选";
- L3575: <span class="-link-match-panel-title">匹配「${esc(m.anchorText)}」</span>
- L3582: econdary -btn--sm" id="${prefix}-plain">仅选未包裹</button>
- L3584: atch-list">${rows 丨丨 '<p class="-muted">未找到匹配</p>'}</div>
- L3753: setStatus("扫描失败", String(e.message 丨丨 e));
- L3797: primary -btn--sm" id="config-new-link">新建链接</button>
- L3798: <span class="-muted">正文选区右键也可创建 · 源码/预览均可拖选 · 匹配文本须与 [[…]] 一致</span>
- L3801: html += `<p class="-muted">尚未配置链接 · [[文本]] 按知识点 id 或文件名解析</p>`;
- L3809: ? ` · 备选 ${link.pool.length}`
- L3824: ? ` · 已挂接 ${auditEntry.preview_attached_count} 处`
- L3826: ? ` · 正文 ${auditEntry.body_wrapped_count} 处 [[]]`
- L3833: or)}" role="button" tabindex="0" title="点击在下方匹配正文位置">
- L3837: pan class="-muted">→ ${esc(targets 丨丨 "（未绑定）")}${pool}${instHint}</span>
- L3841: data-link-match="${esc(anchor)}" title="在下方选择正文挂接位置">匹配</button>
- L3842: tn--sm" data-link-edit="${esc(anchor)}">编辑</button>
- L3843: ata-link-delete="${esc(anchor)}" title="删除跳转入口及正文 [[]]">删除</button>
- L3863: imary -btn--sm" data-config-confirm-all>全部确认</button>
- L3864: dary -btn--sm" data-config-sync-pending>刷新待确认</button>
- L3865: kbTotal != null ? `<span class="-muted">全库 ${kbTotal} 项</span>` : ""}
- L3868: html += `<p class="-muted">无待确认提议 · 扫描标题、段落 mention 或定义句（「X 是…」）后出现在此</p>`;
- L3873: up"><div class="-config-kp-group-title">标题提议 (${pendingHeading.length})</div>`;
- L3881: <span class="-config-tag">标题</span>
- L3886: -btn--sm" data-confirm-heading="${idx}">直接确认</button>
- L3887: btn--sm" data-heading-proposal="${idx}">配置并确认</button>
- L3888: -dismiss-pending="${esc(p.pending_id)}">忽略</button>` : ""}
- L3896: up"><div class="-config-kp-group-title">段落提议 (${pendingMention.length})</div>`;
- L3904: <span class="-config-tag">段落</span>
- L3909: -btn--sm" data-confirm-mention="${idx}">直接确认</button>
- L3910: btn--sm" data-mention-proposal="${idx}">配置并确认</button>
- L3911: -dismiss-pending="${esc(p.pending_id)}">忽略</button>` : ""}
- L3919: up"><div class="-config-kp-group-title">定义句提议 (${pendingDefinition.length})</div>`;
- L3927: <span class="-config-tag">定义</span>
- L3932: n--sm" data-confirm-definition="${idx}">直接确认</button>
- L3933: --sm" data-definition-proposal="${idx}">配置并确认</button>
- L4092: $("#config-title").textContent = `文件配置 — ${basename(doc.path)}`;
- L4094: let html = `<p class="-config-summary">元数据 ${hasSidecar ? "已配置" : "未创建"} · 知识点 ${counts.kp} · 链接 ${counts.lin
- L4096: html += `<p class="-config-error">错误：${esc(validation.errors.join("；"))}</p>`;
- L4099: html += `<p class="-config-warn">警告：${esc(validation.warnings.join("；"))}</p>`;
- L4104: html += `<p class="-config-warn">链接一致性：${n} 个入口未在正文中挂接为可点击 [[…]]</p>`;
- L4141: setStatusError(res.message 丨丨 "忽略失败");
- L4153: setStatus("已忽略待确认项");
- L4155: setStatusError("忽略失败", e.message);
- L4161: setStatus("刷新待确认…");
- L4164: setStatusError(res.message 丨丨 "刷新失败");
- L4176: setStatus("待确认已刷新", `${res.pending_count ?? 0} 项`);
- L4178: setStatusError("刷新失败", e.message);
- L4189: { id: "range", label: "范围" },
- L4190: { id: "identity", label: "标识" },
- L4191: { id: "tags", label: "标签" },
- L4192: { id: "edges", label: "边" },
- L4269: <label>起点行 <input type="number" class="-line-input" data-range-start min="1"
- L4270: <label>终点行 <input type="number" class="-line-input" data-range-end min="1" ma
- L4271: <span class="-muted">滚轮可微调行号${total ? ` · 正文共 ${total} 行` : ""}</span>
- L4274: class="-range-candidates-label"><strong>起点候选</strong></p>`;
- L4278: class="-range-candidates-label"><strong>终点候选</strong></p>`;
- L4282: html += `<p class="-muted">调整行号即更新高亮；预览区可切换源码 / Markdown。</p>`;
- L4352: saveBtn.textContent = "确定";
- L4353: saveBtn.title = "确定";
- L4443: ? `新建知识点${panel.name ? " — " + panel.name : ""}`
- L4444: : `知识点 — ${kp.name 丨丨 kp.id}`;
- L4477: <label class="-link-field">名称 <input type="text" id="kp-field-name" value="${esc(nameVal)}" autoc
- L4478: <p class="-config-hint">${isCreate ? "新建知识点请先在此填写 id 与名称，再到「范围」调整行号。" : "修改 id 将全库同步链接配置与正文 [[…]]（显示文字保持不变）。
- L4495: <label class="-link-field">描述
- L4496: d="kp-desc-input" rows="3" placeholder="可选；用于检索与图谱提示">${esc(desc)}</textarea>
- L4500: nc-aux" class="-btn secondary -btn--sm">同步检索 aux</button>
- L4501: t-desc" class="-btn secondary -btn--sm">建议描述</button>
- L4503: <p class="-config-hint">${isCreate ? "描述可选；确认创建时会一并保存。tag/别名/描述候选可提前配置。" : "已选 tag/别名保存后参与检索；候选区点击应用；「同步检索 a
- L4691: const labels = { contain: "包含", reference: "引用", extend: "扩展" };
- L4698: if (edge.origin === "link") return "链接";
- L4699: if (edge.origin === "range") return "嵌套";
- L4700: edge.origin === "sidecar_edge") return "手动";
- L4714: tml += `<h4 class="-edge-section-title">出边 <span class="-tab-count">${outgoing.length}</span></h4>`;
- L4724: ) html += `<span class="-edge-no-build">已抑制</span>`;
- L4728: id)}" data-target="${esc(e.target_id)}">恢复</button>`;
- L4730: id)}" data-target="${esc(e.target_id)}">抑制</button>`;
- L4733: target_id)}" data-type="${esc(e.type)}">删除</button>`;
- L4736: nk" data-anchor="${esc(e.anchor_text)}">配置链接</button>`;
- L4743: html += `<p class="-muted">无出边</p>`;
- L4749: tml += `<h4 class="-edge-section-title">入边 <span class="-tab-count">${incoming.length}</span></h4>`;
- L4759: ) html += `<span class="-edge-no-build">已抑制</span>`;
- L4765: html += `<p class="-muted">入边需全库扫描，当前仅显示本文件内入边</p>`;
- L4771: tml += `<h4 class="-edge-section-title">新建边</h4>`;
- L4773: html += `<label class="-link-field">目标 KP <input type="text" id="kp-edge-target" placeholder="输入 KP id" au
- L4774: html += `<label class="-link-field">边类型 <select id="kp-edge-type"><option value="reference">引用 (reference)
- L4775: html += `<label class="-link-field">关联度 <input type="range" id="kp-edge-relevance" min="0" max="1" step="0
- L4776: eate-btn" class="-btn primary -btn--sm">新建</button>`;
- L4802: setStatus("抑制 contain 边…");
- L4805: setStatus(res.message 丨丨 "操作失败");
- L4808: setStatus("已抑制 contain 边");
- L4819: setStatus("恢复 contain 边…");
- L4822: setStatus(res.message 丨丨 "操作失败");
- L4825: setStatus("已恢复 contain 边");
- L4837: if (!window.confirm(`删除边 ${sourceId} → ${targetId} (${edgeType})？`)) return;
- L4838: setStatus("删除边…");
- L4841: setStatus(res.message 丨丨 "操作失败");
- L4844: setStatus("已删除边");
- L4917: setStatusError("请输入目标 KP id");
- L4921: setStatus("新建边…");
- L4924: setStatus(res.message 丨丨 "新建边失败");
- L4927: setStatus("已新建边");
- L5067: ta-alias-dismiss="${esc(alias)}" title="移除">×</button>`;
- L5074: const title = kind === "selected" ? "点击移到候选" : "点击应用到已选";
- L5084: d" data-desc-cand="${esc(text)}" title="点击填入描述">
- L5086: data-desc-dismiss="${esc(text)}" title="移除">×</button>
- L5094: : `<span class="-muted -kp-tags-empty">暂无已选别名</span>`;
- L5101: : `<span class="-muted -kp-tags-empty">同步 aux 或下方新建</span>`;
- L5104: <div class="-kp-tags-zone-head">已选别名 <span class="-muted">参与检索</span></div>
- L5108: <div class="-kp-tags-zone-head">别名候选</div>
- L5111: t" id="kp-alias-new-input" placeholder="新建别名" autocomplete="off" spellcheck="false" />
- L5112: dd-btn" class="-btn secondary -btn--sm">加入候选</button>
- L5126: : `<span class="-muted -kp-tags-empty">同步 aux 后显示描述候选</span>`;
- L5128: <div class="-kp-tags-zone-head">描述候选 <span class="-muted">点击填入上方描述框</span></div>
- L5140: : `<span class="-muted -kp-tags-empty">暂无已选别名</span>`;
- L5148: : `<span class="-muted -kp-tags-empty">同步 aux 或下方新建</span>`;
- L5158: : `<span class="-muted -kp-tags-empty">同步 aux 后显示描述候选</span>`;
- L5166: " data-tag-dismiss="${esc(tag)}" title="移除">×</button>`;
- L5174: kind === "selected" ? "点击移到候选" : "点击应用到已选";
- L5185: : `<span class="-muted -kp-tags-empty">暂无已选 tag</span>`;
- L5194: : `<span class="-muted -kp-tags-empty">点击「系统建议」或下方新建</span>`;
- L5197: <div class="-kp-tags-zone-head">已选 <span class="-muted">点击 chip 移到候选</span></div>
- L5201: <div class="-kp-tags-zone-head">候选 <span class="-muted">点击 chip 应用到已选 · × 移除</span></div>
- L5203: t-tags" class="-btn secondary -btn--sm">系统建议</button>
- L5205: ext" id="kp-tag-new-input" placeholder="新建 tag" autocomplete="off" spellcheck="false" />
- L5206: dd-btn" class="-btn secondary -btn--sm">加入候选</button>
- L5223: : `<span class="-muted -kp-tags-empty">暂无已选 tag</span>`;
- L5234: : `<span class="-muted -kp-tags-empty">点击「系统建议」或下方新建</span>`;
- L5457: setStatus(res.message 丨丨 "同步失败");
- L5488: setStatus(added ? `已从 aux 合并 ${added} 条候选` : "aux 已同步，无新候选");
- L5531: if (!items.length) setStatus("暂无 tag 建议");
- L5532: else if (added) setStatus(`已加入 ${added} 个系统候选`);
- L5548: `<p class="-config-hint">名称相近的知识点（仅供参考，合并需手动处理）：</p>` +
- L5649: setStatusError("无法确认", `终点第 ${end} 行超出正文（共 ${total} 行）`);
- L5655: setStatusError("无法确认", "提议缺少 id");
- L5659: setStatusError("无法确认", "提议缺少名称");
- L5665: setStatusError(check.message 丨丨 "目标 id 已存在", check.files?.join(", "));
- L5669: setStatusError("校验失败", String(e.message 丨丨 e));
- L5672: setStatus("确认知识点…");
- L5675: setStatusError(res.message 丨丨 "写入失败");
- L5688: setStatus("已确认知识点", `${kpId} L${start}–${end}`);
- L5715: setStatus("没有可确认的待确认项");
- L5720: `将直接确认 ${all.length} 个待确认提议（使用提议默认 id 与名称）。\nid 冲突或范围异常的项目会跳过，并在最后列出。`
- L5729: setStatus(`确认中… ${i + 1}/${all.length}：${p.name 丨丨 p.concept_id 丨丨 ""}`);
- L5737: skipped.push(`${kpName 丨丨 "?"}：缺少 id/名称`);
- L5741: skipped.push(`${kpName}：范围超出正文`);
- L5747: skipped.push(`${kpName}：id 已存在`);
- L5751: skipped.push(`${kpName}：校验失败 ${e.message 丨丨 e}`);
- L5756: ipped.push(`${kpName}：${res.message 丨丨 "写入失败"}`);
- L5774: setStatusError(`已确认 ${ok.length} 项 · 跳过 ${skipped.length} 项`, skipped[0]);
- L5776: `以下 ${skipped.length} 项未能自动确认：\n\n${skipped.join("\n")}\n\n可对具体项使用「配置并确
- L5779: setStatus(`已全部确认 ${ok.length} 项`);
- L5819: setStatus(`已跳转 · ${label}`, candidate.file);
- L5850: $("#link-confirm").textContent = "确认跳转";
- L5964: setStatus(res.message 丨丨 "推荐失败");
- L5970: setStatus(`已应用 ${tid} 推荐`, formatLinkRelevance(props.relevance));
- L5973: "智能推荐尚未接入",
- L5974: `${tid} 默认 ${formatLinkRelevance(res.default ?? defaultRelevanceForEdgeType(pr
- L5978: setStatus("推荐失败", String(e.message 丨丨 e));
- L6001: <span class="-link-target-edge-label">类型</span>
- L6003: {et === "reference" ? " selected" : ""}>引用</option>
- L6004: d"${et === "extend" ? " selected" : ""}>扩展</option>
- L6006: <span class="-link-target-edge-label">强度</span>
- L6007: x="1" step="0.05" value="${rel}" title="边强度 relevance" />
- L6008: ue="${formatLinkRelevance(rel)}" title="边强度 relevance" />
- L6009: data-target-edge-suggest="${pickIndex}">推荐</button>
- L6016: <div class="-link-section-title">图谱边</div>
- L6017: <p class="-muted -link-edge-empty">请先勾选跳转目标，再在此配置语义边属性</p>
- L6037: <div class="-link-section-title">图谱边</div>
- L6038: ss="-config-hint -link-edge-panel-hint">为下方每个已选目标单独设置语义关系。</p>
- L6064: if (!p.selectedOrder.length) return "请添加至少一个跳转目标";
- L6067: return "请填写匹配文本，并在下方添加跳转目标";
- L6147: if (!p.selectedOrder.length) return "请添加至少一个跳转目标";
- L6149: if (p.mode === "create") return `新建链接 · ${n} 个目标`;
- L6150: return n > 1 ? `已选 ${n} 个跳转目标` : "已选 1 个跳转目标";
- L6157: ml += `<label class="-link-field"><span>匹配文本</span>
- L6159: chor" value="${esc(text)}" placeholder="正文与预览中可见的文字，即跳转键" autocomplete="off" spellcheck="false" />
- L6181: <p class="-config-hint -link-meta-hint">对应正文 <code>[[匹配文本]]</code>。输入框为搜索词；保存时按所选匹配项采用正文 canonical 文本。</p>`;
- L6182: ml += `<div class="-link-section-title">点击后打开</div>`;
- L6183: class="-config-hint -link-targets-hint">勾选目标参与跳转。</p>`;
- L6188: pan><i class="-link-legend-dot sel"></i>已选（参与跳转）</span>
- L6189: <span><i class="-link-legend-dot"></i>未选（保留备选）</span>
- L6193: <i class="-link-legend-dot primary"></i>队首跳转</span>
- L6194: pan><i class="-link-legend-dot sel"></i>已选入队</span>
- L6195: <span><i class="-link-legend-dot"></i>未选</span>
- L6200: html += `<p class="-muted -link-empty">暂无目标 · 在下方添加 KP id 或文件 stem</p>`;
- L6216: tCls}" data-pick-dot="${i}" aria-label="选择">
- L6223: arget" data-remove-target="${i}" title="移除">×</button>` : ""}
- L6231: t" id="link-add-target" placeholder="id 或 tag:rl policy …" autocomplete="off" spellcheck="false" />
- L6234: type="button" id="link-add-target-btn">添加</button>
- L6238: <summary>智能匹配（M4 · tag / Lexical）</summary>
- L6239: <p class="-config-hint">输入 tag: 前缀按标签检索知识点；无匹配时可创建未绑定链接或新建知识点。</p>
- L6245: lass="-btn secondary -btn--sm" disabled>选用</button>
- L6263: `队首：${primary?.name 丨丨 candidateTargetId(primary) 丨丨 primary?.file}` +
- L6264: (rest > 0 ? ` · 另有 ${rest} 个将加入标签栏` : "");
- L6267: hint.textContent = "点击圆圈多选；再次点击队首可取消";
- L6440: setStatus("目标已存在", tid);
- L6465: setStatus("添加失败", String(e.message 丨丨 e));
- L6583: ? `创建链接 — ${displayText.slice(0, 32)}`
- L6584: : "创建链接";
- L6586: $("#link-title").textContent = `编辑链接 — ${displayText.slice(0, 32)}`;
- L6622: setStatus("发现已有路由", overrides[t].join(", "));
- L6633: $("#link-title").textContent = `多目标链接 — ${title}`;
- L6656: setStatus("保存失败", "至少选择一个目标");
- L6663: setStatus("保存失败", "请填写匹配文本");
- L6681: setStatus("保存中…");
- L6716: setStatus(res.message 丨丨 "保存失败");
- L6761: setStatus(applyRes.message 丨丨 "挂接失败");
- L6766: setStatus("保存失败", "请在弹窗中勾选正文匹配位置");
- L6780: setStatus("链接已保存", res.warning 丨丨 `${saveAnchor} → ${targetIds.join(", ")}`);
- L6790: setStatus("保存失败", String(e.message 丨丨 e));
- L6815: if (m.blocked) return "冲突";
- L6816: if (m.excluded) return "已排除";
- L6817: if (m.is_substring) return "子串";
- L6818: if (m.fuzzy) return "模糊";
- L6819: if (m.wrapped) return "已包裹";
- L6820: if (m.attached) return "已挂接";
- L6904: setStatus("扫描匹配…");
- L6914: setStatus("扫描失败", String(e.message 丨丨 e));
- L6918: setStatus(scan.message 丨丨 "扫描失败");
- L6950: setStatus(`匹配「${anchor}」`);
- L6956: setStatus("应用失败", "无匹配任务");
- L6962: "应用失败",
- L6964: ? "子串匹配不可挂接，请勾选带复选框的行"
- L6965: : "请先勾选要包裹的正文位置"
- L6972: setStatus("应用失败", "匹配文本不能为空");
- L6975: setStatus("应用匹配…");
- L6999: setStatus(res.message 丨丨 "应用失败");
- L7014: setStatus("链接已挂接", `${saveAnchor} · ${m.selected.size} 处`);
- L7026: setStatus("应用失败", String(e.message 丨丨 e));
- L7039: `从跳转入口移除此处？\n\nL${line 丨丨 "?"} · ${label}\n\n仅解除本处链接标记，保留跳转配置。`
- L7043: setStatus("无法定位行号", "请在预览中右键链接");
- L7046: setStatus("移除中…");
- L7055: setStatus(res.message 丨丨 "移除失败");
- L7065: setStatus("已从跳转入口移除此处");
- L7067: setStatus("移除失败", String(e.message 丨丨 e));
- L7076: `删除跳转入口「${anchor}」？\n\n将移除链接配置，并解除正文中所有 [[${anchor}]] 包裹（保留可见文字）。`
- L7079: setStatus("删除中…");
- L7083: setStatus(res.message 丨丨 "删除失败");
- L7098: setStatus("已删除跳转入口", anchor);
- L7100: setStatus("删除失败", String(e.message 丨丨 e));
- L7107: `移除链接标记并保留显示文字？\n\n${payload.displayText 丨丨 payload.targetId}`
- L7110: setStatus("移除中…");
- L7120: setStatus(res.message 丨丨 "移除失败");
- L7130: setStatus("已移除 [[]] 标记");
- L7132: setStatus("移除失败", String(e.message 丨丨 e));
- L7156: setStatus(res.message 丨丨 "多目标链接无法解析", "未绑定");
- L7180: setStatus(res.message 丨丨 `未找到 [[${target}]]`, "未绑定");
- L7189: setStatus(`未找到 [[${target}]]`, "未绑定");
- L7194: setStatus(`链接失败: ${target}`, String(e.message 丨丨 e));
- L7273: el.setAttribute("title", "跳转到 " + target);
- L7277: el.setAttribute("title", "未绑定目标: " + target);
- L7424: : ' <span class="-muted">（目标未入图谱）</span>';
- L7425: hint.innerHTML = `<span class="-muted">链接</span> ${esc(src)} → ${esc(tgt)}${note}`;
- L8324: setStatus("剪贴板为空或无法读取");
- L8336: setStatus("请先将光标置于源码编辑区");
- L9946: Path[0]) { log("STYLE", "_extract FAIL: 跨引用段"); return null; }
- L9949: log("STYLE", "_extract FAIL: 引用段为空/无效");
- L9952: k.type]) { log("STYLE", "_extract FAIL: 引用段不可编辑"); return null; }
- L10044: ck.type]) return { ok: false, message: "该内容不可编辑" };
- L10053: return { ok: ok, message: ok ? "" : "样式应用失败" };
- L10062: return { ok: false, message: "选区边界包含不可拆分元素或跨段落" };
- L10068: (!block) return { ok: false, message: "选区包含无效块" };
- L10069: ck.type]) return { ok: false, message: "选区包含不可编辑内容（表格/代码/公式等）" };
- L10070: = "list") return { ok: false, message: "暂不支持跨列表选区的样式应用" };
- L10071: ckquote") return { ok: false, message: "暂不支持跨引用块边界的样式应用" };
- L10081: if (!sp) return { ok: false, message: "选区边界不可拆分" };
- L10091: if (!ep) return { ok: false, message: "选区边界不可拆分" };
- L10137: uched > 0, message: touched > 0 ? "" : "样式应用失败" };
- L10149: matType)) return { ok: false, message: "不支持的样式类型" };
- L10390: setStatus("剪贴板为空或无法读取");
- L10396: setStatus("编辑器未就绪");
- L10405: setStatus("粘贴失败（当前位置不可编辑）");
- L10413: setStatus("已粘贴");
- L10734: '  <div class="-cp-title">添加自定义颜色</div>' +
- L10741: button type="button" class="-cp-cancel">取消</button>' +
- L10742: <button type="button" class="-cp-ok">确定</button>' +
- L10969: ad.innerHTML = '<span class="-ctx-head">自定义颜色 ' + hex + "</span>";
- L10980: del.textContent = "删除该颜色";
- L10986: setStatus("已删除自定义颜色 " + hex);
- L11023: btn.title = "自定义颜色 " + hex + "（右键菜单可删除）";
- L11047: openColorPicker("添加自定义颜色", (hex) => {
- L11051: setStatus("颜色 " + hex + " 已在色板中");
- L11058: setStatus("已添加自定义颜色 " + hex);
- L11075: return fmt === "highlight" ? "高亮" :
- L11076: fmt === "fontcolor" ? "字体颜色" :
- L11077: fmt === "bold" ? "加粗" : "斜体";
- L11138: setStatus("画笔已就绪：可继续点选其他样式，左键拖动涂抹一并应用；右键点击已选样式单独取消");
- L11159: setStatus("已取消画笔");
- L11163: setStatus("已取消" + _brushLabel(fmt) + "，其余样式保留");
- L11182: s.length) return { ok: false, message: "未选择任何样式，请先点击样式按钮或色块" };
- L11198: plied > 0, message: applied > 0 ? "" : "样式应用失败", applied };
- L11216: setStatus("画笔就绪：左键拖动涂抹应用" + _brushListLabel() + "，右键点击已选样式取消");
- L11226: setStatus("已应用" + _brushListLabel() + "，继续拖动涂抹或按 Esc 退出");
- L11320: setStatus("格式化仅支持单行内选择");
- L11355: setStatus("已应用样式");
- L11359: setStatus("样式应用失败");
- L11366: setStatus("请先在预览区选中文字，再使用「无色」");
- L11372: setStatus("请先在源码中选中文字");
- L11379: setStatus(res.message 丨丨 "格式化失败");
- L11391: setStatus(res.message 丨丨 "格式化失败");
- L11435: start_snippet_not_found: "起点未找到",
- L11436: end_snippet_not_found: "终点未找到",
- L11437: end_before_start: "终点在起点前",
- L11438: missing_snippet: "缺少 snippet",
- L11716: .split(/\n/)[0].trim().slice(0, 80) 丨丨 "新知识点";
- L11763: <span class="-muted">范围预览</span>
- L11764: view-toggle" role="tablist" aria-label="预览模式">
- L11765: ctive" : ""}" data-assist-view="source">源码</button>
- L11873: const text = `… 上文第 1–${viewStart - 1} 行`;
- L11885: const text = `… 下文第 ${viewEnd + 1}–${total} 行`;
- L11959: -assist-preview-ellipsis-top -muted">… 上文第 1–${viewStart - 1} 行</div>`;
- L11973: ssist-preview-ellipsis-bottom -muted">… 下文第 ${viewEnd + 1}–${total} 行</div>`;
- L11984: -assist-preview-ellipsis-top -muted">… 上文第 1–${viewStart - 1} 行</div>`;
- L12009: ssist-preview-ellipsis-bottom -muted">… 下文第 ${viewEnd + 1}–${total} 行</div>`;
- L12041: '<p class="-assist-preview-error">行号无效：终点不能早于起点</p>';
- L12154: setStatusError(res.message 丨丨 "保存失败");
- L12175: setStatusError("保存失败", "名称不能为空");
- L12181: `将知识点 id「${oldId}」重命名为「${newId}」？\n全库链接配置与正文 [[…]] 将同步更新，显示文字不变。`
- L12186: setStatus("迁移 id…");
- L12189: setStatusError(renameRes.message 丨丨 "id 迁移失败");
- L12203: ? `${renameRes.sidecar_files.length} 个文件配置 · ${renameRes.md_replacements 丨丨 0} 处正文`
- L12205: setStatus(`id 已迁移 ${oldId} → ${newId}`, stats);
- L12207: setStatus("保存配置…");
- L12209: eloadDocAfterKpChange(res, panel.kpId, `已更新 ${panel.kpId}`);
- L12228: setStatus("保存配置…");
- L12241: eloadDocAfterKpChange(res, panel.kpId, `已更新标签 · ${panel.kpId}`);
- L12267: `删除知识点「${label}」？\n配置中的条目将移除；正文不会被改写。`
- L12272: setStatus("删除中…");
- L12275: setStatus(res.message 丨丨 "删除失败");
- L12284: setStatus("已删除知识点", kpId);
- L12339: setStatusError("行号无效", "终点不能早于起点");
- L12344: "行号无效",
- L12345: `终点第 ${end} 行超出正文（共 ${total} 行）；请使用编辑器行号，不是含 frontmatter 的文件行号`
- L12350: setStatusError("无法保存", `起点第 ${start} 行为空，请选择有内容的行`);
- L12354: setStatusError("无法保存", `终点第 ${end} 行为空，请选择有内容的行`);
- L12369: setStatusError("保存失败", "id 不能为空");
- L12373: setStatusError("保存失败", "名称不能为空");
- L12379: setStatusError(check.message 丨丨 "目标 id 已存在", check.files?.join(", "));
- L12383: setStatusError("校验失败", String(e.message 丨丨 e));
- L12388: setStatus("保存配置…");
- L12398: setStatusError(res.message 丨丨 "写入失败");
- L12423: setStatus(created ? "已创建知识点" : "已更新 range", `${kpId} L${start}–${end}`);
- L12516: ? `仅搜索 ${basename(state.currentPath)}`
- L12517: : "请先打开文件";
- L12524: ? fileBtn?.title 丨丨 "仅当前文件（点击切换全库）"
- L12525: : "搜索全库（点击切换文件）";
- L12536: setStatusError("请先打开文件");
- L12559: return `语义 ${Math.round(sem)}%`;
- L12563: if (lex != null && lex > 0) parts.push(`字 ${Math.round(lex)}`);
- L12564: if (sem != null && sem > 0) parts.push(`语义 ${Math.round(sem)}%`);
- L12566: const tierLabel = { high: "高", medium: "中", low: "低" }[hit.tier] 丨丨 hit.tier;
- L12572: const tierLabel = { high: "高", medium: "中", low: "低" }[hit.tier] 丨丨 hit.tier;
- L12573: return `字 ${Math.round(lex != null ? lex : hit.score 丨丨 0)} · ${tierLabel}`;
- L12575: if (lex != null) return `字 ${Math.round(lex)}`;
- L12593: setStatusError("请先打开文件");
- L12598: setStatus("搜索…", q + scopeHint);
- L12604: setStatusError("搜索失败", res.message 丨丨 "");
- L12613: let hint = `无匹配 · ${esc(q)}`;
- L12620: embedding_not_enabled: "语义搜索未开启（设置 → 检索）",
- L12621: embedding_not_installed: "未安装 sentence-transformers",
- L12622: embedding_index_empty: "语义索引为空",
- L12627: hint += " · 可在设置→检索开启「搜索正文定位」";
- L12646: += `<div class="-search-section-label">正文定位</div>`;
- L12652: est-score -suggest-score--muted" title="正文行匹配">L${esc(String(it.line 丨丨 ""))}</span>
- L12682: setStatus("搜索完成", `${total} 条 · ${q}${scopeHint}`);
- L12685: setStatusError("搜索失败", String(e.message 丨丨 e));
- L12828: setStatusError("请先打开文件");
- L12861: iteText(ta.value).then(() => setStatus("冲突报告已复制")).catch(() => { document.execCommand("copy"); setStatus("冲突报告
- L12921: setStatus(`已定位 L${ln}`, match?.is_substring ? "子串匹配，不可挂接" : "点击复选框可挂接");
- L12989: setStatus(`已定位 L${ln}`, match?.is_substring ? "子串匹配，不可挂接" : "");

## 迁移记录

| 日期 | 文件/区域 | 动作 | 结果 |
|------|------|------|------|
|（初始生成）| 全部 | 盘点登记 | — |

