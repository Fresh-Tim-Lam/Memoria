# Memoria {version} — Windows 发布包

双击运行：

  Memoria.exe

同目录还需要保留（不可单独拷贝 exe）：

  lib/              程序依赖
  resources/        示例知识库、图标、内置 Agent 整理提示词（agent-prompts/）
  config/           程序设置（首次运行后自动生成 ui-settings.json）
